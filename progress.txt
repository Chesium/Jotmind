# Ralph Progress Log
Started: Sat 13 Jun 2026 08:14:49 PM +08
---

## Codebase Patterns
- Verification gates (US-034): `pnpm verify` is the authoritative NO-INFRA gate and MUST stay green without a DB/API/Ollama. AI safety = `test:api` runs with `JOTMIND_VERIFY=true`, loading the Vitest setupFile `apps/api/src/test/verify-env.ts` (in vitest.config.ts `setupFiles`) which strips real provider key env + rejects non-`mock` AI_PROVIDER_CONFIG (it checks the STARTING env at file load, so tests setting mock dynamically are fine). DB/API acceptance is the opt-in `pnpm verify:db` (`test:api:db` migrates+runs FULL API suite incl all integration files vs the compose DB; `test:e2e:real` sets E2E_REAL_API=1 -> Playwright `real-chromium` project boots `@jotmind/api start` + vite via a webServer ARRAY, mock AI injected via the webServer `env` block NOT inline shell because JSON braces/commas break shell env assignment; `e2e/global-setup.ts` runs db:migrate + db:reset:test). The real-flow spec (`e2e/v1-flows.real.spec.ts`) must `page.reload()`+reselect the KB after creating records before a sibling per-KB component (Claims) reads them (no live-sync). Local Ollama = manual `pnpm verify:local-ai` ONLY: name files `*.local-ai.test.ts`, EXCLUDE them in vitest.config.ts (vitest exclude blocks even an explicit CLI path, so run them via a separate `vitest.local-ai.config.ts`). `db:reset:test` (TRUNCATE public) is double-guarded by DATABASE_URL + JOTMIND_ALLOW_DB_RESET. Runtime stubs documented in fix_plan.md; stub projector surfaced in GraphViews via `graph-projector-stubbed` banner.
- Deployment/runtime (US-033): the production API is run with `tsx src/server.ts` (the `start` script), NOT `node dist/server.js` — because `@jotmind/schemas` exports TS SOURCE (`./src/index.ts`), a compiled `node dist/...` crashes with `ERR_UNKNOWN_FILE_EXTENSION ".ts"`. tsx is the same runtime as `dev`/`worker`/`db:migrate`. Do NOT "fix" this by pointing schemas exports at built dist; that would break the no-build-step Vite/Vitest/tsc/tsx resolution and the green `pnpm verify`. The reference container (`infra/api/Dockerfile` + `api` service in docker-compose.yml) is stateless and runs via tsx; `.dockerignore` keeps host node_modules/dist out of the build context. Single-origin web serving = `createApp({webDistPath})`/`WEB_DIST_PATH` -> after all `/api` routers an explicit `app.use('/api',=>404 JSON)` MUST precede `express.static` + the regex SPA fallback `/^\/(?!api(?:\/|$)).*/` so unknown API routes aren't swallowed. CORS is deny-by-default in `apps/api/src/cors.ts` (exact-origin allowlist, never `*`, credentials true, preflight 204/403) wired via `createApp({allowedOrigins})`/`CORS_ALLOWED_ORIGINS`. The web app's API base URL is build-time configurable via `import.meta.env.VITE_API_BASE_URL` -> all `apps/web/src/api.ts` calls go through `apiFetch`/`apiUrl` (add new web API calls via those, not bare `fetch`). Deployment docs: `docs/deployment.md`.
- Portable export/import (US-032): a full-fidelity per-KB backup = shared Zod doc `portableKnowledgeBaseExportSchema` (@jotmind/schemas exports.ts) reusing existing record schemas + carrying FULL schema version history (`versions[]`, not just activeVersion). Build it in `apps/api/src/exports/service.ts buildPortableExport` by reading every store; sanitize EVERY JSONB column with `sanitizeJsonb` (deep-strips `AI_PROVIDER_SECRET_FIELDS` e.g. apiKey, AC5). Import (`importPortableKnowledgeBase`) ALWAYS creates a NEW KB (no merge/dedupe), regenerates ids through the canonical create paths, and remaps cross-refs via Maps (entity/note/source/claim/version). PREFLIGHT-validate all refs and throw `PortableImportError`->400 BEFORE creating the KB (canonical stores own their own tx, so there's no single import tx — preflight is the safety net). Schema version history is replayed createDefinition(v1)+updateDefinition(later) and mapped old->new by version NUMBER so records keep their old version ref. Export routes need ADMIN+ KB role; import is mounted at sibling `/api/knowledge-bases/import-portable` (KB router has no matching POST -> falls through) and only needs authed+CSRF. To export a new KB-scoped store you must add a `listX` read; for full version history add `schemaStore.listDefinitionsWithVersions` (and update the createMemorySchemaStore fake when you extend the SchemaStore interface, or every entity/claim/module test breaks). GOTCHA: schema PUT update body is top-level `{propertySchema|spec}` (a breaking/tightening change makes a new version; loosening updates in place).
- Built-in domain Modules (US-029): a Module is an installable bundle of US-027 schema defs + US-022 rule pack refs, defined as static content in @jotmind/schemas modules.ts (BUILTIN_MODULES). Install in the ROUTER (apps/api/src/modules/index.ts) reusing the injected schemaStore.createDefinition + ruleStore.installRulePack — NO new store, NO migration — so created content uses canonical write paths + audit and is idempotent (duplicate_name/already-installed -> skipped). Mount AFTER the KB router; createApp reuses already-resolved schemaStore/ruleStore/kbStore/authStore seams. propertySchema only supports string/number/boolean/date; model "uncertainty" as an extra precision string; entity name/aliases/tags/notes are first-class (not custom props). To add another domain Module (US-030), just append to BUILTIN_MODULES.
- Custom schema validation (US-027): entity-type propertySchema + claim-predicate spec validation runs in the entity/claim ROUTERS (not stores) via shared checkEntityAgainstSchema/checkClaimAgainstSchema (apps/api/src/schema-defs/index.ts), which look up the active schema_versions row by conceptual name/predicate, validate, and return the schemaVersionId to STAMP on the record (AC3/AC5). No custom schema for a type/predicate => unconstrained (schemaVersionId null) — so existing built-in-type records keep working. The validation logic itself (validateEntityProperties/validatePredicateArguments) lives in @jotmind/schemas so API + web share it. GOTCHA: createApp resolves `schemaStore = options.schemaStore ?? dbSchemaStore` and injects it into the entity/claim routers, but those routers DON'T self-default schemaStore (validation is skipped when absent). So any DB-free unit test that builds the app via createApp and POSTs entities/claims MUST inject a schema store — reuse `createMemorySchemaStore()` exported from apps/api/src/schema-defs/schema-defs.test.ts (no constraints by default). schema_definitions/schema_versions tables already exist from US-005 (no migration); schemas are audit-only (not graph projection targets).
- Apache AGE access (US-026): AGE needs `LOAD 'age'` + `SET search_path = ag_catalog,...` PER SESSION, but postgres-js pools connections. Wrap ALL AGE work in `withAge(fn)` = `getSqlClient().begin(async tx => { LOAD; SET LOCAL; ensure graph; fn })` (begin pins one connection). CRITICAL: AGE's 3rd `cypher()` arg must be a BARE bind param inferred as agtype — `$1::agtype` / a literal are REJECTED ("third argument of cypher function must be a parameter"). postgres-js sends interpolated `${JSON.stringify(params)}` as a type-inferred bind param so it works AND escapes user text — NEVER concatenate user strings into the Cypher body. Build providers/services via the `withAge` seam; AGE is a DERIVED index (never canonical) so failures can't corrupt records; default projector is now ageProjector (override with GRAPH_PROJECTOR=stub; unit tests inject a stub/fake). Rebuild runs as a durable job and drives a singleton `graph_projection_status` (state rebuilding/synchronized/failed). NOTE: that status row has an FK to knowledge_bases, so `TRUNCATE knowledge_bases CASCADE` in integration tests wipes it — the store's get() returns a synchronized default when missing, and AGE persists between test runs (call ageProjector.prepareRebuild() in setup/teardown to clear it).
- Drizzle raw `sql` WHERE with OR: when ANDing multiple `sql\`A OR B\`` fragments inside `and(...)`, WRAP EACH in its own parens (`sql\`(A OR B)\``). SQL binds AND tighter than OR, so unparenthesized `cond1 AND (validStart IS NULL OR validStart<=now()) AND (validEnd IS NULL OR validEnd>=now())` silently becomes `(cond1 AND validStart IS NULL) OR (validStart<=now() AND validEnd IS NULL) OR (validEnd>=now())` and leaks rows. Caught only by an integration test (unit/in-memory fakes won't catch it) — always add a DB integration test for any hand-written valid-time/soft-delete filter.
- Rule/DSL execution (US-024): keep the EVALUATOR a pure function in apps/api (engine.ts: executeRule(ast, facts)) that takes already-filtered facts and NEVER touches the DB — so it unit-tests with plain fixtures and there's no eval/network/fs. Put the DB fact-loading (with hidden deleted_at + valid-time predicates) behind a separate injectable store (RuleRunStore.loadFacts) so the router wires engine+store and both halves test independently (engine unit + loadFacts integration + router unit with an in-memory RuleRunStore). Derived/operational records (rule_runs, inferred_results) have NO soft-delete and are NOT graph projection targets (audit-only writes); label inferred output distinctly and never write it into canonical tables.
- Restricted-DSL parsing (rules US-023): put pure parser/validator logic in @jotmind/schemas (like validateCustomProperties) so BOTH the API (save/enable gating) and web (live preview) call the SAME function. parseRule returns {valid,errors,recursive,recursionCap,rule:AST|null}; it NEVER eval's (data only) so there's no fs/network/JS access. Store the compiled AST + validation in the existing JSONB `compiled` column under an `authored` key (parallel to US-022's `builtin` key) — no migration. Surface validity by RE-PARSING ruleText in the router's toRule mapper (single source of truth) rather than trusting stored flags, so built-in + authored rules are consistent and enable-gating can't be bypassed. For "save invalid as draft but block enable" (a common pattern): always create as 'draft', and gate the enable transition in the ROUTER (re-parse, 422 on invalid) — keep the status-setter store method dumb.
- pgvector in Drizzle: there is no native vector column type. Define a `customType<{data:number[];driverData:string}>` named `vector` in db/schema.ts (dataType()->'vector', toDriver -> '[1,2,3]', fromDriver parses it). Declare it WITHOUT a fixed dimension to hold any provider's dim (mock=8). drizzle-kit generate emits `"col" vector NOT NULL` correctly. For nearest-neighbor use RAW sql `embedding <=> ${'[..]'}::vector` (cosine distance; `1 - (...)` = similarity); exact search works fine at per-KB scale without an index. After db:generate, run prettier --write on the new drizzle/meta/*.json + 0005_snapshot.json (they trip format:check).
- Background-job handlers (jobs/handlers.ts) must be SELF-CONTAINED: resolve their own DB stores + env-config providers inside the handler (NOT injected) so the SAME registry works in both the in-process worker and worker-process.ts. Mirror the embeddings.index handler: parse job.payload with its Zod schema, build deps from db<Store>s + resolve*FromEnv(), call the pure runner.
- AI feature env resolution: every AI consumer (extraction US-017, command US-019, answers US-020, embeddings US-021) reads provider config from the SAME `AI_PROVIDER_CONFIG` JSON env var via a `resolve*FromEnv` mirror of resolveExtractorFromEnv. The router default uses it; tests set/unset process.env.AI_PROVIDER_CONFIG. Remote-vs-local gating reuses resolveAiPolicy (US-016) strictest-wins; embeddings additionally require `resolved.remoteEmbeddingsAllowed` (the separate consent) before a remote provider runs.
- AI provider config is ENVIRONMENT-ONLY (no persisted provider store/table). To send provider info to the browser, ALWAYS build it via `buildAiProviderStatus(config|null)` (@jotmind/schemas, US-046) — an ALLOWLIST of display-safe fields (kind/name/classification/remote/demo/verified/llmModel/embeddingModel/embeddingsSupported/baseUrl) so secrets (apiKey) can never leak (same approach as toPortableProviderConfig). NEVER JSON-serialize a raw AiProviderConfig to a client. Status is served at authed-only `GET /api/ai/provider` (apps/api/src/ai/status.ts resolveAiProviderStatusFromEnv → createAiPolicyRouter); web reads it via getAiProviderStatus() into the read-only `<AiProviderStatusPanel/>` (AiPolicySettings.tsx). Classification: openai/anthropic='remote', mock='demo' (label as deterministic demo, AC5), else 'local'. Readiness `verified=!remote` (remote ships configured-but-untested, matching the extraction/answers/command `verified` convention). The status carries editable:false/configSource:'environment' so the UI states config can't be edited in the browser — US-047 (provider editing) must keep this unless it adds a real persisted provider store.
- Provider setup UI (US-047): while provider config is environment-only, DO NOT render disabled/placeholder provider edit forms. `<AiProviderStatusPanel/>` should show sanitized read-only status plus actionable `AI_PROVIDER_CONFIG`/deployment-secret guidance, and tests should assert no textbox/select/button provider controls exist so users never enter API keys into non-functional UI.
- Environment has ONLY Node 18. Pin tooling to Node-18-compatible versions: Vite 5, Vitest 2, ESLint 9, TypeScript 5.x, pnpm 9. Latest pnpm needs Node 22+.
- pnpm is installed globally at ~/.npm-global/bin (npm prefix set to ~/.npm-global). Prepend `~/.npm-global/bin` to PATH in every shell: `export PATH="$HOME/.npm-global/bin:$PATH"`.
- Monorepo = pnpm workspace (`apps/*`, `packages/*`). Shared package `@jotmind/schemas` exports TS source (`./src/index.ts`), NOT built dist, so tsc (moduleResolution: Bundler) + Vite resolve directly with no build step.
- Do NOT add TS project `references` from apps to packages/schemas — composite refs make tsc resolve unbuilt dist/*.d.ts and fail with TS6305. Rely on package `exports` -> source.
- tsconfig.base.json is strict with `verbatimModuleSyntax` -> import types with `import { type X }`. Relative imports use `.js` extensions even in .ts/.tsx (ESM).
- Validation: `pnpm verify:quick` (format:check+typecheck+lint+test) for fast loop, `pnpm verify` for full (adds test:api, test:e2e). `pnpm test` excludes @jotmind/api (run via `pnpm test:api`).
- Playwright browsers must be installed once: `pnpm --filter @jotmind/web exec playwright install chromium`.
- Prettier ignores Ralph scaffolding (prd.json, progress.txt, prompt.md, .agents, etc.) via .prettierignore. Add new non-source top-level files there if they break format:check.
- DB stack = PostgreSQL 18 + Apache AGE 1.7.0 + pgvector 0.8.1 via infra/db/Dockerfile (FROM apache/age:latest, compile pgvector). pgvector MUST be >= v0.8.1 (older fails to build on PG 18). PG 18 docker volume MUST mount at /var/lib/postgresql (NOT /data) or the container exits.
- ORM = Drizzle (drizzle-orm + postgres-js + drizzle-kit) in apps/api. Schema: apps/api/src/db/schema.ts; generate: `pnpm --filter @jotmind/api db:generate`; apply: `db:migrate`. Commit generated apps/api/drizzle/** (SQL + meta/).
- DB access gated on DATABASE_URL: when unset, checkDatabaseHealth() -> configured:false and /api/health stays ok. DB-needing tests use describe.skipIf(!process.env.DATABASE_URL) so default test:api is green without infra.
- DB-backed routes: use an injectable store interface (e.g. AuthStore) + DB impl + in-memory fake; inject via createApp options (createApp({checkDatabase, authStore})). Lets route handlers be unit-tested with no live DB. Mirror this for new domains.
- @node-rs/argon2 `Algorithm` is an ambient const enum -> cannot value-import under verbatimModuleSyntax. Use numeric literal: `const X: Algorithm = 2 as Algorithm` (Argon2id===2). Same caution for any ambient const enum dependency.
- Auth: Argon2id passwords, PG-backed sessions (sessions table), HTTP-only `jm_session` cookie, CSRF synchronizer token (`jm_csrf` readable cookie + `x-csrf-token` header) on cookie-auth mutations. System account role (admin|member) is separate from KB-scoped roles (US-004+). Web mutations need credentials:include + x-csrf-token.
- dev-browser skill is NOT available in this env. For required browser verification of UI stories, use Playwright + Chromium (installed) against the running API (write a temp e2e spec, run, screenshot, then delete it).
- Canonical graph writes (entities US-008, claims US-009, etc.) must write the relational row, enqueueGraphOutbox(tx,...), AND an audit_events row in ONE transaction. dbGraphWriteStore (graph/store.ts) only does outbox; per-domain stores (dbEntityStore) add the audit insert. Reads filter `deleted_at IS NULL` and scope by knowledgeBaseId.
- Nested KB-scoped routes: a separate Router({mergeParams:true}) mounted at /api/knowledge-bases/:kbId/<thing> (AFTER the KB router, which has no matching nested route so it falls through). Reuse kbStore.getRole in a local requireKbRole(min): 404 for non-members (hide existence), 403 for too-low role. Reads need viewer; mutations need editor + requireCsrf.
- Web interaction tests: @testing-library/user-event is NOT installed — use fireEvent from @testing-library/react. For real-browser verification, write a TEMP playwright config with two webServers (API w/ DATABASE_URL env + reuseExistingServer:true, web dev), truncate DB users first (so first-run setup shows), run, screenshot, then delete the temp files.
- Background work = durable `jobs` table (US-007). Enqueue via dbJobStore.enqueue({type,...}); register handlers in apps/api/src/jobs/handlers.ts:defaultJobHandlers. Workers claim atomically via claimNext (FOR UPDATE SKIP LOCKED in a tx); runNextJob handles capped-exponential-backoff retry + dead-letter. Two run modes share runNextJob: `pnpm --filter @jotmind/api worker` (separate process) and startInProcessWorker (WORKER_INLINE=true in server.ts). Don't invent new background loops.
- Drizzle dynamic SQL: a helper that takes table COLUMNS to build conditions must type the param as `SQLWrapper` (or `AnyColumn|SQL`), NOT `SQL` (a PgColumn is not an SQL). `sql\`${col}::text ILIKE ${pattern}\`` works for text AND JSONB columns; cast `::text` to search JSONB array/object contents. Escape LIKE wildcards (\ % _) in user input. `db.execute<T>(sql)` returns the rows array directly; use information_schema/to_regclass to feature-detect tables. For req.query schemas use z.coerce.number + union/transform for booleans.
- Only entity/claim/note/source are GRAPH_TARGET_TYPES (project to AGE via outbox). Records of those kinds emit graph_outbox + audit in one tx; NON-projected records (e.g. source_excerpts) emit audit ONLY (no enqueueGraphOutbox). Add a kind to GRAPH_TARGET_TYPES in @jotmind/schemas graph.ts before enqueuing outbox for it. New KB-scoped domain = mirror entity/claim: store interface + db<Domain>Store + in-memory fake test + Router({mergeParams:true}) mounted AFTER the KB router + createApp seam.
- Web: App.tsx is ~1.7k lines — put new large components in their OWN file (e.g. apps/web/src/GraphViews.tsx) and import into App.tsx; don't keep growing App.tsx. Per-KB sibling components (<Entities>/<Claims>/<Capture>/<GraphViews>) each fetch their own data once per kb.id and DON'T live-sync — records created in one won't show in another until reload+reselect (browser tests must reload+reselect after creating). Watch for testid collisions across these sections (e.g. sources-empty) — scope RTL/Playwright queries to the section's container testid.
- Selected-KB invalidation bus wiring (US-035 mechanism; US-036+ wire panels): the lightweight domain-keyed pub/sub in apps/web/src/invalidation.tsx is the cross-panel refresh mechanism (replaces the "reload+reselect" workaround above). A MUTATING panel calls `const invalidate = useInvalidate()` then, after a successful write, `invalidate([...domains])`. A READING panel calls `useInvalidationEffect([...domains], () => void refresh())` to re-run its existing loader (the bus does NOT cache data — it only signals "domain changed"). Default context is a NOOP_BUS so panels still work in isolation/in unit tests; <InvalidationProvider key={selectedKb.id}> (in App.tsx) shares one bus per selected KB. Publish the domains a mutation actually affects (e.g. entity delete/merge -> ['entities','claims'] because merges retarget claim args server-side); subscribe to every domain a panel reads. Regression-test cross-panel sync WITHOUT reload by rendering <App/>, selecting a KB, mutating in one panel via a state-based fetch mock (GET flips after POST), and asserting the sibling panel updates (see App.test.tsx US-036/US-037 tests). US-036 wired Entities(pub)->Claims(sub); US-037 made GraphViews a SUBSCRIBER of ['entities','claims','notes','sources','sourceExcerpts','graphViews'] (its refresh() reloads all relational data + best-effort projection status, preserving banners/AGE fallback) and added 'graphViews' to Entities/Claims/Capture publishes (Claims & Capture had no publishes before; Capture deletes also invalidate 'sourceExcerpts' since citations get removed). US-038 made Capture a SUBSCRIBER of ['claims']. US-039 made Proposals.tsx accept() a PUBLISHER of ['proposals','entities','claims','notes','sources','sourceExcerpts','graphViews','searchResults','answers','audit'] (publish the union of domains a SERVER-SIDE accept mutates, not just what the UI shows), made Entities a SUBSCRIBER of ['entities'] (was publish-only), and widened Capture's subscription to ['claims','notes','sources','sourceExcerpts']. US-040 wired Imports as a PUBLISHER (submit -> ['jobs','audit']; completion-detection -> ['proposals','jobs','audit'] when a job goes non-terminal->terminal, driven by the 'Refresh status' button AND a new self-limiting setInterval poll that runs only while hasPendingJobs), made Proposals a SUBSCRIBER of ['proposals'], and made KbAdmin a SUBSCRIBER of ['jobs','audit'] (refactored its one-shot inline useEffect into useCallback load() + useEffect + useInvalidationEffect so job/audit status can't go permanently stale). US-041 made Rules.tsx accept-inferred-result a PUBLISHER of ['claims','graphViews','searchResults','answers','audit']. US-042 made Modules.tsx a PUBLISHER (install -> ['modules','schemas','rules','audit']) + SUBSCRIBER ['modules'], made SchemaDefinitions a SUBSCRIBER of ['schemas'], and made Rules a SUBSCRIBER of ['rules']. US-043 made Search/CommandBox (App.tsx) + Answers (Answers.tsx) SUBSCRIBERS that MARK-STALE (not refresh/auto-rerun): each adds a `stale` useState + useInvalidationEffect(RESULT_STALE_DOMAINS, () => setStale(true)) and renders a `<p data-testid="{search|command|answers}-stale">` banner when `response && stale`; setStale(false) on a successful (re)run. RESULT_STALE_DOMAINS + RESULT_STALE_MESSAGE ('Graph data changed. Rerun this search for current results.') are EXPORTED from invalidation.tsx (NOT App.tsx) so Answers.tsx can import them without a circular dep. Mark-stale (vs auto-rerun) means no network loop + the query draft is never touched. US-044 is the ONE case that does NOT use the bus: a portable JSON import (in KbAdmin) creates a NEW KB in the `KnowledgeBases` PARENT list, which lives OUTSIDE the per-selected-KB <InvalidationProvider> (the bus resets per selected KB + has no `knowledgeBases` domain), so it threads a documented localized parent-ref escape hatch instead — `KnowledgeBases` passes its `refresh` down as `onKbListChanged` -> KbAdmin -> ExportBackup, called after a successful import so the imported KB becomes selectable without reload (US-035 AC3 sanctions this with a code comment). US-045 wired the `aiPolicy` domain: KbAiPolicy (App.tsx automation column, INSIDE the bus) on KB-save publishes `invalidate(['aiPolicy'])` AND subscribes `useInvalidationEffect(['aiPolicy'], reload)` to re-fetch its effective policy; the AI-availability surfaces CommandBox/Answers (App.tsx) + quick capture (Proposals.tsx) each subscribe `['aiPolicy']` to set a SEPARATE `policyStale` flag (distinct from US-043's graph `stale`) that renders a `<p data-testid="{command|answers|capture}-ai-policy-stale">` banner with the new EXPORTED `AI_POLICY_STALE_MESSAGE` ('AI policy changed. Rerun for current AI availability.') when there is a `response`/`lastResult`; cleared on the next (re)run. SERVER/USER policy is edited in <AiPolicySettings>, which renders in AuthedHome OUTSIDE the per-KB bus — so it CANNOT publish onto the bus. Cross-boundary escape hatch (mirrors US-044 direction-wise): AiPolicySettings calls a new `onServerOrUserPolicyChanged` prop after a server/user save; AuthedHome bumps an `aiPolicyVersion` counter passed down to KnowledgeBases; a tiny in-bus bridge `<AiPolicyInvalidator version={aiPolicyVersion}>` rendered INSIDE <InvalidationProvider> publishes `invalidate(['aiPolicy'])` whenever the version changes (skips initial mount via a useRef seen-check) — reaching KbAiPolicy + the availability surfaces. GOTCHA: effective policy is STRICTEST-WINS over server+user+KB (off<local_only<remote*); a KB-only change to local_only keeps effective `off` while server/user stay at the default `off`, so a browser test that wants to SEE effective change must raise ALL THREE layers (the unit test mocks the GET response so it can assert the re-fetch wiring with a flipped effective regardless). No embeddings UI exists yet (US-048), so AC4's "embeddings UI" surface is N/A this story. No API/schema/migration changes — pure web wiring. BROWSER-VERIFY GOTCHA: :3001 is usually taken by a concurrent dev API (don't kill it) — run your own API on a free port + temporarily repoint the vite proxy at it (SameSite=Lax cookies need SAME-ORIGIN, so cross-origin base-url+CORS logs you out); revert vite.config + remove temp spec after. TWO MORE: (a) start the throwaway API/vite with `setsid pnpm exec ...` (NOT plain nohup, which gets reaped between Bash tool calls); (b) NEVER `pkill -f "tsx src/server.ts"` — it matches the dev :3001 API too; kill the throwaway stack by PORT->pid.
- Reviewable imports (US-031): an import = a durable job (IMPORT_JOB_TYPE) whose handler parses the payload into candidate ProposalChange[] and stores ONE pending Proposal of kind 'import' — it NEVER writes canonical records, so the existing US-018 review/accept path applies them. Keep PARSING pure + shared in @jotmind/schemas (parseCsv + buildImportChanges) so it unit-tests with fixtures; keep the runner (runImport) a thin function over an injectable ProposalStore (no DB) so the self-contained job handler just wires dbProposalStore. List import jobs at a viewer-visible GET /imports (filter jobStore.list by type) for AC4 status — the /api/jobs admin endpoint is NOT user-visible. The proposal change discriminated union grew create_note/create_source (so text/Markdown import is also reviewable, not a direct note write); when you add an op, update applyProposalChanges AND every op-switch (web describeChange). The dev :3001 API runs NO worker, so durable-job stories (US-021/026/031) need WORKER_INLINE/worker-process to actually process — for browser verify, drain once via a temp tsx calling processAvailableJobs({store:dbJobStore,handlers:defaultJobHandlers}).
- Applying a queued change (proposal accept US-018, imports US-031) belongs in the ROUTER, not the store: inject entityStore/claimStore and call their createEntity/createClaim so the new records reuse the canonical write path (graph_outbox + audit per record). The queue's own store only mutates its row. Pre-validate references (e.g. claim entity-arg refs via entityStore.getEntity) BEFORE any mutation; re-parse the stored payload with its Zod schema so invalid data can't execute. Queue rows are pending-only for review actions (return undefined for non-pending -> 404/409 to block double-apply).
- Claims have a first-class provenance store field: CreateClaimInput.provenance -> claims.provenance JSONB (US-018). Use it (not properties) for origin/source/provider/acceptedBy/timestamp metadata. The claims.provenance column existed since US-005, so no migration is needed to populate it.
- Browser-testing AI extraction: capture availability is strictest-wins over server+user+KB AI policy and a fresh KB's missing policy resolves to OFF, so set ALL THREE layers (ai-policy-server/-user + kb-ai-policy, testids <prefix>-mode/-save) to local_only AND start the API with AI_PROVIDER_CONFIG='{"kind":"mock","name":"..."}' + AI_DEMO_EXTRACTION=true so resolveExtractorFromEnv builds the MockGraphExtractor. CredentialFields inputs have NO testids — use getByLabel('Email')/('Password').

## Sat 13 Jun 2026 20:25 +08 - US-001
Thread: https://ampcode.com/threads/T-019ec0ea-b9f7-74a4-8859-43f08d85d8f4
- Created the typed pnpm monorepo foundation.
- apps/api: Express app with `/api/health` returning a schema-validated HealthStatus; src/app.ts (testable factory) + src/server.ts (binds 127.0.0.1:3001 by default).
- apps/web: React 18 + Vite 5 app rendering API health; proxies /api to :3001. Vitest (jsdom + testing-library) unit test + Playwright e2e smoke test.
- packages/schemas: shared Zod healthStatusSchema + parseOrThrow util, imported by both apps.
- Root tooling: package.json scripts (format, format:check, typecheck, lint, test, test:api, test:e2e, verify:quick, verify), eslint.config.js (flat), .prettierrc.json, .prettierignore, tsconfig.base.json, pnpm-workspace.yaml, .gitignore, README.md, AGENTS.md.
- Verified: `pnpm verify` fully green; API boots & curl /api/health returns valid JSON; web `vite build` succeeds; Playwright smoke test passes.
- **Learnings for future iterations:**
  - See Codebase Patterns above (Node 18 pinning, PATH for pnpm, schemas-resolve-from-source, no TS project references, .js import extensions).
  - The web e2e smoke test runs web-only; /api proxy errors in logs are expected/benign because the API isn't started during `pnpm test:e2e`. Future stories that need real API in e2e should start it (or run via root).
  - US-002 next: PostgreSQL + Apache AGE + pgvector via Docker Compose, Drizzle migrations, and a real DB-backed health check (extend /api/health).
---

## Sat 13 Jun 2026 20:42 +08 - US-002
Thread: https://ampcode.com/threads/T-019ec0f2-8f1d-770c-9dea-473056778dea
- Provisioned the canonical PostgreSQL store with Apache AGE + pgvector and wired DB health into the API.
- infra/db/Dockerfile: FROM apache/age:latest (PG 18 + AGE 1.7.0) and compiles pgvector v0.8.1. infra/db/init/01-extensions.sql creates both extensions on first volume init.
- docker-compose.yml (root): `db` service builds the image, binds 127.0.0.1:5432, named volume jotmind-db mounted at /var/lib/postgresql (PG18 convention), healthcheck via pg_isready.
- apps/api: added drizzle-orm + postgres + drizzle-kit. src/db/schema.ts (minimal app_meta table = initial schema), src/db/client.ts (lazy postgres-js pool + drizzle), src/db/health.ts (checkDatabaseHealth + isDatabaseHealthy), src/db/migrate.ts (creates extensions then runs migrations; CLI-runnable), drizzle.config.ts, drizzle/0000_*.sql + meta/. Scripts: db:generate, db:migrate.
- /api/health (apps/api/src/app.ts) now reports {database:{configured,connected,extensions:{age,vector},error?}}; status degraded if configured but unreachable/missing ext, ok otherwise. createApp accepts injectable checkDatabase for unit tests.
- packages/schemas/src/health.ts: added databaseExtensionsSchema + databaseHealthSchema; healthStatusSchema.database is OPTIONAL (keeps web mock + old callers valid).
- Tests: app.test.ts unit-covers ok/degraded/not-configured via injected mock; db.integration.test.ts is env-gated (describe.skipIf(!DATABASE_URL)) and verifies migrations create app_meta + extensions report true. Docs: README DB section + browser-storage-non-canonical/backup warning; .env.example.
- Files changed: infra/db/{Dockerfile,init/01-extensions.sql}, docker-compose.yml, .env.example, apps/api/{package.json,drizzle.config.ts,drizzle/**,src/db/**,src/app.ts,src/app.test.ts}, packages/schemas/src/health.ts, README.md, AGENTS.md, pnpm-lock.yaml.
- Verified: `pnpm verify` fully green (integration tests skip w/o DB). Brought up compose DB, confirmed extensions via init script, ran integration tests + live `curl /api/health` against it -> database.connected:true, age:true, vector:true. Cleaned up containers/temp build image.
- **Learnings for future iterations:**
  - See Codebase Patterns + AGENTS.md "Database" section. KEY GOTCHAS: pgvector must be >= v0.8.1 to build on PG 18; PG 18 docker volume MUST mount at /var/lib/postgresql (not /data) or the container exits on start.
  - apache/age:latest is PG 18 + AGE 1.7.0 and auto-creates the `age` extension on the default DB; still create it idempotently for clarity/other DBs.
  - Keep DB access gated on DATABASE_URL and health graceful (configured:false -> ok) so unit tests / no-DB runs stay green. US-005 will add the real canonical graph tables on top of this Drizzle setup; US-034 will make API tests run against a test PG container (then the integration test stops skipping in CI).
  - To run DB-backed tests locally: `docker compose up -d --build db` then `DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/jotmind pnpm --filter @jotmind/api test`.
---

## Sat 13 Jun 2026 20:53 +08 - US-003
Thread: https://ampcode.com/threads/T-019ec100-c051-7796-9f00-09e9846afce2
- Implemented local/server accounts + PostgreSQL-backed sessions with Argon2id, HTTP-only session cookies, CSRF, first-run admin setup, and admin-controlled account creation.
- packages/schemas/src/auth.ts: shared Zod schemas (accountRole admin|member, credentials, createAccount, publicUser, setupStatus, authState). Exported from index.ts.
- apps/api/src/db/schema.ts: added `users` (uuid pk gen_random_uuid, email unique, password_hash, role default member, timestamps) + `sessions` (text token pk, user_id FK cascade, csrf_token, expires_at, created_at). Migration drizzle/0001_colorful_vampiro.sql.
- apps/api/src/auth/password.ts: hashPassword/verifyPassword via @node-rs/argon2 (Argon2id). verifyPassword swallows malformed-hash errors -> false.
- apps/api/src/auth/store.ts: AuthStore interface + dbAuthStore (Drizzle-backed, lazy getDb per call). Injectable for tests.
- apps/api/src/auth/index.ts: createAuthRouter({store}) mounted at /api/auth in createApp. Endpoints GET /setup-status, POST /setup (first-run admin, 409 if any user), POST /login (401 generic, dummy-verify to blunt user enumeration), GET /me, POST /logout, POST /users (admin-only). issueSession sets jm_session (HttpOnly, Secure in prod/COOKIE_SECURE, SameSite=Lax) + jm_csrf (readable). requireAuth/requireAdmin/requireCsrf middleware; req.auth via global Express Request augmentation. CSRF = synchronizer token compared constant-time (timingSafeEqual).
- apps/api/src/app.ts: mount cookieParser() + auth router; createApp now accepts {authStore}.
- apps/web/src/api.ts: typed fetch client (credentials:include, x-csrf-token header on mutations). apps/web/src/App.tsx: phase machine (loading->setup|login->authed); SetupForm/LoginForm/AuthedHome/AccountCreator (admin-only). App.test.tsx rewritten to mock the auth endpoints.
- Files changed: packages/schemas/src/{auth.ts,index.ts}, apps/api/{package.json, src/db/schema.ts, drizzle/0001*+meta, src/app.ts, src/auth/*}, apps/web/src/{api.ts,App.tsx,App.test.tsx}, README.md (Authentication section), AGENTS.md (Authentication section), .env.example (COOKIE_SECURE), pnpm-lock.yaml.
- Verified: `pnpm verify` fully green. Brought up compose DB, ran db:migrate, ran live integration tests (auth store create user/session + Argon2id round-trip). Exercised full flow via curl cookie jar: setup-status true->false, setup 201 (HttpOnly cookie), 2nd setup 409, /me, create-account 403 without CSRF / 201 with, member 403 (not admin), logout 204 + /me 401. Playwright browser test: login form renders, login succeeds, "Signed in as admin@example.com" + admin Create-account panel shown, logout returns to Sign in (screenshot captured). Cleaned up temp spec + DB containers.
- **Learnings for future iterations:**
  - See AGENTS.md "Authentication" section + Codebase Patterns below. KEY: @node-rs/argon2 `Algorithm` is an ambient const enum -> cannot value-import under verbatimModuleSyntax; use `2 as Algorithm` (Argon2id===2).
  - Injectable-store pattern is the way to keep DB-backed routes unit-testable without a live DB: define an interface, ship a DB impl + in-memory fake, inject via createApp options. Mirror this for US-004+ (KB routes) and future stories.
  - createApp injection seams: `{ checkDatabase, authStore }`. Add new seams the same way.
  - dev-browser skill is NOT available in this environment; used Playwright+Chromium (already installed) against the running API for browser verification. Do the same for future UI stories.
  - To run DB-backed auth tests: `docker compose up -d --build db`, `DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/jotmind pnpm --filter @jotmind/api db:migrate`, then `DATABASE_URL=... pnpm --filter @jotmind/api test`.
  - US-004 (next) adds knowledge_base_id scoping + KB roles (owner/admin/editor/viewer) + role middleware + audit events; the system account role (admin/member) here is separate from KB-scoped roles.
---

## Sat 13 Jun 2026 21:05 +08 - US-004
Thread: https://ampcode.com/threads/T-019ec10c-85b6-70d6-bd9f-18b7089f516f
- Implemented Knowledge Base scope + role-based access control + audit events.
- DB (apps/api/src/db/schema.ts): knowledge_bases (uuid pk, name, description, created_by FK users ON DELETE restrict), knowledge_base_members (composite PK (knowledge_base_id, user_id), role text, timestamps), audit_events (uuid pk, nullable knowledge_base_id FK cascade + actor_user_id FK set null, action, target_type/target_id, JSONB metadata default '{}'). Migration drizzle/0002_rare_maddog.sql + meta.
- Shared schemas packages/schemas/src/knowledge-base.ts: KB_ROLES=['viewer','editor','admin','owner'] (least->most), KB_ROLE_RANK + kbRoleSatisfies(role,required), create/list/member/assign/audit Zod schemas. Exported from index.ts. KB roles are SEPARATE from the system account role (admin|member in auth.ts).
- Routes apps/api/src/kb/{store.ts,index.ts}: createKnowledgeBaseRouter({store,authStore}) mounted at /api/knowledge-bases in createApp. GET / (list own KBs), POST / (create -> creator becomes owner, CSRF), GET /:id (viewer+), GET /:id/members + POST /:id/members (admin+, CSRF on POST), GET /:id/audit (admin+). requireKbRole(min) middleware loads caller membership for :id, returns 404 for non-members (hides existence) + 403 for insufficient role.
- Reused/exported shared auth middleware from auth/index.ts: requireAuth(authStore), requireCsrf, asyncHandler (were private; now exported). createApp resolves authStore once and passes to both auth + kb routers; seams now {checkDatabase, authStore, kbStore}.
- dbKnowledgeBaseStore: createKnowledgeBase + assignRole write membership + audit_events in the SAME transaction as the canonical write. Creation records knowledge_base.created + knowledge_base.role_assigned(owner); assignRole upserts (onConflictDoUpdate) + records role_assigned.
- Web: apps/web/src/api.ts listKnowledgeBases/createKnowledgeBase (uses knowledgeBaseListSchema; web has no direct zod dep so array schema lives in @jotmind/schemas). apps/web/src/App.tsx KnowledgeBases section in AuthedHome: lists KBs with role, click-to-select, create form.
- Tests: 9 kb unit (in-memory KB store + in-memory auth store, full HTTP via supertest agent) + 1 kb integration (skipIf no DATABASE_URL) + 3 schema unit + 1 web list test. Added apps/api/vitest.config.ts with fileParallelism:false.
- Files changed: packages/schemas/src/{knowledge-base.ts,knowledge-base.test.ts,index.ts}, apps/api/src/db/schema.ts, apps/api/drizzle/0002*+meta, apps/api/src/auth/index.ts (export middleware), apps/api/src/app.ts, apps/api/src/kb/{store.ts,index.ts,kb.test.ts,kb.integration.test.ts}, apps/api/vitest.config.ts, apps/web/src/{api.ts,App.tsx,App.test.tsx}, AGENTS.md, prd.json.
- Verified: pnpm verify fully green (format:check, typecheck, lint, test, test:api, test:e2e; integration tests skip w/o DB). Against live compose DB: db:migrate applied 0002, kb+auth+db integration tests pass. Playwright+Chromium browser flow: first-run setup -> create "My First KB" -> appears as "My First KB (owner) — selected"; screenshot confirmed. Cleaned up temp spec + stopped DB container.
- **Learnings for future iterations:**
  - See AGENTS.md "Knowledge Bases, roles & audit" section. KEY: KB roles (owner/admin/editor/viewer) are NOT the system account role (admin/member). Use kbRoleSatisfies + KB_ROLE_RANK for role gating.
  - GOTCHA: DB integration test files share one database and TRUNCATE the same tables; vitest runs files in parallel by default -> one file's TRUNCATE wipes another's fixtures mid-test (FK violations). Fixed with apps/api/vitest.config.ts fileParallelism:false. Keep it for future *.integration.test.ts files.
  - GOTCHA: apps/web has no direct `zod` dependency. Don't `import { z } from 'zod'` in web; add array/derived schemas to @jotmind/schemas instead (e.g. knowledgeBaseListSchema).
  - PATTERN: record audit_events in the SAME transaction as the canonical mutation, and reuse exported auth middleware (requireAuth/requireCsrf/asyncHandler) + requireKbRole for KB-scoped routes. US-005+ graph writes should follow this (and add graph_outbox in the same tx for US-006).
  - To run a manual browser check: start DB (docker compose up -d --build db), migrate, start API detached with setsid (DATABASE_URL=... COOKIE_SECURE=false setsid pnpm --filter @jotmind/api dev <... &), then `pnpm --filter @jotmind/web exec playwright test <tmp spec>` (playwright auto-starts the vite web server which proxies /api -> :3001). Background API via plain `nohup ... &` got killed between tool calls; `setsid ... < /dev/null & disown` survived.
  - US-005 (next) adds the canonical graph tables (entities, claims, notes, sources, ...) all carrying knowledge_base_id referencing knowledge_bases.
---

## Sat 13 Jun 2026 21:20 +08 - US-005
Thread: https://ampcode.com/threads/T-019ec117-7377-754f-9fc8-eb19f5e01f67
- Added the canonical relational graph schema tables (source of truth; AGE is a derived projection fed via graph_outbox in US-006).
- apps/api/src/db/schema.ts: new tables schema_definitions, schema_versions, entities, claims, claim_arguments, notes, sources, source_excerpts, rule_definitions, proposals, jobs, graph_outbox (audit_events already existed from US-004). Migration drizzle/0003_massive_kronos.sql (+ meta snapshot).
- Conventions: UUIDv7 PKs via PG18-native uuidv7() default (NOT gen_random_uuid()); knowledge_base_id FK cascade; created_at/updated_at timestamptz; nullable deleted_at soft-delete; created_by FK users set-null; JSONB props/metadata/provenance/spec/payload default '{}'/'[]'. Operational tables (jobs, graph_outbox) have no soft-delete.
- entities/claims reference nullable schema_version_id (validated-against version); type/predicate are conceptual string types for cross-version search (US-028). claim_arguments are role-labeled and entity-ref OR literal (argument_kind + check constraint) -> multi-argument, not binary edges. notes & sources are first-class canonical records, not entity rows.
- DB check constraints (confidence 0..1, valid-time order, span order, source_excerpt single-origin, entity not-self-merge, status enums) + partial unique indexes (deleted_at IS NULL) for recreatable schema defs/rules + helpful indexes (kb/type, kb/predicate, claimable status/run_after/created_at, etc.). entities.merged_into_id is a self-FK (AnyPgColumn cast). graph_outbox + jobs carry projection/retry fields for US-006/US-007.
- packages/schemas/src/graph.ts: custom-property validation HOOK validateCustomProperties(propertySchema, properties) (US-005 AC3), PropertySchema/PropertyField types, BUILTIN_ENTITY_TYPES (Person/Event/Concept/Place/Character), and status enums (proposal/rule/job/outbox/schema-kind). Exported from index.ts. 8 unit tests in graph.test.ts.
- apps/api/src/db/graph-schema.integration.test.ts (skipIf no DATABASE_URL): asserts all 12 graph tables exist; inserts user/kb/note/source/2 entities/multi-arg claim/3 claim_arguments (incl. literal)/outbox event with UUIDv7 ids (verifies time-sortable ordering); asserts confidence check rejects value=2.
- Files changed: apps/api/src/db/schema.ts, apps/api/drizzle/0003_massive_kronos.sql + meta/{0003_snapshot.json,_journal.json}, apps/api/src/db/graph-schema.integration.test.ts, packages/schemas/src/{graph.ts,graph.test.ts,index.ts}, AGENTS.md, prd.json, progress.txt.
- Verified: pnpm verify fully green (format:check, typecheck, lint, test, test:api, test:e2e; integration tests skip w/o DB). Against live compose DB (docker compose up -d --build db): db:migrate applied 0003, full apps/api test run 36/36 pass incl. graph-schema integration, `\dt` confirms all 12 graph tables + audit_events. Cleaned up DB container + image.
- **Learnings for future iterations:**
  - See AGENTS.md "Canonical graph schema (US-005)" section. KEY: use uuidv7() default for canonical graph PKs (PG18 native); older auth/KB tables use gen_random_uuid() — don't mix expectations.
  - GOTCHA: a Zod field with `.optional().default(false)` makes the inferred OUTPUT type REQUIRE that field (default is applied on output), so object literals typed as the inferred type must include it. Use plain `.optional()` (no default) when you want the type to stay optional.
  - GOTCHA: drizzle-generated migration meta JSON + the new migration SQL are NOT prettier-ignored — run `pnpm format` after `db:generate` or format:check fails.
  - Self-referencing FK in drizzle needs `.references((): AnyPgColumn => table.col, {...})` (import type AnyPgColumn from drizzle-orm/pg-core).
  - drizzle check()/uniqueIndex().where()/index() are available in drizzle-orm 0.36; partial unique indexes use `.where(sql\`deleted_at IS NULL\`)`.
  - schema_version_id is nullable on entities/claims so US-008/009 (manual entity/claim CRUD) can run before US-027 (custom schemas). US-006 (next) wires graph_outbox events into the same tx as entity/claim/note/source writes + adds the projector seam.
---

## Sat 13 Jun 2026 21:30 +08 - US-006
Thread: https://ampcode.com/threads/T-019ec123-5e2b-719a-99eb-e5eb2a0cb7d4
- Implemented the graph outbox + projection seam so AGE can stay a derived projection.
- apps/api/src/graph/outbox.ts: enqueueGraphOutbox(tx, events) helper (accepts a tx via DbExecutor type so the graph_outbox insert is in the SAME transaction as the canonical write) + outboxEventType("<target>.<event>").
- apps/api/src/graph/store.ts: GraphWriteStore interface + dbGraphWriteStore. createEntity/createNote/createSource/createClaim each write canonical row(s) + matching graph_outbox event in one transaction; createClaim also writes claim_arguments in-tx. This is the write seam US-008/009/011 will build on.
- apps/api/src/graph/projector.ts: Projector interface {name, stubbed, project(event)}; stubProjector (logs, stubbed:true, NO AGE writes yet); ProjectionStore interface + dbProjectionStore; processOutbox() drains pending events ordered by (created_at, id) with FOR UPDATE SKIP LOCKED and marks each processed/failed (+attempts/last_error); rebuildProjection() repairs by reprojecting all non-deleted canonical records straight from relational tables (bypasses outbox); getProjectionStatus() returns {projector{name,stubbed}, counts{pending,processed,failed}}.
- apps/api/src/graph/index.ts: createGraphRouter mounted at /api/graph in createApp. GET /projection/status (any authed user), POST /projection/process + POST /projection/rebuild (system admin only + requireCsrf).
- Stubbed state visible (AC3): createApp logs the stub notice ONCE per process (module flag in app.ts to avoid flooding test output) and /api/graph/projection/status exposes stubbed:true.
- requireAdmin now EXPORTED from auth/index.ts (was private) for reuse by graph maintenance routes. createApp seams now {checkDatabase, authStore, kbStore, projectionStore, projector}.
- packages/schemas/src/graph.ts: added GRAPH_TARGET_TYPES (entity/claim/note/source), GRAPH_EVENT_TYPES (created/updated/deleted), and graphProjectionStatusSchema.
- Files changed: packages/schemas/src/graph.ts; apps/api/src/graph/{outbox,store,projector,index,graph.test,graph.integration.test}.ts; apps/api/src/auth/index.ts (export requireAdmin); apps/api/src/app.ts (mount /api/graph + once-per-process stub log + seams); AGENTS.md; prd.json; progress.txt.
- Tests: 11 graph unit (in-memory store + recording projector: outbox helper, processOutbox success/fail/no-reprocess, rebuild, status, API auth/non-admin-403/csrf-403/admin-drain) + 5 graph integration (skipIf no DATABASE_URL: same-tx outbox event for entity/note/source/claim, tx ROLLBACK leaves no outbox/claim when a check constraint fails, processOutbox with stub, rebuild count, soft-deleted excluded from rebuild).
- Verified: pnpm verify fully green; brought up compose DB, db:migrate, ran all 52 api tests incl the 5 graph integration tests -> pass. Tore down DB.
- **Learnings for future iterations:**
  - See new AGENTS.md "Graph outbox & projection seam (US-006)" section. KEY: never write a canonical graph row (entity/claim/note/source) without enqueuing the matching graph_outbox event in the SAME transaction — always go through dbGraphWriteStore (or call enqueueGraphOutbox(tx, ...) inside your own tx). Mirror this for future graph writes (updates/deletes use eventType 'updated'/'deleted').
  - The projector is STUBBED (stubProjector, stubbed:true). The real Apache AGE projector replaces it later: implement Projector.project to write AGE, set stubbed:false, and swap the default in createApp. processOutbox/rebuildProjection already handle batching/marking/repair — only project() changes.
  - DbExecutor type in outbox.ts = Database | tx param; lets helpers run under getDb() OR a tx. Reuse for other in-transaction helpers.
  - US-007 (next) adds the PostgreSQL-backed jobs foundation (jobs table already exists from US-005: status/attempts/max_attempts/run_after/failure_reason/owner_user_id). Follow the same injectable-store + claim-with-SKIP-LOCKED pattern used here for processOutbox.
---

## Sat 13 Jun 2026 21:40 +08 - US-007
Thread: https://ampcode.com/threads/T-019ec12c-75da-75f7-8b39-8f2262dd9cb9
- Implemented the durable PostgreSQL-backed jobs foundation (table already existed from US-005).
- apps/api/src/jobs/store.ts: JobStore interface + dbJobStore. claimNext() does FOR UPDATE SKIP LOCKED select inside a tx then sets status=running + attempts++ (atomic, multi-worker safe). enqueue/markSucceeded/markForRetry/markFailed/getById/list(filter)/countByStatus.
- apps/api/src/jobs/worker.ts: computeBackoffMs (capped exponential, base 1s/cap 5min). runNextJob (claim->run handler for job.type; success->markSucceeded; throw-> retry with runAfter=now+backoff until attempts>=maxAttempts then dead-letter via markFailed; missing handler-> immediate dead-letter). processAvailableJobs (drain). runWorkerLoop + startInProcessWorker (two run modes share runNextJob). Clock injectable via now:()=>Date for deterministic backoff tests.
- apps/api/src/jobs/handlers.ts: defaultJobHandlers registry (incl noop). worker-process.ts: separate-process entry (graceful SIGINT/SIGTERM, WORKER_POLL_INTERVAL_MS, closeDb on stop).
- apps/api/src/jobs/index.ts: createJobsRouter at /api/jobs -> GET / (list, ?status/?knowledgeBaseId/?limit), GET /stats, GET /:id (404). All system-admin only, read-only (no CSRF needed). toPublicJob serializer.
- Wiring: app.ts adds jobStore seam + mounts /api/jobs; server.ts starts in-process worker when WORKER_INLINE=true; package.json adds `worker` script. packages/schemas/src/jobs.ts (publicJob/jobListQuery/jobStats, reuses jobStatusSchema from graph.ts) + index export.
- Tests: jobs.test.ts (backoff, runNextJob success/no-handler/retry->deadletter w/ injected clock, processAvailableJobs, admin API 401/403/list+filter/400/stats/get+404) + jobs.integration.test.ts (skipIf no DATABASE_URL, TRUNCATE jobs/knowledge_bases/users).
- Files changed: apps/api/src/jobs/{store,worker,handlers,worker-process,index,jobs.test,jobs.integration.test}.ts, apps/api/src/{app,server}.ts, apps/api/package.json, packages/schemas/src/{jobs.ts,index.ts}, AGENTS.md.
- Verified: pnpm verify fully green; live compose DB (DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/jotmind) db:migrate + all 69 tests pass incl integration; separate-process worker smoke-tested end-to-end (inserted noop job -> `pnpm --filter @jotmind/api worker` claimed+ran it -> status=succeeded with result populated).
- **Learnings for future iterations:**
  - The jobs DB queue pattern (claimNext via FOR UPDATE SKIP LOCKED + tx) is the canonical way to add background work. Register new job types in apps/api/src/jobs/handlers.ts:defaultJobHandlers and enqueue via dbJobStore.enqueue(). US-018 (AI proposals), US-021 (embeddings), imports/exports, and the real AGE projector worker should all plug in here rather than inventing new loops.
  - GOTCHA when testing backoff: claimNext filters runnable jobs by `runAfter <= now`. If you enqueue with default runAfter (real now) but call runNextJob with an injected past `now`, the job is NOT runnable and you get undefined. Either enqueue with runAfter matching your injected base, or advance the injected clock forward across retries (see jobs.test.ts / jobs.integration.test.ts).
  - `claimNext` increments attempts, so inside a handler / in runNextJob, `job.attempts` already counts the current attempt. Dead-letter check is `attempts >= maxAttempts` (maxAttempts default 5 => 5 total attempts).
  - createApp injection seams are now {checkDatabase, authStore, kbStore, projectionStore, projector, jobStore}. Mirror the injectable-store pattern for any new DB-backed domain.
  - This was a backend-only story; US-007 ACs do not require browser verification (no UI). dev-browser still unavailable in this env.
---

## Sat 13 Jun 2026 21:52 +08 - US-008
Thread: https://ampcode.com/threads/T-019ec136-d2d1-7001-9672-c2e53bdf93ee
- Implemented manual create/edit of typed entities (built-in types Person/Event/Concept/Place/Character) with KB role gating, audit + graph outbox events, and a web UI.
- packages/schemas/src/graph.ts: added entitySchema, entityListSchema, createEntitySchema (type+name required, trims), updateEntitySchema (all-optional, .refine requires >=1 field, nullable description). Exported via index.ts (graph.js is re-exported). + graph.test.ts cases.
- apps/api/src/entities/store.ts: EntityStore interface + dbEntityStore. createEntity/updateEntity write the canonical `entities` row AND enqueueGraphOutbox(tx,...) AND an audit_events row in ONE transaction (entity.created/entity.updated). Reads (list/get) filter deleted_at IS NULL and scope by knowledgeBaseId.
- apps/api/src/entities/index.ts: createEntityRouter({store,kbStore,authStore}) as Router({mergeParams:true}); mounted at /api/knowledge-bases/:kbId/entities in app.ts (AFTER the KB router; KB router has no /:id/entities so it falls through). Local requireKbRole(min) uses kbStore.getRole -> 404 for non-members, 403 for too-low role. GET / + GET /:entityId require viewer; POST / + PATCH /:entityId require editor + requireCsrf.
- apps/api/src/app.ts: added entityStore seam + mount.
- apps/web/src/api.ts: listEntities/createEntity/updateEntity (credentials:include, x-csrf-token on mutations). apps/web/src/App.tsx: <Entities> component shown when a KB is selected; kbRoleSatisfies(kb.role,'editor') gates the form vs a read-only notice; type dropdown (BUILTIN_ENTITY_TYPES), name, comma-separated aliases/tags, description, JSON custom-properties textarea, edit-in-place. App.test.tsx: added editor-creates-entity + viewer-read-only tests (stateful fetch mock distinguishing method).
- Files changed: packages/schemas/src/{graph.ts,graph.test.ts}, apps/api/src/{app.ts, entities/{store.ts,index.ts,entities.test.ts,entities.integration.test.ts}}, apps/web/src/{api.ts,App.tsx,App.test.tsx}, AGENTS.md.
- Verified: `pnpm verify:quick` + `pnpm test:api` green (9 entity unit tests, 4 integration tests pass against live compose DB confirming audit+outbox rows). Browser verification via temp Playwright spec against real API+DB: first-run setup -> create KB (owner) -> select KB -> create Person "Ada Lovelace" (aliases/tags/JSON props) -> shows in list -> edit name -> updates. Screenshot confirmed. Temp pw config/spec deleted afterward.
- **Learnings for future iterations:**
  - See AGENTS.md "Entities" section. KEY: canonical graph writes need BOTH outbox AND audit events in the same tx; dbGraphWriteStore (graph/store.ts) only does outbox, so entity/claim stores add the audit insert themselves. Mirror dbEntityStore for US-009 (claims) and US-010 (delete/merge -> set deleted_at + emit deleted outbox + audit).
  - Nested KB-scoped routes: separate Router({mergeParams:true}) mounted at /api/knowledge-bases/:kbId/<thing>, reusing kbStore.getRole for requireKbRole. Mount AFTER the KB router.
  - @testing-library/user-event is NOT installed; use fireEvent from @testing-library/react for web interaction tests. For browser verification, write a temp playwright config with two webServers (API with DATABASE_URL env + reuseExistingServer:true, and web dev server), truncate DB users first so the app shows first-run setup, then delete the temp files.
  - US-009 (next) adds claims (multi-argument, claim_arguments rows, confidence, provenance) building on the same canonical+outbox+audit transaction pattern.
---

## Sat 13 Jun 2026 22:05 +08 - US-009
Thread: https://ampcode.com/threads/T-019ec142-3146-727d-a3fa-008938680ebf
- Implemented manual claim create/edit with multi-argument, provenance-aware claims (predicate, description, confidence, valid-time range, role-labeled arguments).
- packages/schemas/src/graph.ts: added claim shapes — CLAIM_ARGUMENT_KINDS, claimArgumentSchema, claimSchema (includes arguments), claimListSchema, createClaimArgumentSchema (superRefine: entity needs entityId+no value, literal needs value+no entityId; argumentKind defaults 'entity'), createClaimSchema (predicate + >=1 arg, refine validStart<=validEnd, confidence 0..1), updateClaimSchema (all-optional, >=1 field, optional args-replace >=1). 10 new schema unit tests.
- apps/api/src/claims/store.ts: ClaimStore interface + dbClaimStore. createClaim inserts claims row + claim_arguments (sequential position) + graph_outbox(claim.created) + audit_events in ONE tx. updateClaim updates metadata and, when fields.arguments present, REPLACES the full argument set (hard-delete + reinsert) so roles/order are editable; emits claim.updated. Returns ClaimWithArguments. Reads filter deleted_at IS NULL, scope by KB.
- apps/api/src/claims/index.ts: createClaimRouter mounted at /api/knowledge-bases/:kbId/claims (mergeParams, AFTER KB router). Role gating mirrors entity router (reads viewer+, mutations editor+ + requireCsrf, 404 for non-members). toClaim() serializes Date->ISO + nests arguments.
- apps/api/src/app.ts: added claimStore seam + mounted claim router after entity router.
- apps/web/src/api.ts: listClaims/createClaim/updateClaim. apps/web/src/App.tsx: <Claims> component (rendered after <Entities> for selected KB) — fetches claims + entities for entity-arg dropdowns, dynamic add/remove argument rows, literal-vs-entity per-argument, edit-in-place, editor-only form.
- Files changed: packages/schemas/src/{graph.ts,graph.test.ts}, apps/api/src/{app.ts,claims/{store.ts,index.ts,claims.test.ts,claims.integration.test.ts}}, apps/web/src/{api.ts,App.tsx}, AGENTS.md, prd.json, progress.txt.
- Tests: 11 claims router unit (in-memory ClaimStore fake records audit+outbox), 4 claims integration (skipIf no DATABASE_URL: multi-arg create tx, metadata+args replace update, confidence check-constraint rejection, list w/ args), 10 schema unit. Verified: pnpm verify fully green; live compose DB db:migrate + all 98 API tests pass (15 files); Playwright browser flow (setup -> KB -> 2 entities -> multi-arg claim with subject/object entity args + literal topic) + screenshot confirmed.
- **Learnings for future iterations:**
  - See AGENTS.md "Claims (manual create/edit, US-009)" section. Multi-argument claims: a claims row + N claim_arguments rows in the same tx. Each arg is entity (entityId set) OR literal (value JSONB set), kind enforced by claim_arguments_kind_check. Don't reduce to binary edges.
  - Editing argument roles = replace the whole argument set (hard-delete + reinsert with new positions). updateClaim only touches arguments when fields.arguments is present.
  - Zod: use .superRefine on the argument schema for the entity/literal XOR rule; createClaimArgumentSchema.argumentKind has .default('entity'). value columns are z.unknown() (JSONB).
  - Web gotcha: <Claims> only refetches entities when kb.id changes, so entities just created in <Entities> won't show in the claim form's entity dropdown until a page reload (re-select KB after reload). Verified in browser via reload+reselect. If a future story needs live cross-component sync, lift entity state up.
  - US-010 (next) = delete + merge graph records safely (soft-delete deletedAt, entity merge via mergedIntoId + provenance.historical_source_entities). Reuse the outbox+audit-in-one-tx pattern with eventType 'deleted'.
---

## Sat 13 Jun 2026 22:19 +08 - US-010
Thread: https://ampcode.com/threads/T-019ec14e-0078-70ac-b158-92be1097d0d0
- Implemented safe soft-delete + Archive&Pointer merge for graph records (entities, claims). Notes/Sources have no store yet (US-011) — their delete reuses this same soft-delete + outbox + audit pattern when added.
- apps/api/src/entities/store.ts: EntityStore gains deleteEntity (soft-delete, entity.deleted outbox+audit), getEntityImpact (claims referencing an entity, for confirmations), mergeEntities (Archive & Pointer). Merge folds aliases/tags (set-union) + source name as alias, properties target-wins, soft-deletes source with merged_into_id->survivor, retargets claim_arguments source->target, appends provenance.historical_source_entities, emits entity.updated+entity.deleted+claim.updated(per claim)+entity.merged audit — all one tx. Returns discriminated MergeEntitiesResult.
- apps/api/src/entities/index.ts: DELETE /:entityId, POST /:entityId/merge (mergeEntitySchema body {targetId}), GET /:entityId/impact. Reuse local requireKbRole (viewer reads / editor mutations + requireCsrf). same_entity->400, source/target_not_found->404.
- apps/api/src/claims/store.ts + index.ts: deleteClaim (soft-delete claim + its claim_arguments, claim.deleted outbox+audit) and DELETE /:claimId.
- packages/schemas/src/graph.ts: mergeEntitySchema, entityImpactSchema/entityImpactClaimSchema (+ types).
- apps/web/src/api.ts: deleteEntity, mergeEntity, getEntityImpact, deleteClaim. apps/web/src/App.tsx: <Entities> rows get Delete + "Merge into" survivor <select> + Merge (impact-aware window.confirm); <Claims> rows get Delete. mergeTargets state per entity.
- Tests: entities.test.ts + claims.test.ts in-memory fakes extended (deleteEntity/mergeEntities/getEntityImpact/deleteClaim) + router tests (delete/merge/impact role-gating, CSRF, 404/400). entities.integration.test.ts + claims.integration.test.ts: live-DB delete/merge/impact/provenance/retarget/outbox/audit assertions (truncate now includes claims/claim_arguments).
- Verified: pnpm verify:quick green; full `DATABASE_URL=... pnpm --filter @jotmind/api test` 114/114 green (integration no longer skipped); Playwright browser flow green (setup->KB->2 entities->claim->delete claim->merge entities (survivor remains)->delete entity).
- **Learnings for future iterations:**
  - See AGENTS.md "Delete & merge (US-010)". No migration needed: entities.merged_into_id + claims.provenance JSONB already existed from US-005 schema.
  - Soft-delete is the rule for ALL canonical graph records: set deletedAt, emit <target>.deleted outbox + audit in the same tx; reads already filter deletedAt IS NULL. Deleting a parent (claim) should soft-delete its children (claim_arguments) too.
  - Merge is Archive & Pointer (NOT a compound node): soft-delete source + merged_into_id, fold props/aliases/tags into survivor, retarget claim_arguments, append provenance.historical_source_entities. Use a discriminated result type so the router maps reasons to 400/404 cleanly.
  - Delete/merge confirmation impact (AC2) is served by GET /:entityId/impact (viewer) returning affected claims; the web confirm dialog uses it. The in-memory test fake returns empty impact (no claim tracking) — impact correctness is covered by the integration test.
  - dev-browser skill still unavailable: verify UI via a TEMP playwright config (two webServers: API w/ DATABASE_URL + reuseExistingServer:true, web dev) after truncating the DB so first-run setup shows; wait for auth-loading to be hidden before interacting; delete temp files + test-results after.
  - US-011 next: add Notes/Sources stores + routers; give them the same soft-delete (deleteNote/deleteSource) so US-010 AC ("note/source deletes are soft-deletes") is fully satisfied.
---

## Sat 13 Jun 2026 23:00 +08 - US-011
Thread: https://ampcode.com/threads/T-019ec16d-7a6f-724d-8c20-195fb8fc8937
- Implemented manual capture of Notes, Sources, and Source Excerpts/Citations (no AI required).
- Shared schemas (packages/schemas/src/graph.ts): note/source/sourceExcerpt public shapes + create/update; sourceExcerptViewSchema extends the excerpt with claim:{id,predicate}|null; createSourceExcerptSchema superRefine enforces exactly-one-origin (noteId XOR sourceId) and excerpt-or-span.
- API: 3 new modules mirroring entity/claim pattern — apps/api/src/notes/, sources/, source-excerpts/ (store interface + db<Domain>Store + index.ts router + in-memory-fake unit tests). Mounted at /api/knowledge-bases/:kbId/{notes,sources,source-excerpts} (mergeParams, after KB router). createApp seams: noteStore, sourceStore, sourceExcerptStore. Role gating mirrors entities (viewer read / editor mutate+CSRF / 404 for non-members).
- Notes & Sources are canonical graph records: writes emit graph_outbox (note.*/source.*) + audit in one tx. Source Excerpts are NOT a graph target type -> audit ONLY, no outbox. Excerpt store validates origin note/source AND linked claim exist in same KB (discriminated SourceExcerptResult -> 404), and joins claim predicate for views; list filters by ?noteId/?sourceId/?claimId.
- Web: api.ts clients (list/create/update/delete for notes & sources; list/create/delete for excerpts). New <Capture> component in App.tsx (after <Claims>): note + source create forms, per-item citation forms (excerpt text + optional claim dropdown), note/source views display original content + linked claims via excerpts.
- No DB migration — notes/sources/source_excerpts tables already existed from US-005.
- Files: packages/schemas/src/graph.ts; apps/api/src/{notes,sources,source-excerpts}/*; apps/api/src/app.ts; apps/web/src/{api.ts,App.tsx}; AGENTS.md.
- Verified: pnpm verify fully green. With live compose DB: db:migrate + full apps/api test suite = 139 passed (incl. capture.integration.test.ts). Browser: temp Playwright spec (first-run setup -> KB -> claim (literal arg) -> note -> source w/ JSON metadata -> citation linked to claim); note view showed excerpt "first algorithm" -> claim "pioneered". Screenshot confirmed. Temp e2e/config removed.
- **Learnings for future iterations:**
  - See AGENTS.md "Notes, Sources & Citations (capture, US-011)" + Codebase Patterns. KEY: only entity/claim/note/source are GRAPH_TARGET_TYPES — source_excerpts/audit-only (no outbox). Add new graph-projected kinds to GRAPH_TARGET_TYPES in @jotmind/schemas graph.ts before enqueuing outbox for them.
  - The <Capture> citation dropdown (like <Claims> entity dropdown) only refetches claims when kb.id changes; cross-component live sync isn't wired. For browser tests, avoid the entity-dropdown-in-claims refresh gotcha by using a literal claim argument.
  - US-012 (next): manual search & filters over entities/claims/notes/sources.
---

## Sat 13 Jun 2026 23:15 +08 - US-012
Thread: https://ampcode.com/threads/T-019ec17e-8740-750c-b22c-9f004bcf4d71
- Implemented manual token search + structured filters that work without any AI provider (No AI mode stays useful).
- API: new apps/api/src/search/ domain (store.ts SearchStore interface + dbSearchStore; index.ts createSearchRouter) mounted at /api/knowledge-bases/:kbId/search (mergeParams, AFTER KB router). createApp seam `searchStore`. GET / requires viewer; 404 for non-members (hides existence) -> search respects KB permissions (AC3).
- Token search unions four per-kind queries (entities, claims, notes, sources), each filtering deleted_at IS NULL + knowledge_base_id, AND-matching whitespace tokens via ILIKE across the kind's text + JSONB columns (JSONB cast ::text so aliases/tags/properties/provenance/metadata contents match). Covers AC1 (entities, claims, notes, sources, aliases, tags, descriptions).
- Filters (AC2): entity type + tag (entities only; tag via jsonb @> containment), claim predicate + confidenceMin/Max + hasProvenance (claims only), dateFrom/dateTo on createdAt (all kinds), kinds CSV restriction. resolveKinds() intersects so entity-only and claim-only filters can't both produce hits. place/person are entity types -> covered by the type filter.
- Vector search (AC4): getVectorSearchAvailability() checks information_schema.tables for an `embeddings` table; it doesn't exist yet so returns {available:false, reason}. Token search is unaffected. Endpoint returns {results, vectorSearch}.
- Shared schemas: packages/schemas/src/search.ts (searchQuerySchema with coercion + hasProvenance transform + confidence-range refine; searchResult/searchResponse/vectorSearchAvailability). Exported from index.ts.
- Web: apps/web/src/api.ts search() + <Search> component in App.tsx (rendered first when a KB is selected). Filter form (query, kind checkboxes, type, predicate, tag, confidence min/max, date range, hasProvenance) -> URLSearchParams; renders unified result list + vector-unavailable notice.
- Files changed: packages/schemas/src/{search.ts,search.test.ts,index.ts}, apps/api/src/search/{store.ts,index.ts,search.test.ts,search.integration.test.ts}, apps/api/src/app.ts, apps/web/src/{api.ts,App.tsx}, AGENTS.md, prd.json, progress.txt.
- Tests: 7 search API unit (in-memory store: auth/role gating, filter parsing, vector-unavailable) + 6 search integration (skipIf no DATABASE_URL: cross-kind token search, alias/tag match, KB-scoping + soft-delete exclusion, type/tag/predicate/confidence/provenance filters, vector unavailable) + 6 schema unit.
- Verified: pnpm verify fully green; live compose DB integration tests pass (152 passed with DATABASE_URL); Playwright+Chromium browser flow (setup -> create KB -> create entity Ada Lovelace -> search "ada" -> [entity] result + "Semantic (vector) search unavailable" notice) + screenshot confirmed; temp playwright config/spec/screenshot cleaned up.
- **Learnings for future iterations:**
  - Search is read-only + KB-scoped: mirror the entity/claim nested-router pattern (Router mergeParams, mounted AFTER KB router, local requireKbRole('viewer'), 404 for non-members). createApp seam added: searchStore.
  - drizzle: passing table COLUMNS into a helper that builds sql conditions needs the param typed as `SQLWrapper` (or `AnyColumn|SQL`), NOT `SQL` (PgColumn is not an SQL). `sql\`${col}::text ILIKE ${pattern}\`` works for both text and JSONB columns; cast ::text to search JSONB array/object contents. Escape LIKE wildcards (\ % _) in user tokens.
  - db.execute<T>(sql) returns the rows array directly (postgres-js driver); use information_schema.tables / to_regclass to feature-detect tables (used for the AC4 embeddings check).
  - searchQuerySchema reads from req.query (all strings): use z.coerce.number for numerics and a union+transform for the boolean hasProvenance ('true'/'false'->boolean). CSV params (kinds) parsed in the router, not the schema.
  - Future vector/embeddings story: create an `embeddings` table and flip getVectorSearchAvailability to return available:true; then add the semantic-search branch alongside the existing token search (don't replace it).

## Sat 13 Jun 2026 23:25 +08 - US-013
Thread: https://ampcode.com/threads/T-019ec18a-37ee-7313-81d8-dd1bbc641a06
- Implemented multiple graph views over the same Knowledge Base (Network, Timeline, Table, Detail, Sources & Notes) with click-navigation. Frontend-only: all data comes from existing list endpoints (listEntities/listClaims/listNotes/listSources/listSourceExcerpts) — no new API or schema, no migration.
- New file apps/web/src/GraphViews.tsx exporting <GraphViews kb csrfToken>; imported into <KnowledgeBases> in App.tsx and rendered first when a KB is selected (before <Search>). Kept it in its own file because App.tsx is already ~1.7k lines.
- One shared `selected` Selection ({kind,id}) drives all views so clicking anything navigates to the same detail/source context. Tablist switches 5 views:
  - Network: dependency-free SVG circular layout (entities on a circle, claim entity-arguments drawn as predicate-labeled edges). Nodes are clickable -> entity detail.
  - Timeline: chronological cards from claims (validStart??createdAt), notes (createdAt), and Event-typed entities; newest first; cards clickable.
  - Table: entities/claims toggle, sortable column-header buttons, text filter, tag filter (clicking a tag anywhere filters the entity table).
  - Detail: selected entity/claim -> related claims + sources/citations + inline-editable entity metadata (description/tags) via updateEntity; related items + arg entities + tags + citations all navigate.
  - Sources & Notes: original note/source content (blockquote) + linked claims via excerpts; highlights the selected note/source.
- Files changed: apps/web/src/GraphViews.tsx (new), apps/web/src/App.tsx (import + render), apps/web/src/App.test.tsx (new graph-views nav test + `within` import), AGENTS.md (new "Graph views (US-013)" section), prd.json, progress.txt.
- Verified: pnpm verify:quick fully green (8 web tests incl new "renders graph views and navigates from the table to entity detail"; schemas 33; typecheck+lint+format clean). Full API suite 152/152 against live compose DB (DATABASE_URL=...). Browser: temp Playwright+Chromium spec (truncate users -> first-run setup -> create+select KB -> add 2 entities -> reload+reselect -> Network node visible -> Timeline -> Table row click -> entity Detail w/ "Related claims" + "Sources & citations" -> Sources & Notes view) passed; screenshot confirmed. Temp pw.tmp.config.ts/e2e-tmp/test-results removed.
- **Learnings for future iterations:**
  - See AGENTS.md "Graph views (US-013)". KEY: <GraphViews> fetches its data once per kb.id (same cross-component sync gotcha as <Claims>/<Capture>) — records created in sibling sections need a reload+reselect to appear. Browser tests must reload+reselect after creating records.
  - Testid collision: <Capture> and <GraphViews> both use `sources-empty`. Scope queries to getByTestId('graph-views') (or a table row) when a testid isn't unique page-wide. View tabs: view-tab-{network,timeline,table,sources}; nodes/rows: network-node-<id>, table-row-<id>.
  - Network view avoids a heavy graph-layout dependency by using a simple SVG circular layout; fine for small KBs. If a future story needs large/interactive graphs, consider a real layout lib then (US-026 AGE traversal may inform this).
  - Large web components belong in their own files now that App.tsx is ~1.7k lines; import into App.tsx. Don't keep growing App.tsx.
---

## Sat 13 Jun 2026 23:32 +08 - US-014
Thread: https://ampcode.com/threads/T-019ec194-ab8d-77cf-ba1b-84f01e353091
- Enforced/consolidated audit + permissions for manual graph flows. Most of US-014 was already satisfied by US-004-013: every graph mutation (entity/claim/note/source create/update/delete, entity merge, KB role assignment) writes an audit_events row with actorUserId in the SAME tx as the canonical write; viewers are read-only (mutations gated editor+requireCsrf, reads gated viewer); KB audit summary endpoint (GET /:id/audit, admin+) already existed.
- Gap closed (AC4 "admins/owners can inspect ... job status"): added KB-scoped job status endpoint GET /api/knowledge-bases/:id/jobs (admin+). The KB router now takes an injectable jobStore and reuses jobStore.list({knowledgeBaseId}) + toPublicJob from jobs/index.ts. createApp passes options.jobStore into createKnowledgeBaseRouter.
- Web: new apps/web/src/KbAdmin.tsx (own file — App.tsx is huge) rendered in <KnowledgeBases> after <Capture>, gated by kbRoleSatisfies(kb.role,'admin') (returns null for non-admins). Shows audit summary + job status. Added listKbAudit/listKbJobs to apps/web/src/api.ts.
- Files changed: apps/api/src/kb/index.ts (jobStore seam + /:id/jobs route), apps/api/src/app.ts (pass jobStore to KB router), apps/api/src/kb/kb.test.ts (in-memory JobStore fake + 3 tests: admin 200 scoped, viewer 403, non-member 404), apps/web/src/api.ts, apps/web/src/KbAdmin.tsx (new), apps/web/src/App.tsx, AGENTS.md, prd.json, progress.txt.
- Verified: pnpm verify fully green (typecheck/lint/format/test + e2e). KB jobs endpoint unit tests pass (kb.test.ts 12 tests). Ran kb integration test against live compose DB (passed). Browser-verified the admin panel via a TEMP Playwright config/spec (API w/ DATABASE_URL + web dev, reuseExistingServer): first-run setup -> create KB -> select -> assert <kb-admin> shows "knowledge_base.created" audit event + "No background jobs" job section; screenshot confirmed; temp files deleted.
- **Learnings for future iterations:**
  - When reusing the KB router's ownerAgent across createApp instances in tests, the agent is bound to ONE app instance — don't rebuild the app after the agent is created with different store injections, or the agent will hit the old app. Use a mutable array the in-memory store reads lazily (store.list reads jobRows[] at call time) so you can seed data after KB creation while keeping the SAME app/agent.
  - The KB select button testid is `kb-select-<uuid>` (the KB id, NOT the name); in Playwright select by role+name text inside getByTestId('kb-list') instead.
  - System-wide job API (/api/jobs) stays system-admin only; the KB-scoped /:id/jobs is the per-KB view for KB admins/owners. Later stories adding settings should add a viewer-blocked settings mutation + audit row to fully satisfy AC1's "setting changes" (settings don't exist yet — US-016).
---

## Sat 13 Jun 2026 23:42 +08 - US-015
Thread: https://ampcode.com/threads/T-019ec19d-4a8a-752c-92ef-deaece284314
- Built the AI provider adapter layer so all AI integrations sit behind typed adapters (no vendor SDK calls from UI/graph code). Backend foundation only — no DB, no migration, no router, no UI (US-015 ACs have no browser requirement).
- Shared schemas (packages/schemas/src/ai.ts, exported from index.ts): AI_PROVIDER_KINDS (ollama, openai-compatible, openai, anthropic, mock); chatMessage / llmCompletionRequest+Result / tokenUsage / embeddingRequest+Result. Provider config = z.discriminatedUnion('kind', ...) with TWO variants per kind: a *portable* schema (export-safe) and a *full* schema that .extend()s secret fields. AI_PROVIDER_SECRET_FIELDS=['apiKey']. toPortableProviderConfig() deletes secret fields AND re-parses the portable union (parse drops unknown keys, so a leaked secret can't round-trip). providerConfigHasSecrets() helper. openai/anthropic REQUIRE apiKey (config validation complete, AC3); ollama/openai-compatible work with no creds (AC2).
- Adapter layer apps/api/src/ai/: types.ts (AiProvider/LlmProvider/EmbeddingProvider interfaces + AiProviderError + embeddingInputs helper), mock.ts (deterministic MockLlmProvider 'mock-response: <last user msg>' + MockEmbeddingProvider FNV-1a fixed-dim vectors — AC5), http.ts (postJson via global fetch, maps failures to AiProviderError, joinUrl), ollama.ts (OllamaProvider /api/chat + per-input /api/embeddings), openai-compatible.ts (OpenAiCompatibleProvider /chat/completions + /embeddings, optional Bearer; reused for kind 'openai' too), anthropic.ts (AnthropicProvider /v1/messages, LLM-only, splits system prompt), factory.ts (createLlmProvider/createEmbeddingProvider switch on kind, anthropic embedding throws, parseProviderConfig), index.ts (barrel).
- Tests: packages/schemas/src/ai.test.ts (9: config validation, openai/anthropic require apiKey, unknown kind rejected, secret stripping for export, message/embedding shapes) + apps/api/src/ai/ai.test.ts (8: mock determinism, factory builds right adapter per kind, anthropic embedding throws, fetch-mocked ollama /api/chat + openai-compatible Bearer header + non-2xx -> AiProviderError).
- Files: packages/schemas/src/{ai.ts,ai.test.ts,index.ts}; apps/api/src/ai/{types,mock,http,ollama,openai-compatible,anthropic,factory,index}.ts + ai.test.ts; AGENTS.md; prd.json; progress.txt.
- Verified: pnpm verify fully green (format:check, typecheck, lint, test=42 schemas+8 web, test:api=124 passed/39 skipped, e2e). No live-DB run needed (story adds no tables/queries).
- **Learnings for future iterations:**
  - See AGENTS.md "AI provider adapter layer (US-015)". KEY: build providers ONLY via ai/factory.ts (createLlmProvider/createEmbeddingProvider). openai reuses OpenAiCompatibleProvider (identical REST shape). Anthropic has NO embeddings — createEmbeddingProvider throws AiProviderError for it.
  - Secret separation pattern (reuse for any exportable config): discriminatedUnion with portable + full(.extend secrets) schemas; strip via a SECRET_FIELDS list AND re-parse the portable schema so unknown keys are dropped. US-016 (privacy settings) + US-032 (export) should reuse toPortableProviderConfig so API keys never land in KB exports.
  - HTTP adapters use Node 18 global fetch (@types/node ^20 supplies the type; lib is ES2022, no DOM). Unit-test them with vi.spyOn(globalThis,'fetch').mockResolvedValue(new Response(...)) — never hit a live endpoint; normal tests must use the mock providers (AC5).
  - prettier --write needed TWO passes once on a new test file before --check passed (transient); if format:check still flags a just-written file, run --write again then re-check.
  - US-016 next: layered AI privacy settings — likely persists AiProviderConfig (server/KB/per-feature) with a settings table + audit; reuse this adapter layer + toPortableProviderConfig and add a viewer-blocked settings mutation + audit row.
---

## Sun 14 Jun 2026 10:46 +08 - US-016
Thread: https://ampcode.com/threads/T-019ec3ff-2257-73e9-9185-230c18eb7882
- Implemented layered AI privacy settings (server / knowledge_base / user) with strictest-policy-wins resolution. (Most of the backend was already drafted in the worktree from a prior iteration; I fixed its typecheck errors, added the missing web UI, browser-verified, and committed.)
- packages/schemas/src/ai.ts (already drafted): AI_POLICY_MODES (off<local_only<remote_per_request<remote_always) + AI_POLICY_MODE_RANK, aiPolicySchema, DEFAULT_AI_POLICY (No AI), updateAiPolicySchema (rejects empty), resolveAiPolicy() (strictest mode; remoteEmbeddingsAllowed only when remote allowed AND every layer consents = AC4; requiresPerRequestConfirmation AC3), remoteCallConfirmationSchema (AC5), buildRemoteAiAuditMetadata() allowlist builder (AC6 — never leaks prompts/content/keys/responses), REMOTE_AI_AUDIT_FORBIDDEN_KEYS.
- apps/api: db/schema.ts ai_policies table (uuidv7 pk, scope/scopeId, mode, remoteEmbeddings, updatedBy; check constraints + partial unique indexes: one server row, unique scope+scopeId); migration drizzle/0004_glamorous_jocasta.sql. ai-policy/store.ts (AiPolicyStore interface + dbAiPolicyStore; getPolicy returns DEFAULT when no row; updatePolicy UPSERTs + writes audit_events ai_policy.updated in same tx). ai-policy/index.ts (createAiPolicyRouter @ /api/ai: GET server any-authed/PUT admin+CSRF, GET|PUT /policy/me self CSRF, GET /policy effective server+user; createAiPolicyKbRouter @ /api/knowledge-bases/:kbId/ai/policy AFTER KB router: GET viewer/PUT KB-admin+CSRF, 404 for non-members). app.ts seam aiPolicyStore.
- apps/web: NEW AiPolicySettings.tsx — <AiPolicySettings> (server+user, server editable only for system admin; in AuthedHome) + <KbAiPolicy> (KB layer, editable for KB admin/owner; in selected-KB section); both show effective resolved policy. api.ts: getAiPolicyOverview/updateServerAiPolicy/updateMyAiPolicy/getKbAiPolicy/updateKbAiPolicy. Wired into App.tsx.
- FIXES to the pre-existing draft: ai-policy.test.ts imported KnowledgeBaseWithRole from ../kb/index.js (it's only exported from ../kb/store.js) and typed an agent as request.SuperAgentTest (use ReturnType<typeof request.agent>). Both broke `tsc`.
- Tests: 25 schema unit + 11 API router unit (in-memory AuthStore/KbStore/AiPolicyStore fakes) + 3 DB integration (skipIf no DATABASE_URL). pnpm verify fully green; integration tests pass against compose PG; Playwright browser flow verified (setup -> effective No AI default (AC1) -> edit user to remote_per_request -> server stays off so effective stays No AI (AC2) -> loosen server to remote_always -> effective follows stricter user = Remote per request w/ per-request confirmation (AC2/AC3)). Cleaned up temp playwright config/spec/screenshot.
- **Learnings for future iterations:**
  - See AGENTS.md "Layered AI privacy policy (US-016)" section. KEY: pass a MISSING applicable layer as DEFAULT_AI_POLICY (=off), NOT omitted; only omit a layer that doesn't apply (non-KB calls skip the KB layer). resolveAiPolicy([]) -> off.
  - For FUTURE remote-AI features (US-017 proposals, US-020 answers): record audit ONLY via buildRemoteAiAuditMetadata (allowlist) and build the user confirmation from remoteCallConfirmationSchema (provider/model/feature/contentCategories). Never put raw prompts/content/keys/responses in audit.
  - Importing types from a domain dir: re-check what index.ts actually re-exports vs store.ts — KnowledgeBaseWithRole is on ../kb/store.js, not ../kb/index.js.
  - Setup/login forms use <CredentialFields> with NO testids — Playwright must use getByLabel('Email')/getByLabel('Password'). Reused the temp-playwright-config-with-two-webServers pattern (API w/ DATABASE_URL + reuseExistingServer, web dev) after TRUNCATEing users so first-run setup shows.
---

## Sun 14 Jun 2026 11:08 +08 - US-017
Thread: https://ampcode.com/threads/T-019ec412-b62d-70bf-ad4f-6f655a28f7c4
- Quick-capture into the AI proposal review queue. The backend (proposals store+router, capture router, extraction adapter layer), shared schemas, and api.ts client were ALREADY present uncommitted in the worktree from a prior iteration; this iteration completed the story by building the missing web UI, fixing a lint error, formatting, and verifying end-to-end (incl. browser).
- Backend apps/api/src/proposals/: store.ts (ProposalStore + dbProposalStore — reads filter deleted_at IS NULL; createProposal writes the proposal + an audit_events `proposal.created` row in one tx, NO graph_outbox) and index.ts (createProposalRouter at /api/knowledge-bases/:kbId/proposals GET viewer+ with status/sourceNoteId/sourceSourceId filters; createCaptureRouter at /api/knowledge-bases/:kbId/capture POST editor+CSRF; toProposal mapper). Both mergeParams:true, mounted AFTER the KB router.
- Capture stores text as Note/Source FIRST (AC1, never lost), THEN resolves availability + runs extraction. captureResponseSchema returns stored note/source + extraction{status: created|empty|unavailable|error}. No-AI / off-policy / remote-disallowed / extraction-throw all keep the stored record (AC4).
- Extraction behind GraphExtractor interface (apps/api/src/extraction/index.ts, mirrors US-015 adapter layer — no vendor SDK in flow): MockGraphExtractor deterministic demo (proper-noun Concept entities + mentioned_with claim), LlmGraphExtractor wraps LlmProvider, strict-JSON prompt validated by proposalChangesSchema (invalid->[]). resolveExtractorFromEnv reads AI_PROVIDER_CONFIG JSON; null when unset (No-AI). Mock gated by AI_DEMO_EXTRACTION=true or NODE_ENV!=production (AC6). computeExtractionAvailability folds resolveAiPolicy (server+user+KB strictest-wins from US-016).
- Web (THIS iteration): new apps/web/src/Proposals.tsx <Proposals> imported into <KnowledgeBases> after <Capture>: quick-capture form (note/source + title + content), capture outcome (extracted/empty/unavailable/error + Mock-AI demo label), and a read-only pending-proposal list rendering each candidate change. Uses quickCapture/listProposals from api.ts.
- Files changed (this iteration): apps/web/src/Proposals.tsx (new), apps/web/src/App.tsx (import + mount), apps/api/src/extraction/extraction.test.ts (lint fix: drop unused _req param + LlmCompletionRequest import), AGENTS.md (new US-017 section), prd.json, progress.txt. Plus committing the prior-iteration files: packages/schemas/src/proposals.ts(+test), apps/api/src/{extraction,proposals}/, app.ts wiring, api.ts client, schemas index export.
- Verified: pnpm verify fully green (format/typecheck/lint/test/test:api/test:e2e); live compose DB db:migrate + all 206 API tests pass (incl 2 proposals integration); Playwright+Chromium browser flow (setup -> KB -> capture no-AI=unavailable -> set local_only on server/user/KB -> capture 'Ada Lovelace met Charles Babbage in London.' -> 4 demo-labeled proposal changes in the queue) + screenshot confirmed.
- **Learnings for future iterations:**
  - GOTCHA (already-in-progress work): a prior iteration left US-017 backend+schemas UNCOMMITTED in the worktree. Per Ralph rules, do NOT revert work you didn't make — review it, finish the gap (here: the web UI), and verify. Always `git status` first.
  - Proposals are NOT graph projection targets: audit-only, NO graph_outbox (same as source_excerpts). They never mutate canonical records — that's US-018.
  - To browser-verify the AI path you must set ALL THREE policy layers (server+user+KB) to >= local_only (strictest-wins; missing rows = off) AND run the API with AI_PROVIDER_CONFIG='{"kind":"mock","name":"..."}' + AI_DEMO_EXTRACTION=true (NODE_ENV must not be production). The mock extractor needs >=2 proper nouns to emit a linking claim.
  - Effective-policy UI shows the human label ("Local only"), not the enum value ("local_only") — assert on the label in Playwright.
  - US-018 (next) adds proposal review/accept/reject/edit (item + batch), preserving provenance/citation/provider/model/confirmation metadata on accepted claims, rejecting-keeps-linked (AC5 of US-017), and viewer read-only enforcement. The <Proposals> queue + proposalChangesSchema are the foundation to build on.
---

## Sun 14 Jun 2026 11:22 +08 - US-018
Thread: https://ampcode.com/threads/T-019ec41a-9677-76a9-b9ba-085c384aa746
- Implemented review & apply of AI/import graph proposals (accept/reject/edit, item + batch level).
- Backend: apps/api/src/proposals/store.ts gained updateProposalChanges + reviewProposal (both pending-only, write proposal.updated / proposal.{accepted,rejected,dismissed} audit in one tx). apps/api/src/proposals/index.ts: createProposalRouter now injects entityStore/claimStore and exposes PATCH /:id (edit changes, re-validated), POST /:id/accept (applyProposalChanges -> creates entities then claims via the stores; pre-validates entity-arg refs; provenance on accepted claims), POST /:id/reject. apps/api/src/claims/store.ts: CreateClaimInput gained optional `provenance` persisted to claims.provenance JSONB. app.ts wires entityStore/claimStore into the proposal router.
- Schemas: packages/schemas/src/proposals.ts editProposalSchema/acceptProposalSchema/rejectProposalSchema/acceptProposalResultSchema (+ exported).
- Web: apps/web/src/api.ts acceptProposal/rejectProposal/editProposal; apps/web/src/Proposals.tsx <ProposalItem> now interactive (per-item checkboxes, Accept all / Accept selected / Reject / Edit-JSON, source line).
- Tests: +6 schema unit, +12 proposals API unit (23 total in that file), +3 proposals integration (8 total). Full pnpm verify green; 220 API tests pass vs live compose DB. Playwright+Chromium browser flow verified end-to-end (capture -> 4 mock proposals -> Accept all -> queue empties; DB shows 3 entities + 1 claim with ai_proposal provenance + accepted proposal). Screenshot captured, temp pw config/spec removed.
- **Learnings for future iterations:**
  - APPLYING proposals belongs in the ROUTER, not the store: applyProposalChanges() calls entityStore.createEntity/claimStore.createClaim so accepted records reuse the canonical write path (outbox + audit per record). The ProposalStore stays focused on the proposal row only. This keeps each layer testable with in-memory fakes.
  - Claim provenance now has a first-class store field (CreateClaimInput.provenance -> claims.provenance JSONB). Use it (not properties) for origin/source/provider/acceptedBy metadata. claims.provenance already existed from US-005, so NO migration was needed.
  - Strictest-policy-wins bites browser tests: capture extraction availability folds server + user + KB layers, and a fresh KB's missing AI policy resolves to OFF (DEFAULT_AI_POLICY). To get a mock proposal in the browser you must set ALL THREE layers (ai-policy-server, ai-policy-user, kb-ai-policy) to local_only, AND start the API with AI_PROVIDER_CONFIG='{"kind":"mock","name":"..."}' + AI_DEMO_EXTRACTION=true so resolveExtractorFromEnv builds the MockGraphExtractor.
  - Proposal review is pending-only: updateProposalChanges/reviewProposal return undefined for non-pending rows (router maps to 404/409), which is how double-accept/double-review is prevented. Accept re-parses the stored changes with proposalChangesSchema before applying (defense-in-depth for AC4).
  - The CredentialFields setup/login inputs have NO data-testids (only the submit buttons do); use Playwright getByLabel('Email')/getByLabel('Password'). KB rows are kb-select-<id>; AI policy testids are <prefix>-mode/-save where prefix is ai-policy-server / ai-policy-user / kb-ai-policy.
---

## Sun 14 Jun 2026 - US-019
Thread: https://ampcode.com/threads/T-019ec428-73c9-7710-9f5f-06ea0327044d
- Implemented the universal command/search box (one NL box → manual search always + structured AI interpretation when permitted).
- New apps/api/src/command/: interpreter.ts (CommandInterpreter interface; MockCommandInterpreter deterministic demo parsing type:/predicate:/tag:/kind: hints; LlmCommandInterpreter strict-JSON + ONE repair retry inside interpret(); resolveCommandInterpreterFromEnv mirrors resolveExtractorFromEnv) and index.ts (createCommandRouter, POST / read-only viewer, no CSRF, always-fallback search + AI interpretation gated by resolveAiPolicy, low-confidence<0.4 → fallback).
- packages/schemas/src/command.ts (+ index export): commandRequest/commandSearchFilters/commandInterpretation (discriminatedUnion search|create)/commandAiInfo/commandResponse, COMMAND_CONFIDENCE_THRESHOLD.
- app.ts: new commandInterpreter seam + mount; reuses searchStore + aiPolicyStore seams.
- Web: api.ts runCommand() + <CommandBox> in App.tsx (rendered first per KB) — separate "Structured interpretation" vs "Search results" sections (AC6).
- Tests: 8 schema + 13 interpreter + 11 command-API unit (all green). pnpm verify fully green. Browser-verified with Playwright+Chromium against live compose DB w/ mock AI (no-AI fallback → enable local_only on all 3 layers → structured interpretation w/ Mock label, separate from fallback) + screenshot.
- **Learnings for future iterations:**
  - Structured-output AI flows: keep the parse/validate + single repair retry INSIDE the adapter (LlmCommandInterpreter.interpret), return null on failure, let the router fall back. Mirrors the extraction layer; do the same for US-020/US-019-like flows.
  - Mock LLM provider (MockLlmProvider) echoes "mock-response: ..." which is NOT valid JSON → an LLM-backed structured flow with the mock provider always falls back. For deterministic demo *success* output (browser/tests), add a dedicated Mock<Thing> adapter (MockCommandInterpreter, like MockGraphExtractor) gated by AI_DEMO_EXTRACTION/non-prod, NOT the MockLlmProvider.
  - createInterpreterFromConfig/createExtractorFromConfig take an ALREADY-PARSED AiProviderConfig; the mock variant requires `dimensions` (parseProviderConfig adds the default, raw literals in tests must include it).
  - Read-only POST endpoints (interpret/search) use viewer + NO requireCsrf (CSRF only guards mutations). Non-members still 404 to hide existence.
  - Browser verification: a prior iteration may leave a `tsx watch` API running on :3001 with the right env (DATABASE_URL + AI_PROVIDER_CONFIG mock + AI_DEMO_EXTRACTION). tsx watch auto-reloads your edits, so REUSE it (playwright webServer reuseExistingServer for web only) instead of killing a possibly-concurrent process. Check /proc/<pid>/environ to confirm its config.
---

## Sun 14 Jun 2026 - US-020
Thread: https://ampcode.com/threads/T-019ec436-9400-77a3-a2f7-483f264b9ebd
- Implemented provenance-aware AI answers (one box → AI answer that cites graph evidence + manual-search fallback).
- New apps/api/src/answers/: evidence.ts (AnswerEvidenceStore + dbAnswerEvidenceStore COMPOSES searchStore+claimStore+entityStore — token search scoped to claim/note/source, enriches each claim hit with arguments + entity NAMES so a cited claim carries predicate/connected-entities/confidence/provenance, AC3), answerer.ts (AnswerGenerator interface; MockAnswerGenerator deterministic demo labeling statements known/inferred/uncertain/missing; LlmAnswerGenerator strict-JSON + ONE repair retry inside generate(); resolveAnswerGeneratorFromEnv mirrors resolveCommandInterpreterFromEnv), index.ts (createAnswerRouter POST / read-only viewer + no CSRF; ALWAYS runs manual search fallback AC2; AI gated by resolveAiPolicy so viewer generation only when policy permits AC6; CLAMPS statement citation refs to real evidence so the model can't fabricate citations AC1/AC3).
- packages/schemas/src/answers.ts (+ index export): answerRequestSchema, answerCitationSchema (ref-indexed evidence w/ entities/confidence/provenance), answerStatementSchema (factKind), generatedAnswerSchema, answerResponseSchema, ANSWER_FACT_KINDS.
- app.ts: new answerGenerator + answerEvidenceStore seams + mount; reuses searchStore/aiPolicyStore.
- Web: api.ts answerQuestion() + <Answers> in own file apps/web/src/Answers.tsx (rendered after <CommandBox>) — AI status (Mock label / unverified-remote / repaired), labeled statements w/ fact-kind badges + clickable citation refs, citation list w/ inline detail panel (predicate/entities/confidence/provenance), and a separate manual-search fallback section.
- Tests: 8 schema + 16 answerer + 10 answers-API unit + 2 evidence integration. pnpm verify fully green; 272 API tests pass vs live compose DB; curl smoke + Playwright+Chromium browser flow verified (cited known-fact answer w/ Mock label + citation detail) + screenshot.
- **Learnings for future iterations:**
  - The "AI-on-graph" flows now share ONE shape: a deterministic Mock<Thing> + an Llm<Thing> (strict-JSON + single repair retry inside the adapter, return null on failure) behind an interface, resolved from AI_PROVIDER_CONFIG via resolve<Thing>FromEnv (mock gated by AI_DEMO_EXTRACTION/non-prod), mounted as a read-only viewer POST that ALWAYS runs a manual-search fallback and gates AI on resolveAiPolicy (server+user+KB strictest-wins). US-017 extraction, US-019 command, US-020 answers all follow it — copy it for future AI features.
  - ANTI-HALLUCINATION pattern for cited AI output: build the citations/evidence SERVER-SIDE from real records (so predicate/entities/confidence/provenance are accurate), give each a numeric `ref`, let the model only REFERENCE refs, then CLAMP statement citations to the real ref set in the router. Don't let the model emit the citation content itself.
  - Evidence enrichment reuses existing tested stores by COMPOSITION (searchStore→ids, claimStore.getClaim→arguments, entityStore.getEntity→names) instead of new SQL — keeps it unit-testable with in-memory fakes and adds only a tiny integration test for the composition.
  - Web gotcha: don't render the same citation-detail component in two places (inline under the citation AND a separate panel) — duplicate data-testids break getByTestId. One inline toggle is enough for AC4.
  - Browser verify reused the running tsx-watch API on :3001 (mock AI env) + vite proxy (vite.config.ts proxies /api → :3001). Setup was already done on that DB, so login (don't truncate a possibly-shared DB) and set local_only on all 3 AI policy layers before asking. Remember to RESET any policy you flip to `off` during No-AI testing before the AI browser check.
---

## Sun 14 Jun 2026 12:12 +08 - US-021
Thread: https://ampcode.com/threads/T-019ec447-4552-723f-b5b6-fb811d5bc822
- Implemented optional vector embeddings indexed through durable jobs (US-007 reuse).
- DB: new `embeddings` table (drizzle 0005) storing pgvector vectors via a custom Drizzle `vector` type (no fixed dim). Unique idx (target_type,target_id,model), kb idx, check constraints. Not soft-deleted (derived index).
- apps/api/src/embeddings/: store.ts (EmbeddingStore+dbEmbeddingStore: upsert/deleteByTarget/getStats/hasEmbeddings/vectorSearch cosine), content.ts (per-kind content builders; entity content folds aliases+tags AC2; hashContent), targets.ts (EmbeddingTargetSource reads canonical rows WHERE deleted_at IS NULL), provider.ts (resolveEmbeddingProviderFromEnv; anthropic->null), indexer.ts (runEmbeddingIndex: policy-gated, remote needs remoteEmbeddingsAllowed AC3, batch embed + idempotent upsert), index.ts (createEmbeddingsRouter GET status viewer / POST reindex editor+CSRF enqueues 'embeddings.index' job 202).
- jobs/handlers.ts registers a SELF-CONTAINED embeddings.index handler (works in both worker modes). createApp seam embeddingStore. search/store.ts getVectorSearchAvailability now checks embedding ROWS (AC4, decoupled from provider). packages/schemas/src/embeddings.ts shared shapes.
- Files: packages/schemas/src/{embeddings.ts,embeddings.test.ts,index.ts}; apps/api/src/db/schema.ts; apps/api/drizzle/0005_*.sql+meta; apps/api/src/embeddings/* (store/content/targets/provider/indexer/index + 5 test files); apps/api/src/jobs/handlers.ts; apps/api/src/app.ts; apps/api/src/search/store.ts; AGENTS.md; prd.json; progress.txt.
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test/test:api/test:e2e). Live compose DB db:migrate (0005) + all 306 API tests pass incl 4 embeddings integration (pgvector round-trip + nearest-neighbor score~1, upsert-replace, indexer vs real rows, deleteByTarget). 37 new unit/integration embedding tests.
- **Learnings for future iterations:**
  - See the 3 new Codebase Patterns at top (pgvector custom Drizzle type + raw `<=>` search; self-contained job handlers; shared AI_PROVIDER_CONFIG env resolution + remoteEmbeddingsAllowed gating) and the new "Vector embeddings & indexing jobs (US-021)" AGENTS.md section.
  - US-021 AC list has NO "Verify in browser" line (only Tests/Typecheck pass) and the story ships no UI — skipped browser verification deliberately.
  - DEFERRED: query-time vector search (embedding the user's query at search time to do real semantic search) is NOT wired into the search endpoint — it needs a live provider. Only the stored-embedding availability + data persistence (the AC4-testable part) is implemented. A later story (or US-029/US-020 enhancement) can compute the query embedding and call dbEmbeddingStore.vectorSearch to merge semantic hits.
  - When adding a Drizzle migration, run prettier --write on the new drizzle/meta/_journal.json + NNNN_snapshot.json — they otherwise fail format:check.
---

## Sun 14 Jun 2026 - US-022
Thread: https://ampcode.com/threads/T-019ec456-18b1-72be-906f-bd5f96e125e4
- Implemented built-in rule packs installable as Module bundle content inside a Knowledge Base. This story sits BEFORE rule authoring (US-023) and execution (US-024), so rules are stored as canonical text + versioned/auditable definitions only; nothing parses/runs them yet.
- Shared catalog packages/schemas/src/rules.ts (+ index export): BUILTIN_RULE_MODULES (Module -> packs -> rules), findBuiltinRulePack(), ruleDefinitionSchema (public shape w/ moduleId/packId), installRulePackSchema/updateRuleStatusSchema/installRulePackResultSchema. Reuses ruleStatusSchema/RULE_STATUSES from graph.js (US-005) — those already existed, so importing avoids a duplicate-export typecheck error. Two showcase Modules: personal-relationship (event-participation pack: acquainted_via_event + co_member_via_group) and reading-character-map (family-relationship pack: sibling + grandparent). Rule text is canonical Prolog-like (head(?a,?b) <- claim(?c,"pred"), arg(?c,"role",?a) ...).
- API apps/api/src/rules/: store.ts (RuleStore + dbRuleStore + readBuiltinMeta) + index.ts (createRuleRouter + toRule mapper). Mounted /api/knowledge-bases/:kbId/rules (mergeParams, AFTER KB router); createApp seam ruleStore. Routes: GET /packs (catalog) + GET / (installed) viewer; POST /install + PATCH /:ruleId editor+CSRF; non-members 404, viewers 403 on mutations.
- Rules are NOT graph projection targets -> writes record audit_events ONLY (no graph_outbox). installRulePack: status='disabled', version=pack.version, idempotent skip on (kb,name,version) dup -> {installed,skipped} + audit rule.installed; setRuleStatus: enabled/disabled + audit rule.enabled/rule.disabled (AC4). Built-in provenance {moduleId,packId,ruleKey,packVersion} stashed in rule_definitions.compiled JSONB under builtin key (no schema column needed). No DB migration (rule_definitions existed from US-005).
- Web apps/web/src/Rules.tsx <Rules> (rendered after <Proposals> for selected KB) + api.ts listBuiltinRulePacks/listRules/installRulePack/setRuleStatus. Built-in Modules/packs list w/ Install buttons + installed-rules list w/ Enable/Disable toggles; editor-gated, read-only notice for viewers.
- Files: packages/schemas/src/{rules.ts,rules.test.ts,index.ts}; apps/api/src/{app.ts, rules/{store.ts,index.ts,rules.test.ts,rules.integration.test.ts}}; apps/web/src/{api.ts,Rules.tsx,App.tsx}; AGENTS.md; prd.json; progress.txt.
- Tests: 7 schema unit + 9 rules API unit + 3 rules integration. pnpm verify fully green; live compose DB all 318 API tests pass; curl end-to-end (install->enable->audit) + Playwright+Chromium browser flow + screenshot confirmed.
- **Learnings for future iterations:**
  - See AGENTS.md "Built-in rule packs (US-022)". Rules are audit-only (no graph_outbox) — they are NOT in GRAPH_TARGET_TYPES. Stash non-column provenance in the existing JSONB (rule_definitions.compiled) under a namespaced key rather than adding a column.
  - GOTCHA: graph.ts (US-005) already defines RULE_STATUSES/ruleStatusSchema/RuleStatus. A new schema file in @jotmind/schemas must IMPORT them, not re-declare, or `export *` in index.ts throws TS2308 duplicate-export. Grep graph.ts for existing rule/status exports before adding rule-related schemas (US-023/024 will hit this too).
  - GOTCHA: asyncHandler requires a Promise-returning handler (its type rejects void). For a synchronous route (static catalog GET /packs) use a plain (req,res)=>{} — wrapping it in asyncHandler is a typecheck error.
  - Idempotent install relies on the rule_definitions_kb_name_version_uq partial unique index (deleted_at IS NULL) from US-005 — check (kb,name,version) before inserting so re-install skips instead of throwing.
  - US-022 ships rules DISABLED by default so the enable/disable audit path (AC4) is exercised and so US-024 execution only runs explicitly-enabled rules. US-023 (next) = restricted Datalog text authoring + validation (range-restriction safety, reject existentials/skolems); the built-in ruleText strings are the reference syntax to parse. US-024 then runs enabled rules with traces.
  - A prior US-021 commit left AGENTS.md failing format:check (one `*emphasis*` vs `_emphasis_`); fixed via prettier --write while adding the US-022 section. Run `pnpm exec prettier --check AGENTS.md` if format:check fails unexpectedly on files you didn't touch.
  - Browser verify reused the running tsx-watch API on :3001 (DATABASE_URL set, fresh/truncated users so setup-status=true). Rules need no AI, so AI_PROVIDER_CONFIG is irrelevant here. Login submit testid is `login-submit`; CredentialFields email/password have no testid (use getByLabel).
---

## Sun 14 Jun 2026 12:42 +08 - US-023
Thread: https://ampcode.com/threads/T-019ec463-c83e-731f-9731-453c7459e764
- Implemented restricted Datalog-like rule authoring (parser + validator + author/edit/validate API + web UI).
- Shared parser `parseRule(text,{recursionCap?})` in `packages/schemas/src/rules.ts`: hand-written tokenizer + recursive-descent parser -> CompiledRule AST ({head, body[], recursive, recursionCap}); never eval'd (data only, AC7). Built-in low-level predicates entity/3, claim/2, arg/3 (RULE_BUILTIN_PREDICATES, AC2). Validation: arity, range-restriction safety (head vars in a positive body atom, AC3), rejects skolem/function terms + blank nodes + anonymous/existential vars + quantifier keywords (AC4), no redefining built-ins, bounded direct self-recursion only (AC7/AC8), restricted negation. Configurable cap clamped [1..64] default 16 (clampRecursionCap). New schemas: createRule/updateRule/validateRule/ruleValidationResult; ruleDefinitionSchema +valid/+validationErrors/+recursionCap.
- API store `apps/api/src/rules/store.ts`: createRule (status='draft' v1, duplicate->{ok:false}), updateRule (re-validates; enabled->draft when invalid). compiled JSONB gets `authored:{ast,validation,recursionCap}`; readAuthoredCap. Router `index.ts`: POST /validate (viewer, no CSRF), POST / + PUT /:ruleId (editor+CSRF); PATCH status now blocks enabling invalid rules (422). toRule re-parses ruleText for consistent validity.
- Web `apps/web/src/Rules.tsx`: "Author a Custom Rule" form (Validate/Save-as-draft, editor-only) + valid/invalid badge per installed rule; `api.ts` validateRule/createRule/updateRule.
- Files: packages/schemas/src/rules.ts (+ rules.test.ts), apps/api/src/rules/{store.ts,index.ts,rules.test.ts,rules.integration.test.ts}, apps/web/src/{api.ts,Rules.tsx}, AGENTS.md, progress.txt, prd.json.
- Tests: 12 schema parser unit + 12 API router unit + 2 integration. Verified: pnpm verify fully green; live compose DB 24 rules tests (5 integration) pass; Playwright+Chromium browser flow (setup->KB->author invalid->fix valid->save draft->[draft] ✓ valid->Enable->[enabled]) + screenshot confirmed (temp spec removed).
- **Learnings for future iterations:**
  - See the new "Restricted-DSL parsing" Codebase Pattern at the top + the AGENTS.md "Restricted Datalog rule authoring (US-023)" section.
  - The parser is intentionally restricted to built-in predicates + the rule's OWN head for recursion (no cross-rule IDB refs), which keeps single-rule validation self-contained and recursion provably bounded. If US-024 needs multi-rule programs, validation will need the whole rule set (stratification) — extend parseRule callers, not the per-rule parser.
  - US-024 (next) RUNS rules: read the stored `compiled.authored.ast` (a CompiledRule), evaluate against KB data with the recursionCap as a depth/iteration limit, inject `deleted_at IS NULL` + current-time bounds, and produce traced inferred results (NOT stored as claims by default). Built-in rule texts from US-022 parse cleanly with parseRule too (parse them at run time since US-022 install didn't store an ast).
  - dev-browser still unavailable; used Playwright+Chromium against the running tsx-watch API on :3001 (same DB). Truncate `users` (CASCADE) to force first-run setup, then create KB + select it before the Rules section renders.
---

## Sun 14 Jun 2026 13:02 +08 - US-024
Thread: https://ampcode.com/threads/T-019ec471-37db-7525-89b5-799f7c9dc89c
- Implemented rule execution with traces (Rule Runs + Inferred Results).
- Pure evaluator apps/api/src/rules/engine.ts (executeRule over the US-023 AST; semi-naive join, stratified negation, bounded recursion cap, per-tuple trace); fact loading + persistence apps/api/src/rules/run-store.ts (dbRuleRunStore.loadFacts injects deleted_at + current valid-time predicates; saveRun records run + inferred_results + rule.run audit in one tx). Router endpoints POST /:ruleId/run (editor+CSRF, enabled+valid only), GET /runs, GET /runs/:runId in apps/api/src/rules/index.ts. New tables rule_runs + inferred_results (drizzle 0006). Web Run button + Last-Rule-Run panel in apps/web/src/Rules.tsx; api.ts runRule/listRuleRuns/getRuleRun. Shared schemas in packages/schemas/src/rules.ts.
- Files changed: packages/schemas/src/rules.ts; apps/api/src/db/schema.ts; apps/api/drizzle/0006_colossal_groot.sql + meta; apps/api/src/rules/{engine.ts,run-store.ts,index.ts,engine.test.ts,rules-run.test.ts,rules-run.integration.test.ts}; apps/api/src/app.ts; apps/web/src/{api.ts,Rules.tsx}; AGENTS.md; prd.json; progress.txt.
- **Learnings for future iterations:**
  - See the two new Codebase Patterns above (Drizzle OR-parens gotcha; pure-evaluator + injectable-fact-store split). The valid-time filter bug (missing parens) passed unit tests and only failed the DB integration test — write integration tests for any hand-written soft-delete/valid-time SQL.
  - Single-rule direct recursion in this dialect can't bootstrap (no disjunction), so recursive rules derive nothing; the recursionCap is a safety guard, not a feature you can demo. US-025 (accept inferred results as claims) builds directly on the inferred_results table — convert a row into a claim via claimStore.createClaim with provenance {origin:'inferred', ruleId, ruleRunId, sourceClaimIds, acceptedBy} (mirror the US-018 proposal-accept pattern: do it in the ROUTER, editor+CSRF, viewers 403).
  - Inferred results carry trace.{claimIds,entityIds,argumentIds} + arguments[{name,value,entityName}]; the run response is {run, results}. ruleName lives on the run, not the result row, so the toInferredResult mapper takes it as an arg.
---

## Sun 14 Jun 2026 - US-025
Thread: https://ampcode.com/threads/T-019ec484-1120-735a-bb3e-0910f485de34
- Implemented accepting an Inferred Result (US-024) as a stored Claim, preserving provenance.
- New route POST /api/knowledge-bases/:kbId/rules/runs/:runId/results/:resultId/accept (editor+CSRF; viewers 403 AC4; non-members 404) on the existing rule router (apps/api/src/rules/index.ts). Mirrors the US-018 proposal-accept pattern: conversion happens IN THE ROUTER via the injected claimStore.createClaim so the new claim uses the canonical write path (graph_outbox + audit in one tx). RuleRouterOptions gained a claimStore seam (wired in app.ts).
- RuleRunStore (apps/api/src/rules/run-store.ts) gained getResult(kbId,runId,resultId) scoped to both run AND KB; the in-memory fake in rules-run.test.ts implements it too.
- Argument mapping: each resolved head arg of the inferred result -> claim arg, entityName!==null => entity arg (entityId=value) else literal arg; role = head var name. Provenance (AC2/AC3): {origin:'inferred', ruleId, ruleName, ruleRunId, inferredResultId, sourceClaimIds, sourceEntityIds, sourceArgumentIds, acceptedBy, acceptedAt, confirmationNote?} via CreateClaimInput.provenance. INFERRED_CLAIM_ORIGIN constant distinguishes from user-entered (no origin) + AI-extracted ('ai_proposal').
- Shared schemas packages/schemas/src/rules.ts: acceptInferredResultSchema, acceptInferredResultResultSchema, INFERRED_CLAIM_ORIGIN. Web: api.ts acceptInferredResult() + per-result 'Accept as claim' button (editor-only) in Rules.tsx 'Last Rule Run' panel -> '✓ accepted as claim' badge.
- Files: packages/schemas/src/{rules.ts,rules.test.ts}; apps/api/src/rules/{index.ts,run-store.ts,rules-run.test.ts,rules-run.integration.test.ts}; apps/api/src/app.ts; apps/web/src/{api.ts,Rules.tsx}; AGENTS.md; prd.json; progress.txt.
- Tests: 4 schema unit + 6 API router unit + 1 integration. Verified: pnpm verify:quick green; live compose DB all 356 API tests pass (incl 4 rules-run integration); curl end-to-end + DB-confirmed origin=inferred provenance; Playwright+Chromium browser flow + screenshot.
- **Learnings for future iterations:**
  - See AGENTS.md "Accept inferred results as claims (US-025)". KEY: converting derived/queued data into canonical records (proposals US-018, inferred results US-025) belongs in the ROUTER calling claimStore/entityStore (canonical write path), NOT in the domain store; the domain store only owns its own rows. Inject claimStore as a seam.
  - INFERRED_CLAIM_ORIGIN='inferred' joins the provenance-origin vocabulary: user-entered (no origin), 'ai_proposal' (US-018), 'inferred' (US-025). Future claim-creating flows should pick a distinct origin string so claims stay distinguishable.
  - To browser-verify, reuse the running tsx-watch API on :3001 (auto-reloads edits; check /proc/<pid>/environ for DATABASE_URL). dev-browser skill is NOT available in this environment -> use a temp Playwright spec in apps/web/e2e (vite proxies /api -> :3001, webServer reuseExistingServer); delete the temp spec + screenshot after. pnpm is on PATH at /home/chesium/.npm-global/bin (export PATH before running pnpm).
  - Start the DB with `docker compose up -d db` (jotmind-db-1, 127.0.0.1:5432); integration tests run their own migrations in beforeAll, so just set DATABASE_URL.
---

## Sun 14 Jun 2026 14:41 +08 - US-026
Thread: https://ampcode.com/threads/T-019ec491-d211-7405-bc2b-b7ce21c8134f
- Implemented AGE-backed graph projection + traversal: the US-006 stub projector is replaced by a REAL Apache AGE projector, with a projection-status state machine, a durable rebuild job, a traversal service abstraction, and the web "Indexing Graph…" state.
- Backend new files apps/api/src/graph/: age.ts (withAge session helper + AgeSession.cypher + ensure/clear graph), age-projector.ts (ageProjector: project loads canonical row + upserts AGE nodes/edges; prepareRebuild clears), traversal.ts (GraphTraversalService + ageTraversalService + GraphProjectionUnavailableError). Edited: projector.ts (Projector.prepareRebuild?, ProjectionStatusStore + dbProjectionStatusStore, rebuildProjection state machine + getProjectionStatus lifecycle), index.ts (export AGE bits, status uses statusStore, rebuild ENQUEUES a job), jobs/handlers.ts (GRAPH_REBUILD_JOB_TYPE handler), app.ts (default projector=ageProjector unless GRAPH_PROJECTOR=stub; wire projectionStatusStore + jobStore), db/schema.ts (graph_projection_status singleton table), drizzle 0007 migration (+ seeded 'default' row).
- Shared schemas packages/schemas/src/graph.ts: GRAPH_PROJECTION_STATES, graphProjectionStateSchema, extended graphProjectionStatusSchema (state + lifecycle fields), GRAPH_REBUILD_JOB_TYPE, graphRebuildJobPayloadSchema.
- Web: api.ts getGraphProjectionStatus(); GraphViews.tsx loads it best-effort and shows network-indexing 'Indexing Graph…' while rebuilding (other tabs keep relational data) + graph-projection-failed/-lag banners. New App.test.tsx US-026 test.
- Model (AC1/AC2): (:Entity), (:Claim{...literalArgs}), (:Claim)-[:ARGUMENT{role,position}]->(:Entity) — fixed :ARGUMENT label + role property (no dynamic labels). project() reloads canonical (idempotent). Rebuild (AC3/AC4): markRebuilding->prepareRebuild(clear AGE)->reproject->markSynchronized (failed+rethrow on error); runs as a durable job so it never blocks the app (AGE separate from canonical). Traversal (AC6) only via the service; throws when rebuilding/failed so callers fall back (AC5). AC7 lag/failure visible via status banner + job UI.
- Tests: graph.test.ts updated (in-memory ProjectionStatusStore fake; rebuild state-machine/prepareRebuild/failed; rebuild-enqueues-job API test) + 8 new age-projector.integration.test.ts (param-escaping, entity AC1, multi-arg edges AC2, idempotency, rebuild AC3/4, soft-delete removal, traversal AC6, rebuilding-unavailable AC5).
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas123+web10/test:api 297pass+70skip/test:e2e). Live compose DB: db:migrate (0007) + all 367 API tests pass incl 8 AGE integration. Browser (Playwright+Chromium vs running tsx-watch API on :3001): synchronized=network node renders; set graph_projection_status='rebuilding' -> 'Indexing Graph…' panel + network-view gone + Table tab still shows the entity (relational fallback). Live curl: admin POST /projection/process projected the Ada Lovelace entity -> AGE MATCH (e:Entity) returns the node (projector age:jotmind_graph, stubbed:false). Temp specs/screenshots removed.
- **Learnings for future iterations:**
  - See the new top "Apache AGE access (US-026)" Codebase Pattern + AGENTS.md "AGE-backed graph projection & traversal (US-026)". KEY: all AGE work via withAge() (begin-pinned connection, LOAD+SET); AGE cypher 3rd arg MUST be a bare inferred-agtype bind param (postgres-js handles this) — never interpolate user text into the Cypher body. $1::agtype is rejected.
  - Default projector is now ageProjector. Any NEW createApp-based unit test that exercises /api/graph/projection/process|rebuild|status must inject projector + projectionStatusStore (+ jobStore for rebuild) or it will hit the live DB/AGE. graph.test.ts shows the in-memory fakes to copy.
  - graph_projection_status FK->knowledge_bases: TRUNCATE knowledge_bases CASCADE (every graph integration test) wipes the singleton row; get() falls back to a 'synchronized' default, so don't assert the seeded row survives a truncate. AGE graph persists across test runs — clear with ageProjector.prepareRebuild() in before/after.
  - Rebuild is a durable job (GRAPH_REBUILD_JOB_TYPE) — needs a worker to actually run (WORKER_INLINE or the worker-process). The :3001 dev API doesn't run a worker by default, so the rebuild job stays queued there; use POST /projection/process for synchronous projection or set the status row directly for UI checks.
  - US-027 (next): custom entity types + claim predicates (schema definitions UI/API) — schema_definitions/schema_versions tables already exist from US-005; entities/claims have nullable schema_version_id to record validation version; validateCustomProperties hook is in @jotmind/schemas graph.ts.
---

## Sun 14 Jun 2026 15:46 +08 - US-027
Thread: https://ampcode.com/threads/T-019ec4de-f596-700f-a238-b165d257f2df
- Implemented custom schema minimum UI/API: define custom entity types + claim predicates that validate entities/claims before save and stamp a schema version reference.
- Backend: new apps/api/src/schema-defs/{store.ts,index.ts}. SchemaStore + dbSchemaStore (createDefinition = definition + v1 active version + schema.created audit in one tx; (kb,kind,name) unique -> duplicate_name; getActiveVersionByName; listDefinitions; getDefinition). createSchemaRouter mounted /api/knowledge-bases/:kbId/schema (AFTER KB router): GET / + GET /export (portable JSON, AC4) + GET /:defId viewer; POST / editor+CSRF (409 dup); non-members 404; viewers 403. Schemas are audit-only (not graph projection targets). createApp seam schemaStore.
- Validation integration (AC3/AC5): entity + claim ROUTERS gained schemaStore (claim router also entityStore) seams; shared checkEntityAgainstSchema/checkClaimAgainstSchema validate properties / argument-roles+compatible-entity-types and return schemaVersionId to stamp. Runs on create AND update (update loads existing record to fill unchanged half). UpdateEntityFields/UpdateClaimFields gained schemaVersionId; entity/claim store update sets persist it. No custom schema => unconstrained (schemaVersionId null).
- Shared schemas: packages/schemas/src/schema-defs.ts (predicateSpecSchema, schemaArgumentRoleSchema, schemaVersionSchema, schemaDefinitionSchema, createSchemaDefinitionSchema, schemaExportSchema, validateEntityProperties/validatePredicateArguments). Reuses schemaDefinitionKindSchema from graph.ts. Exported from index.ts.
- Web: apps/web/src/SchemaDefinitions.tsx <SchemaDefinitions> (after <Search>) lists entity types/predicates + editor-only create form (propertySchema/spec as JSON); api.ts listSchemaDefinitions/createSchemaDefinition. Wired into App.tsx.
- Files changed: packages/schemas/src/{schema-defs.ts,schema-defs.test.ts,index.ts}; apps/api/src/{app.ts, schema-defs/*, entities/{index.ts,store.ts}, claims/{index.ts,store.ts}, entities/entities.test.ts, claims/claims.test.ts}; apps/web/src/{api.ts,App.tsx,SchemaDefinitions.tsx}; AGENTS.md; prd.json; progress.txt.
- Verified: pnpm verify:quick green (format/typecheck/lint/test=schemas137+web10); test:api 328 pass; test:e2e green; live compose DB db:migrate + all 401 API tests pass (incl 3 schema + entity/claim validation integration); Playwright+Chromium browser flow (first-run setup -> create KB -> Custom Schemas section -> create entity_type Person requiring numeric born -> appears as 'Person Person — v1 · fields: born') + screenshot confirmed. NO DB migration (schema_definitions/schema_versions existed from US-005).
- **Learnings for future iterations:**
  - See the new "Custom schemas (US-027)" AGENTS.md section + the US-027 Codebase Pattern at the top.
  - GOTCHA: createApp injects dbSchemaStore into the entity/claim routers by default, but those routers don't self-default schemaStore. DB-free unit tests that POST entities/claims via createApp MUST inject createMemorySchemaStore() (exported from schema-defs/schema-defs.test.ts) or they'll hit the DB.
  - US-028 (schema version evolution) builds directly on this: schema_versions already has version/isActive + the one-active partial unique index; entities/claims carry schemaVersionId. Compatible vs breaking changes = update active version in place vs insert a new active version (deactivate the old). Cross-version search/reasoning must key off the conceptual string type/predicate name (already how getActiveVersionByName + search work), NOT schemaVersionId.
  - dev-browser skill unavailable -> Playwright per prior convention; reused running tsx-watch API on :3001 (same DB), truncated users for first-run setup.
---

## Sun 14 Jun 2026 16:05 +08 - US-028
Thread: https://ampcode.com/threads/T-019ec51a-58d1-7308-af87-610ac0eb4eff
- Implemented schema version evolution: editing a custom schema is classified compatible (metadata/loosen) vs breaking (tighten); compatible changes update the active version in place, breaking changes create + activate a new version so existing data stays valid under its old referenced version. Added a validation report surfacing old/mismatched records.
- Shared (@jotmind/schemas schema-defs.ts): classifyPropertySchemaChange / classifyPredicateSpecChange / classifySchemaChange (pure, kind-dispatching), updateSchemaDefinitionSchema (>=1 field, description nullable to clear), updateSchemaResultSchema, schemaValidationReportSchema + schemaRecordWarningSchema. Breaking = added/now-required field|role, type change, removed field|role, narrowed entityTypes, disallowed literal; everything else compatible.
- API: apps/api/src/schema-defs/store.ts SchemaStore.updateDefinition (metadata in place; compatible = update active version's propertySchema/spec in place; breaking = deactivate active + insert version max+1 active; audit schema.updated|schema.version_created in one tx). index.ts: PUT /:defId (editor+CSRF) + GET /:defId/validation (viewer, AC6) — buildValidationReport scans existing records of the conceptual name/predicate via injected entityStore/claimStore and validates each vs the ACTIVE version. New entityStore/claimStore seams on the schema router wired in app.ts. loadActiveVersion retyped to DbExecutor so it accepts the tx handle.
- Web: apps/web/src/SchemaDefinitions.tsx new <SchemaDefRow> (per-row Edit form + Check-data validation report; editor-gated edit, viewer can still check data). api.ts updateSchemaDefinition + getSchemaValidation.
- Files: packages/schemas/src/{schema-defs.ts,schema-defs.test.ts}; apps/api/src/{app.ts,schema-defs/{store.ts,index.ts,schema-defs.test.ts,schema-defs.integration.test.ts}}; apps/web/src/{api.ts,SchemaDefinitions.tsx}; AGENTS.md; prd.json; progress.txt.
- Tests: +17 schema unit (classifiers + update schema), +6 API router unit (compatible/breaking/metadata/404/viewer-403/validation-report), +3 integration (compatible-in-place, breaking-new-version+old-stays, not_found). pnpm verify:quick + format:check green; full test:api 422 pass vs live compose DB (incl 6 schema-defs integration); test:e2e green. Browser-verified via temp Playwright spec (setup -> KB -> create Person{born required} -> loosen=Compatible-in-place -> tighten=Breaking version 2 -> Check data report) + screenshot; temp files deleted.
- **Learnings for future iterations:**
  - See AGENTS.md "Schema version evolution (US-028)". KEY: classify compat-vs-breaking with PURE functions in @jotmind/schemas (shared by store + any web preview); the store applies compatible IN PLACE and breaking as a NEW active version (max+1), never rewriting existing records (AC3). loadActiveVersion takes DbExecutor so the same helper works inside the update tx.
  - AC4/AC5 were already satisfied by US-027 design (lookups + search key off the conceptual string type/predicate via getActiveVersionByName; validateCustomProperties treats absent fields as missing, only erroring on required) — verify, don't rebuild. AC7 (AI schema migration) is "if present" = vacuously satisfied; a future one must use the US-018 proposal-queue pattern, never auto-apply.
  - GOTCHA: createMemorySchemaStore() in schema-defs.test.ts must gain updateDefinition when SchemaStore grows. The validation-report endpoint needs entityStore/claimStore — inject tiny fakes ({listEntities,getEntity}/{listClaims}) via createApp in unit tests, or it hits the live DB.
  - Web GOTCHA (Playwright): SchemaDefRow keeps the edit form OPEN after a save (state persists because the row is keyed by def.id and refresh() only swaps the def prop). So a second edit must NOT re-click schema-edit-<id> (that toggles it CLOSED) — refill schema-edit-rules-<id> directly. CredentialFields still have no testids (getByLabel Email/Password); setup button is setup-submit, KB name input is kb-name, select is kb-select-<id>. Truncate users CASCADE to force first-run setup on the shared :3001 dev DB.
---

## Sun 14 Jun 2026 16:18 +08 - US-029
Thread: https://ampcode.com/threads/T-019ec529-a1c1-703d-a11e-769ea5d358c2
- Built the built-in personal-relationship Module: an installable bundle of default custom schemas (US-027) + rule pack refs (US-022).
- Files: packages/schemas/src/modules.ts (BUILTIN_MODULES catalog + findBuiltinModule + install/status schemas) + modules.test.ts + index.ts export; apps/api/src/modules/index.ts (createModuleRouter, no store/migration) + modules.test.ts; apps/api/src/app.ts (wire router); apps/web/src/api.ts (listModules/installModule), apps/web/src/Modules.tsx (<Modules>), apps/web/src/App.tsx (mount after <Search>); apps/web/src/AiPolicySettings.tsx (AC5 prominent remote-RAG confirm); AGENTS.md (new section).
- personal-relationship: Person/Event/Place entity types (AC1); attended/met/introduced/interacted_with/mentioned relationship claims (AC3); knows + lives_in Person->Place for location recall (AC4); event-participation rule pack. Person props: birthday + birthdayPrecision (uncertainty), email/phone/social/homepage, relationshipStrength (AC2) — names/aliases/tags(groups)/notes are first-class entity fields, none required.
- Install is router-orchestrated (reuses schemaStore.createDefinition + ruleStore.installRulePack, canonical write paths + audit), idempotent (dup_name -> skipped; rule packs reuse US-022 installer). GET / catalog+installed status (viewer); POST /install editor+CSRF; viewers 403, non-members 404.
- AC5: remote sharing minimal by default (US-016); enabling remote embeddings now needs a prominent window.confirm.
- Verified: pnpm verify fully green (165 schemas + 10 web + 369 API pass / 76 skip + e2e); Playwright+Chromium against live compose DB + running :3001 API (setup->KB->Modules->Install->✓ installed->reload->schemas present; AC5 confirm dialog declined keeps off) + screenshot; DB confirmed 3 entity_type + 7 claim_predicate defs + 2 disabled rules.
- **Learnings for future iterations:**
  - See AGENTS.md "Built-in domain Modules (US-029)". A Module = bundle of US-027 schema defs + US-022 rule pack refs; install in the ROUTER reusing existing stores (no new store, no migration), idempotent via duplicate_name/already-installed skip.
  - propertySchema only supports string/number/boolean/date — model "uncertainty" as an extra precision string field; entity name/aliases/tags/notes are first-class, not custom props.
  - Cross-component sync gotcha persists: after install, reload+reselect KB to see new schemas in <SchemaDefinitions>.
  - AC5 confirm dialog: browser tests need page.once('dialog') + .click() (not .check()).
  - US-030 next: reading/character-map Module — reading-character-map already has a family-relationship rule pack (US-022); add its default schemas (Character/Person, Event, Place, Concept) + plot/relationship claims to BUILTIN_MODULES (mirror US-029), and the network/timeline views already exist (US-013).
---

## Sun 14 Jun 2026 16:30 +08 - US-030
Thread: https://ampcode.com/threads/T-019ec536-77e1-735a-840d-790a12771c29
- Implemented the reading / character-map built-in Module (US-030). This was
  almost entirely CATALOG content + two small GraphViews enhancements — NO new
  API route, store, schema table, or migration (the install router + Modules UI
  are generic; just append to BUILTIN_MODULES).
- packages/schemas/src/modules.ts: added `reading-character-map` to
  BUILTIN_MODULES. Entity types Character/Event(Plot Event)/Place/Concept (AC1).
  Character propertySchema = firstAppearance (AC2) + attributes
  role/species/gender/affiliation/status (none required; names/aliases/tags are
  first-class). Plot Event = date + numeric `sequence` (AC4) + location/chapter.
  Claim predicates (AC2): parent_of(parent,child) — roles MATCH the
  family-relationship rule pack atoms (AC5) — plus appears_in, first_appears_in,
  knows, married_to, allied_with, enemy_of, located_at. rulePacks references the
  existing reading-character-map/family-relationship pack from US-022 (AC5).
- apps/web/src/GraphViews.tsx: NetworkView gained a tag/work filter
  (`network-tag-filter`) so the relationship graph scopes to one work/tag (AC3);
  TimelineView gained an order toggle (`timeline-order`: date|sequence) and reads
  an optional numeric `sequence` JSONB prop off plot Events (shown as #N) for
  date-or-sequence ordering (AC4).
- packages/schemas/src/modules.test.ts: +5 unit tests (AC1/AC2/AC4/AC5).
- Files changed: packages/schemas/src/modules.ts, modules.test.ts,
  apps/web/src/GraphViews.tsx, AGENTS.md (US-030 section), prd.json, progress.txt.
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas170+web10/
  test:api 369pass+76skip/test:e2e). Browser-verified via Playwright+Chromium
  against the live compose DB + running tsx-watch API on :3001: setup -> KB ->
  Modules shows reading-character-map (Character/Concept) -> Install -> ✓ badge
  -> reload+reselect -> Custom Schemas shows Character + parent_of; created
  tagged Characters + 2 Plot Events with {sequence} -> Network tag-filter=holmes
  shows only the holmes node (AC3) -> Timeline order=sequence shows #1 before #2
  (AC4). Screenshots confirmed, temp spec/screenshots removed.
- **Learnings for future iterations:**
  - Adding a domain Module (US-029/US-030) is catalog-only: append a
    `BuiltinModule` to BUILTIN_MODULES in @jotmind/schemas modules.ts. The
    install router (modules/index.ts) and `<Modules>` UI iterate the catalog, so
    no API/web/migration changes are needed. The existing modules API tests use
    `arrayContaining`/`toContain`, so adding a 2nd module doesn't break them.
  - A Module's `parent_of` predicate roles (parent/child) MUST match the
    referenced rule pack's atom variable names, or inference won't join.
  - All built-in entity types (Person/Event/Concept/Place/Character) are in
    BUILTIN_ENTITY_TYPES (graph.ts) and the entity-create `<select>` reads that
    list — custom-type entity creation works without further wiring.
  - GraphViews testids: `network-tag-filter`/`network-filter-empty`,
    `timeline-order`/`timeline-container`. When asserting a network node in
    Playwright, scope to `getByTestId('network-view')` — entity names also appear
    in merge-target dropdowns + the Entities list (strict-mode multi-match).
  - GOTCHA still applies: GraphViews fetches once per kb.id, so reload+reselect
    after creating entities before checking Network/Timeline.
---

## Sun 14 Jun 2026 - US-031
Thread: https://ampcode.com/threads/T-019ec540-058a-758d-8a0d-4e1e80908f43
- Implemented reviewable data import (text/Markdown + structured CSV) WITHOUT uncontrolled graph mutation.
- An import enqueues a durable IMPORT_JOB_TYPE='import.process' job (AC4); the handler runs the self-contained runImport, which parses the payload into candidate ProposalChange[] and stores ONE pending Proposal of kind 'import' (AC3). The candidates flow through the existing US-018 review/accept path — nothing touches canonical records until accepted.
- Files: packages/schemas/src/imports.ts (+imports.test.ts) + index.ts export; packages/schemas/src/proposals.ts (extended change union w/ create_note/create_source; acceptProposalResultSchema +createdNoteIds/createdSourceIds); apps/api/src/imports/{runner.ts,index.ts,imports.test.ts}; apps/api/src/jobs/handlers.ts (register import handler); apps/api/src/proposals/index.ts (applyProposalChanges handles note/source via injected noteStore/sourceStore); apps/api/src/proposals/proposals.test.ts (+1 accept-import test); apps/api/src/app.ts (mount import router + pass note/source stores to proposal router); apps/web/src/{Imports.tsx (new),App.tsx,api.ts,Proposals.tsx (describeChange handles 4 ops)}; AGENTS.md; prd.json; progress.txt.
- AC mapping: AC1 text/Markdown -> create_note/create_source; AC2 CSV entity (name/type/properties/tags) or claim (role->column) mapping via buildImportChanges; AC3 everything becomes a reviewable 'import' Proposal; AC4 GET /imports lists this KB's import jobs (viewer-visible) + job result/failureReason show parse failures; AC5 POST /imports is editor+CSRF (viewers 403, non-members 404).
- NO new table/migration — reuses jobs (US-007) + proposals (US-017).
- Tests: 17 schema unit + 9 imports API unit (router + runImport) + 1 proposals API unit. Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas187+web10/test:api 456 incl all integration/test:e2e). Browser-verified via Playwright+Chromium against running tsx-watch API on :3001 (setup -> KB -> text import + CSV import Ada/Grace -> imports-list shows 2 queued jobs -> drained queue via temp tsx processAvailableJobs since :3001 has no worker -> reload -> imports 'succeeded' + Proposals queue shows 'New note: My imported note' + 'New Person: Ada Lovelace' + 'New Person: Grace Hopper') + screenshot; temp drain/specs/screenshots removed.
- **Learnings for future iterations:**
  - See the two new Codebase Patterns at top (reviewable-imports-as-jobs->proposals; the queued-change-apply-in-router note now covers US-031) + AGENTS.md "Reviewable data import (US-031)".
  - The proposal change union now has 4 ops (create_entity/create_claim/create_note/create_source). Any new op-switch must be exhaustive — web describeChange + applyProposalChanges already are. US-032 export should serialize all of them.
  - dev :3001 API has NO worker -> durable import/embedding/rebuild jobs stay queued there; for browser verify, drain once with a temp tsx calling processAvailableJobs({store:dbJobStore,handlers:defaultJobHandlers}) (needs DATABASE_URL). A real deploy needs WORKER_INLINE or the worker-process.
  - US-032 (next): full-fidelity portable JSON export/import (owner/admin). The schema export skeleton already exists (US-027 schemaExportSchema + GET /schema/export). Build a KB-wide export bundling entities/claims/arguments/notes/sources/citations/schemas/schema-versions/rules/provenance/audit; exclude secrets (API keys); Markdown=human-readable + CSV=structured-subset labels; import can reuse the US-031 reviewable proposal path.
---

## Sun 14 Jun 2026 17:15 +08 - US-032
Thread: https://ampcode.com/threads/T-019ec551-0b57-70ed-b60b-590d5aeb559e
- Implemented portable per-KB export (JSON/Markdown/CSV) + full-fidelity portable JSON import + backup docs.
- New shared schemas packages/schemas/src/exports.ts (portableKnowledgeBaseExportSchema w/ full schema version history, portableImportResultSchema, secret-field constant) exported from index.ts.
- New apps/api/src/exports/ {service.ts (buildPortableExport + importPortableKnowledgeBase + sanitizeJsonb secret-strip), render.ts (markdown/csv, both labeled non-round-trip), index.ts (createKbExportRouter admin-only + createPortableImportRouter authed+CSRF)}; wired into app.ts.
- Added schema-store read method dbSchemaStore.listDefinitionsWithVersions + updated createMemorySchemaStore fake.
- Web: apps/web/src/KbAdmin.tsx <ExportBackup> (3 download links + import file input), App.tsx passes csrfToken to <KbAdmin>, api.ts export URLs + importPortableKnowledgeBase.
- Docs: docs/backup-restore.md (portable formats + PostgreSQL dump/restore as operator maintenance, AC6); README links it.
- Tests: 6 schema unit (exports.test.ts) + 19 API unit (exports.test.ts in-memory fakes). NO DB migration.
- Files changed: packages/schemas/src/{exports.ts,exports.test.ts,index.ts}, apps/api/src/{exports/*,schema-defs/store.ts,schema-defs/schema-defs.test.ts,app.ts}, apps/web/src/{KbAdmin.tsx,api.ts,App.tsx}, README.md, docs/backup-restore.md, prd.json, progress.txt.
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas193+web10/test:api 405pass+76skip/test:e2e); live compose DB curl e2e on :3001 (export 3 formats + import->new KB w/ remapped entity/claim refs + custom Widget schema v1->v2 version replay round-trip); Playwright+Chromium browser flow (login->KB->Administration->Export & backup section, 3 download hrefs + import input) + screenshot; temp spec/screenshot removed.
- **Learnings for future iterations:**
  - See the Portable export/import bullet in Codebase Patterns above.
  - Import has NO single transaction (each canonical store owns its own tx), so the safety mechanism is PREFLIGHT reference validation that throws before any KB is created. Acceptable for V1; a future version could move to a single import tx or background job for very large KBs.
  - Extending the SchemaStore interface (added listDefinitionsWithVersions) requires updating the createMemorySchemaStore fake in schema-defs.test.ts — it is imported by entities/claims/modules/exports tests, so a missing method breaks all of them at typecheck.
  - Schema version history only grows on BREAKING (tightening) schema changes; loosening updates the active version in place. The schema PUT body is top-level {propertySchema|spec} (NOT wrapped).
  - US-033 (next): harden deployment/ops — Docker Compose (Node API + PG w/ AGE+pgvector), API serving built frontend, configurable API base URL, localhost-by-default binding, configurable CORS, deployment docs.
---

## Sun 14 Jun 2026 17:35 +08 - US-033
Thread: https://ampcode.com/threads/T-019ec56b-2fa5-70a9-a90f-f63a6bb3453c
- Hardened deployment & operations: reference Docker Compose API container, single-origin web serving, deny-by-default CORS, configurable web API base URL, loopback-by-default binding, and deployment docs.
- Files changed: docker-compose.yml (+api service), infra/api/Dockerfile (new), .dockerignore (new), apps/api/package.json (start -> tsx src/server.ts), apps/api/src/server.ts (non-loopback bind warning), apps/api/src/cors.ts (new) + cors.test.ts (new, 10 tests), apps/api/src/app.ts (+allowedOrigins/webDistPath seams, CORS middleware, /api 404 JSON, static+SPA fallback), apps/api/src/app.test.ts (+6 CORS/static/404 tests), apps/web/src/api.ts (API_BASE_URL + apiUrl/apiFetch over all 65 calls + 3 export URLs), .env.example (+WORKER_INLINE/WEB_DIST_PATH/CORS_ALLOWED_ORIGINS/API_BIND/API_PORT/VITE_API_BASE_URL), docs/deployment.md (new), README.md (+Deployment section), AGENTS.md (+Deployment & operations section), prd.json, progress.txt.
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas193+web10/test:api 422pass+76skip/test:e2e). Live single-origin instance (WEB_DIST_PATH + compose DB): /api/health JSON, / -> index.html, SPA fallback 200, /api/nope 404 JSON, asset 200 application/javascript. CORS curl: allowed-origin preflight 204+ACAO+credentials, denied-origin preflight 403 no ACAO, denied-origin GET no ACAO. Playwright+Chromium loaded the single-origin app -> 200 + JotMind Sign in UI, zero page errors + screenshot.
- **Learnings for future iterations:**
  - See the new Codebase Patterns entry + AGENTS.md "Deployment & operations (US-033)". KEY: prod runs via tsx (NOT node dist) because @jotmind/schemas exports TS source; the static SPA fallback MUST sit after an explicit /api 404 handler; CORS never allows '*' (incompatible with cookie credentials).
  - Docker image build itself was NOT run here (no registry/network in this env); the Dockerfile follows the proven tsx runtime path. A real deploy still needs WORKER_INLINE/worker-process to drain durable jobs (US-021/026/031); the compose api service sets WORKER_INLINE=true.
  - VITE_API_BASE_URL is BUILD-time only (Vite inlines import.meta.env at build); a runtime-config pattern would be a separate change if ever needed.
  - US-034 (next, final) is V1 verification/hardening: confirm `pnpm verify` runs all six steps with AI keys blocked + mock providers, e2e coverage of critical flows, document remaining stubs, and record any deferred manual browser checks in fix_plan.md.
---

## Sun 14 Jun 2026 18:00 +08 - US-034
Thread: https://ampcode.com/threads/T-019ec57c-8732-708c-8873-bd245d9c1496
- Completed V1 verification & hardening (final story). All 7 ACs satisfied; no DB migration (verification-only).
- AC1: `pnpm verify` already chained format:check/typecheck/lint/test/test:api/test:e2e — kept as authoritative NO-INFRA gate.
- AC2: `test:api` now runs JOTMIND_VERIFY=true -> new Vitest setupFile apps/api/src/test/verify-env.ts strips real AI key env + rejects non-mock AI_PROVIDER_CONFIG (+ 7 unit tests).
- AC3: new `pnpm verify:local-ai` runs apps/api/src/ai/ollama.local-ai.test.ts (skipIf !OLLAMA_BASE_URL) via separate vitest.local-ai.config.ts; default run EXCLUDES `**/*.local-ai.test.ts`.
- AC4: new apps/web/e2e/v1-flows.real.spec.ts (Playwright real-chromium, E2E_REAL_API=1) covers setup/login, KB select, entity+claim CRUD, search/views, mock AI proposal review, rule run, JSON export/import. New playwright.config webServer array + e2e/global-setup.ts (db:migrate + db:reset:test).
- AC5: `pnpm test:api:db` migrates + runs full API suite vs compose DB (505/505 pass, 0 skipped).
- AC6: fix_plan.md records gates + dev-browser-unavailable deferred manual checks.
- AC7: fix_plan.md documents stubs; added GraphViews `graph-projector-stubbed` banner for runtime visibility of the (non-default) stub projector.
- Files changed: package.json, apps/api/package.json, apps/api/vitest.config.ts, apps/web/package.json, apps/web/playwright.config.ts, apps/web/src/GraphViews.tsx, AGENTS.md, prd.json, progress.txt; NEW: apps/api/src/test/{verify-env.ts,verify-env.test.ts,reset-db.ts}, apps/api/vitest.local-ai.config.ts, apps/api/src/ai/ollama.local-ai.test.ts, apps/web/e2e/{global-setup.ts,v1-flows.real.spec.ts}, fix_plan.md.
- Verified: `pnpm verify` FULLY GREEN (test:api 429pass+76skip, e2e smoke); `pnpm verify:local-ai` skips cleanly w/o Ollama; `pnpm test:api:db` 505/505 vs compose DB; `pnpm test:e2e:real` v1-flows spec PASSES vs compose DB + real API + mock AI.
- **Learnings for future iterations:**
  - See the new "Verification gates (US-034)" Codebase Pattern + AGENTS.md section.
  - Vitest `exclude` blocks a test file EVEN when passed explicitly on the CLI — to make an excluded file runnable on demand, use a separate config (vitest.local-ai.config.ts) with its own `include`, not `vitest run <path>`.
  - Playwright webServer mock-AI env MUST go in the config `env` object, NOT an inline shell `VAR={json}` assignment (JSON `{`,`,`,spaces break shell env parsing / trigger brace expansion).
  - Real-flow e2e: per-KB sibling components fetch once per kb.id with no live-sync, so `page.reload()` + reselect the KB after creating records in one section before another reads them (Claims reads the entity list).
  - ALL stories now have passes:true — V1 complete.
---

## Sun 14 Jun 2026 - US-035
Thread: https://ampcode.com/threads/T-019ec67f-7377-7684-a0ac-882e6983cbb2
- Established the selected-KB invalidation model: a single lightweight domain-keyed pub/sub bus that later stories (US-036..US-045) wire into individual panels.
- Files changed:
  - NEW apps/web/src/invalidation.tsx: InvalidationDomain union + INVALIDATION_DOMAINS list (all 16 AC2 domains), createInvalidationBus() (Map<domain,Set<listener>>; dedupes a multi-domain listener to one fire per invalidate() call), InvalidationProvider (context, one useMemo bus per mount), useInvalidate() (stable publisher), useInvalidationEffect(domains, cb) (subscribe; latest cb via ref, resubscribe only on domains-key change). Default context = NOOP_BUS so panels work in isolation (AC4).
  - NEW apps/web/src/invalidation.test.tsx: 8 unit tests (bus + hooks).
  - apps/web/src/App.tsx: wrap the selected-KB workflow-grid in <InvalidationProvider key={selectedKb.id}> so the bus resets per KB; import added.
  - .prettierignore: added plugin-source (non-source scaffolding).
  - prettier --write on apps/web/src/{app.css,App.tsx,GraphViews.tsx} to fix pre-existing format failures.
- **Learnings for future iterations:**
  - Invalidation bus (US-035): mutating panel calls `useInvalidate()(domains)` after a successful write; reading panel calls `useInvalidationEffect([domains], reloadFn)` to re-run its loader. The bus does NOT cache data — each panel keeps its own fetch/refresh; the bus only signals "this domain changed". Default NOOP_BUS means hooks are safe no-ops without a provider (panels still work in isolation / in unit tests). Provider is keyed on selectedKb.id so listeners reset on KB switch. This is the mechanism US-036..US-045 must use (prefer it over ad hoc parent refs).
  - GOTCHA: HEAD already had format:check failures from the non-Ralph "open-design ui redo" commits (apps/web/src/app.css, App.tsx, GraphViews.tsx, plugin-source/*.json) — `pnpm verify` was red before this story. Fixed via prettier --write on the 3 source files + .prettierignore for plugin-source scaffolding. Watch for this on future stories.
  - GOTCHA: the eslint config has NO `react-hooks/exhaustive-deps` rule registered, so an `// eslint-disable-next-line react-hooks/exhaustive-deps` comment itself ERRORS ("Definition for rule ... was not found"). Don't add that disable; instead use a ref + a stable join-key dep array.
---

## Sun 14 Jun 2026 - US-036
Thread: https://ampcode.com/threads/T-019ec684-4723-71db-a462-9036054eefa4
- Wired the US-035 selected-KB invalidation bus into the <Entities> (publisher) and <Claims> (subscriber) panels so entities created/changed in Entities are immediately available in claim argument controls — no page reload or KB reselect.
- apps/web/src/App.tsx: imported useInvalidate/useInvalidationEffect from invalidation.js. Entities: added `const invalidate = useInvalidate()` and publish after each successful write — createEntity -> invalidate(['entities']) (AC1/AC3); updateEntity/deleteEntity/mergeEntity -> invalidate(['entities','claims']) (AC2: edits change how an entity reads in claim arg controls; deletes/merges retarget claim arguments server-side). Claims: added useInvalidationEffect(['entities','claims'], () => void refresh()); its existing refresh() already reloads BOTH listClaims + listEntities, so one subscription refreshes the claim list AND the entity argument <select> options.
- No API/schema/migration changes — pure web wiring of the existing bus. Both panels already live inside <InvalidationProvider key={selectedKb.id}> (US-035), so the bus is shared per selected KB and resets on KB switch.
- AC4 regression: new App.test.tsx test 'shows a newly created entity in the claim argument dropdown without reload (US-036)' — render <App/>, select a KB, assert claim-arg-entity-0 has no 'Ada Lovelace' option, POST an entity via the Entities form (state-based fetch mock flips entities GET [] -> [created]), then waitFor the option to appear in the dropdown via within(getByTestId('claim-arg-entity-0')).
- Files changed: apps/web/src/App.tsx, apps/web/src/App.test.tsx, prd.json, progress.txt.
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web19/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against the live compose DB + running tsx-watch API on :3001 (truncate users -> first-run setup -> create+select KB -> claim-arg-entity-0 has 0 'Frodo Baggins' options -> create Frodo Baggins entity -> dropdown option count becomes 1, no reload/reselect) + screenshot confirmed; temp spec/screenshot removed.
- **Learnings for future iterations:**
  - See the new "Selected-KB invalidation bus wiring" Codebase Pattern at the top. US-036 is the first panel wiring; US-037..US-045 follow the identical pattern (publisher invalidate([...]) + subscriber useInvalidationEffect([...], refresh)).
  - The Claims panel's refresh() ALREADY fetched both claims + entities (Promise.all), so subscribing it was a one-liner — check whether a target panel's existing loader already covers a domain before adding new fetch logic.
  - Cross-panel regression tests don't need reload/reselect anymore (that was the pre-bus workaround): a state-based fetch mock (GET response flips after the POST) + waitFor on the sibling panel proves the invalidation path. fireEvent (not user-event, which isn't installed) + within(getByTestId(container)) to scope the <select>.
  - dev-browser skill still unavailable -> Playwright per prior convention; reused the running tsx-watch API on :3001 (DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/jotmind), truncate users CASCADE for first-run setup.
---

## Sun 14 Jun 2026 22:40 +08 - US-037
Thread: https://ampcode.com/threads/T-019ec689-7913-7439-95ff-7911a0379499
- Refresh graph views after graph-record mutations by wiring the US-035 invalidation bus.
- GraphViews.tsx (apps/web/src): SUBSCRIBES via useInvalidationEffect(['entities','claims','notes','sources','sourceExcerpts','graphViews'], () => void refresh()). refresh() already reloads all relational lists + best-effort getGraphProjectionStatus, so all 4 views refresh while projection banners/AGE fallback are preserved (AC1-AC4). Its own DetailView entity edit (onChanged) also publishes ['entities','claims','graphViews'].
- App.tsx PUBLISHERS: Entities adds 'graphViews' to its create/update/delete/merge publishes (AC1). Claims gained useInvalidate + publishes ['claims','graphViews'] on create/update/delete (AC2). Capture gained useInvalidate + publishes notes/sources/sourceExcerpts + 'graphViews' on note/source create, citation create, and note/source/excerpt delete (deletes also invalidate 'sourceExcerpts' since citations are removed) (AC3).
- AC5 regression: new App.test.tsx test asserts a new entity created in the Entities panel appears as a GraphViews network-node WITHOUT reload/reselect.
- Files changed: apps/web/src/GraphViews.tsx, apps/web/src/App.tsx, apps/web/src/App.test.tsx.
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web20/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium (real-chromium, reused live compose DB + tsx API :3001): setup -> create+select KB -> GraphViews shows 0 nodes -> create 'Grace Hopper' in Entities -> network-node count becomes 1 with no reload. Temp spec/screenshot removed.
- **Learnings for future iterations:**
  - Pure web wiring of the existing invalidation bus — no API/schema/migration. The pattern for the remaining US-038..US-045 stories is identical: identify the reading panel, subscribe it to the domains it reads; identify mutating panels, add the affected domains to their invalidate() publishes.
  - GraphViews refresh() does BOTH relational reload AND best-effort projection status, so subscribing it doesn't change AGE/banner behavior (AC4 satisfied for free).
  - Capture/Claims had NO publishes before this story (only Entities did, from US-036). When wiring a panel as a publisher, add `const invalidate = useInvalidate()` near the other useState hooks and call invalidate(...) right after each successful await create/update/delete (before/around the existing await refresh()).
  - Browser-verify gotcha: the `network-empty` <p> resolves but is `hidden` in the live layout — assert on `[data-testid^="network-node-"]` count instead. SVG node label getByText('<name>') is brittle (also matches the Entities list) — assert node COUNT. The full-page screenshot may scroll past the network SVG; the Playwright count assertion is the authoritative proof.
  - test:e2e:real global-setup runs db:reset:test which needs JOTMIND_ALLOW_DB_RESET=true (and DATABASE_URL); it truncates everything so first-run setup shows — no manual TRUNCATE needed.
---

## Sun 14 Jun 2026 22:45 +08 - US-038
Thread: https://ampcode.com/threads/T-019ec691-cb47-7363-a381-11c9a234fcac
- Made the Capture panel a SUBSCRIBER of the 'claims' invalidation domain so claims created/updated/deleted in the Claims panel appear in/disappear from the citation claim dropdowns with no page reload or KB reselect.
- Files changed: apps/web/src/App.tsx (added `useInvalidationEffect(['claims'], () => void refresh())` to <Capture>; refresh() already reloads listClaims so the citation-claim-<id> <select> updates), apps/web/src/App.test.tsx (new US-038 regression test).
- The Claims panel already publishes ['claims','graphViews'] on create/update/delete (US-037), so no publisher changes were needed — this was a one-line subscriber wire.
- **Learnings for future iterations:**
  - Pattern is now well-established (see Codebase Patterns "Selected-KB invalidation bus wiring"): a READING panel subscribes via useInvalidationEffect to every domain it reads; a MUTATING panel publishes the affected domains. Capture reads claims (for citation dropdowns) so it must subscribe to 'claims' even though it doesn't mutate claims.
  - GOTCHA for App.test.tsx state-based-mock regression tests: mock record fixtures MUST satisfy the @jotmind/schemas Zod schemas or the api.ts `*ListSchema.parse(...)` throws and the panel silently renders empty (no visible error in the test, just a missing element + a timeout). noteSchema needs `properties:{}`; claimSchema needs `properties:{}` AND each argument needs a valid uuid `id` + numeric `position` + nullable `entityId`. When a citation dropdown won't render, check the note loaded (the citation form only renders per note/source).
  - US-039..US-045 remain: wire the remaining panels (proposal acceptance -> graph panels; import jobs/proposal sync; inferred-result accept; module install -> schema/rule panels; search/command/answer staleness; KB list after portable import; AI policy availability surfaces).
---

## Sun 14 Jun 2026 22:55 +08 - US-039
Thread: https://ampcode.com/threads/T-019ec699-3717-71ad-9d18-a3677e6d83a1
- Wired the US-035 invalidation bus so accepting a proposal refreshes graph panels + derived AI surfaces without reload.
- PUBLISHER: Proposals.tsx <ProposalItem> accept() now invalidates ['proposals','entities','claims','notes','sources','sourceExcerpts','graphViews','searchResults','answers','audit'] after acceptProposal succeeds (AC1).
- SUBSCRIBERS: Entities (App.tsx) now subscribes useInvalidationEffect(['entities']) (was publisher-only) so accepted entities appear (AC2); Capture subscription widened ['claims']->['claims','notes','sources','sourceExcerpts'] so accepted notes/sources appear (AC4). Claims (['entities','claims']) + GraphViews (['entities','claims','notes','sources','sourceExcerpts','graphViews']) already subscribe (AC3/AC4) — no change.
- Files changed: apps/web/src/Proposals.tsx, apps/web/src/App.tsx, apps/web/src/App.test.tsx (+1 regression test). No API/schema/migration changes.
- Verified: pnpm verify FULLY GREEN (web22 incl new US-039 test; test:api 429pass+76skip; e2e smoke). Browser-verified via Playwright+Chromium against live compose DB + tsx API on :3005 (mock AI + WORKER_INLINE; :3001 was occupied by concurrent dev so used my own API + temp vite proxy on :5175): setup -> KB -> all-3 AI policy layers local_only -> quick-capture -> mock proposal -> accept -> graph network node count increased WITHOUT reload, zero page errors + screenshot. Temp spec/screenshot removed; vite.config proxy reverted to :3001.
- **Learnings for future iterations:**
  - The proposal-accept invalidation pattern is the template for US-041 (accept inferred rule result -> ['claims','graphViews','searchResults','answers','audit']) and US-040 (import job completion -> ['jobs','proposals','audit']). PUBLISH the union of domains a server-side accept mutates; the panels already SUBSCRIBE.
  - GOTCHA (browser verify): :3001 is often already taken by a concurrent tsx-watch dev API (do NOT kill it). Run your own API on a free port AND temporarily point the vite proxy at it (SameSite=Lax session cookies require SAME-ORIGIN, so cross-origin VITE_API_BASE_URL+CORS won't keep you logged in) — then revert vite.config. Vite auto-picks the next free port (5175 here) when 5173/5174 are taken; hardcode that BASE in the temp spec.
  - Entities panel is NOW a subscriber of 'entities' (previously publish-only) — future stories adding entity-creating flows just need to PUBLISH 'entities' and Entities/Claims/GraphViews all refresh.
---

## Sun 14 Jun 2026 23:05 +08 - US-040
Thread: https://ampcode.com/threads/T-019ec6a2-286a-7096-af8b-f08b0f612256
- Kept import jobs, the proposal queue, and KB job status in sync via the US-035 invalidation bus (pure web wiring; no API/schema/migration changes).
- Files changed: apps/web/src/Imports.tsx (publisher + completion poll), apps/web/src/Proposals.tsx (subscribe ['proposals']), apps/web/src/KbAdmin.tsx (refactor load() + subscribe ['jobs','audit']), apps/web/src/App.test.tsx (US-040 regression test).
- **Learnings for future iterations:**
  - AC1 enqueue: Imports.submit() invalidate(['jobs','audit']) after createImport. AC2 completion: Imports.refresh() diffs each job status vs a lastStatusRef Map; on non-terminal->terminal (succeeded/failed/cancelled) it invalidate(['proposals','jobs','audit']). Two drivers: the existing 'Refresh status' button AND a new setInterval(refresh,3000) that runs ONLY while a job is non-terminal (hasPendingJobs) and tears down when all terminal — self-limiting, no loop because completion writes terminal into lastStatusRef so the next pass detects nothing.
  - AC3: Proposals (outer) subscribes ['proposals']. AC4: KbAdmin job/audit status was a one-shot inline useEffect (went stale forever) — refactored to useCallback load() + useEffect + useInvalidationEffect(['jobs','audit'], load).
  - GOTCHA: publicJobSchema fields are runAfter/payload/ownerUserId/knowledgeBaseId (NOT runAt); a wrong job test fixture makes publicJobSchema.parse throw and the list silently errors. JobStatus type is exported via @jotmind/schemas graph.js.
  - Browser verify of durable-job stories: the dev :3001 API runs NO worker, so imports never complete there. Ran my own tsx API on :3055 with WORKER_INLINE=true + a temp vite on :5180 pointed at it via a temporary `process.env.VITE_PROXY_TARGET` hook added to apps/web/vite.config.ts (reverted after). Same-origin (vite proxy) keeps the SameSite=Lax cookie working. CSV import -> inline worker -> proposal + kb-jobs-list updated without reload.
---

## Sun 14 Jun 2026 - US-041
Thread: https://ampcode.com/threads/T-019ec6a9-d608-7438-b4c6-e40894992969
- Wired the US-035 invalidation bus so accepting an inferred rule result (US-024/025) refreshes the Claims + GraphViews sibling panels (and derived AI surfaces) without a page reload or KB reselect.
- Files changed:
  - apps/web/src/Rules.tsx: added `import { useInvalidate }`, `const invalidate = useInvalidate()`, and (in the existing `accept()` handler, right after `setAcceptedResults`) `invalidate(['claims','graphViews','searchResults','answers','audit'])`.
  - apps/web/src/App.test.tsx: new regression test 'invalidates claims and graph views after accepting an inferred result (US-041)' (renders <Rules> + a Probe subscriber inside a real <InvalidationProvider>; mocks rules packs/list/run/accept; Run -> Accept -> asserts both ['claims'] and ['graphViews'] subscribers fired AND `rules-run-accepted-<id>` shows).
- AC mapping: AC1 = the publish (union of domains a server-side accept mutates, since accept creates a canonical claim via claimStore.createClaim -> AGE projection + audit + changed search/answer freshness). AC2 = Claims already subscribes ['entities','claims'] (US-036). AC3 = GraphViews already subscribes ['entities','claims','notes','sources','sourceExcerpts','graphViews'] (US-037) and preserves projection banners/AGE fallback. AC4 = the existing `setAcceptedResults` marker is unchanged. Pure web wiring — NO API/schema/migration changes.
- Verified: `pnpm verify` FULLY GREEN (format/typecheck/lint/test web24/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against an ISOLATED throwaway DB (`jotmind_us041` in the compose container) + own tsx API :3091 (WORKER_INLINE) + own vite :5191 (temp `VITE_PROXY_TARGET` hook, reverted) so the concurrent :3001/:5173 dev stack was untouched: setup -> create+select KB -> create Ada+Babbage -> 'knows' claim (subject=Ada,object=Babbage) -> claims count 1 -> author+enable+run the default knows-symmetry rule -> inferred result -> Accept as claim -> claims count 2 WITHOUT reload + `rules-run-accepted` shown + zero page errors. Temp spec/DB/processes cleaned up; vite.config reverted.
- **Learnings for future iterations:**
  - The invalidation-bus wiring pattern is already in the Codebase Patterns "Selected-KB invalidation bus wiring" note; US-041 is the same recipe (publisher adds `useInvalidate()` + `invalidate([...])` after a successful write; subscribers were already present from US-036/037). US-042..US-045 remain (Modules->schemas/rules; Search/Command/Answers staleness; KB-list after portable import; AI-policy availability surfaces).
  - GOTCHA (browser verify): the authored rule saves as a `draft` — you MUST click the Enable toggle (`rules-toggle-<id>`) before the Run button (`rules-run-<id>`) is enabled (it's disabled unless status===enabled AND valid).
  - GOTCHA (browser verify): the claim-arg entity `<select>` options render as `"Name (Type)"`, not just the name — select by reading the option value (e.g. `option` with hasText 'Ada') rather than `selectOption({label:'Ada'})` (exact-label match fails). Also wait for `entities-list > li` count to settle between successive entity creates (the second create can race the first while the form is busy).
  - GOTCHA (cleanup): used an isolated `jotmind_us041` database in the existing compose container so the concurrent dev session wasn't disrupted; drop it with `DROP DATABASE ... WITH (FORCE)` (force-closes the API's connection) and kill the API pid by its `:3091` listener (broad `pkill -f "tsx src/server.ts"` would also hit the dev API — don't).
---

## Sun 14 Jun 2026 23:30 +08 - US-042
Thread: https://ampcode.com/threads/T-019ec6b4-2a24-72be-8a10-0b2047ff571f
- Wired the US-035 invalidation bus so installing a Module immediately refreshes the sibling Schema + Rule panels (and the Modules panel's own installed status) without a page reload or KB reselect.
- Files changed: apps/web/src/Modules.tsx (PUBLISHER: install() -> invalidate(['modules','schemas','rules','audit']); also SUBSCRIBER ['modules'] for its own status), apps/web/src/SchemaDefinitions.tsx (SUBSCRIBER ['schemas']), apps/web/src/Rules.tsx (SUBSCRIBER ['rules']; useInvalidationEffect import was already present from US-041), apps/web/src/App.test.tsx (new US-042 regression test).
- **Learnings for future iterations:**
  - Pure web wiring of the existing bus — NO API/schema/migration changes. A Module install creates schema defs (schemaStore.createDefinition) + installs rule packs (ruleStore.installRulePack) server-side, so publish ['modules','schemas','rules','audit']; SchemaDefinitions subscribes ['schemas'] and Rules subscribes ['rules'] (Rules.refresh() reloads BOTH listBuiltinRulePacks + listRules so pack install-status AND installed-rule list update). Modules subscribes its own ['modules'] so idempotent reinstall status stays accurate (AC4).
  - Rules.tsx already imported useInvalidationEffect (US-041 left it imported-but-unused) — just used it.
  - Regression-test pattern (same as US-041): render the publisher component + a Probe with useInvalidationEffect subscribers inside a real <InvalidationProvider>, state-based fetch mock (GET flips after the mutating POST), click the action, assert the vi.fn() subscribers fired + the publisher's own UI updated. ModuleStatus fixture = builtinModuleSchema fields + installedEntityTypes/installedClaimPredicates/fullyInstalled; install response = {moduleId, items:[]}.
  - BROWSER-VERIFY GOTCHAS (cost me time): (1) background API/vite launched with plain `nohup ... &` get REAPED between separate Bash tool calls — start them with `setsid pnpm exec tsx src/server.ts` / `setsid pnpm exec vite` (redirect stdin from /dev/null) so they outlive the tool session. (2) NEVER `pkill -f "tsx src/server.ts"` — it ALSO matches the concurrent dev :3001 API; kill the throwaway stack by PORT->pid (ss -ltnp | grep :3092). The dev :3001/:5173 stack survived. Isolated DB jotmind_us042 (migrate, then drop after).
---

## Sun 14 Jun 2026 23:40 +08 - US-043
Thread: https://ampcode.com/threads/T-019ec6bd-6295-72ed-968f-cf873a5b3a36
- Marked Search, Command, and Answer results STALE after graph changes via the US-035 invalidation bus (mark-stale, not auto-rerun).
- Files changed: apps/web/src/invalidation.tsx (exported RESULT_STALE_DOMAINS + RESULT_STALE_MESSAGE), apps/web/src/App.tsx (Search + CommandBox subscribe + stale banner; import switched to the two constants), apps/web/src/Answers.tsx (subscribe + stale banner), apps/web/src/App.test.tsx (US-043 regression test), prd.json, progress.txt.
- Each of Search/CommandBox/Answers: added `stale` state, useInvalidationEffect(RESULT_STALE_DOMAINS, () => setStale(true)), setStale(false) on successful (re)run, and a `{search|command|answers}-stale` banner shown only when `response && stale`. AC1 covered because RESULT_STALE_DOMAINS includes entities/claims/notes/sources/sourceExcerpts (graph mutations) + searchResults/commandResults/answers (already published by proposal-accept US-039 + inferred-claim accept US-041). AC4 message is the exact PRD example.
- **Learnings for future iterations:**
  - Put SHARED cross-file constants (RESULT_STALE_DOMAINS/MESSAGE) in invalidation.tsx, NOT App.tsx — Answers.tsx is its own file and importing from App.tsx would create a circular import.
  - Mark-stale was the right design over auto-rerun: AC3's "no network loop + keep drafts intact" is then vacuously satisfied (the query input is never cleared/refetched).
  - Browser-verify GOTCHA (new): the redesigned workflow-grid overlaps columns, so Playwright reports off-grid nodes as "hidden" and OTHER sections intercept pointer events on submit buttons. Submit search/entity forms by pressing Enter in the input (not clicking the submit button), and assert toHaveCount(1) (DOM attachment) instead of toBeVisible(). The banner still renders visibly in the actual UI (confirmed via screenshot).
  - US-044 (next): refresh the KB list (PARENT, not a sibling per-KB panel) after a portable JSON import in KbAdmin — that needs the parent App's KB-list reload, not just the selected-KB bus.
---

## Sun 14 Jun 2026 23:43 +08 - US-044
Thread: https://ampcode.com/threads/T-019ec6c7-3cb7-710c-ba88-6f2ffa1a6c2c
- Refresh the Knowledge Base list/selector after a portable JSON import so the imported KB becomes selectable immediately without a page reload. Pure web wiring (apps/web/src) — NO API/schema/migration changes.
- Files changed: apps/web/src/App.tsx, apps/web/src/KbAdmin.tsx, apps/web/src/App.test.tsx.
- WHY NOT THE INVALIDATION BUS: the imported KB lands in the `KnowledgeBases` PARENT component's list, which is rendered OUTSIDE the per-selected-KB `<InvalidationProvider>` (the bus is scoped/reset per selected KB via `key={selectedKb.id}` and has no `knowledgeBases` domain). So a bus invalidate() published from inside KbAdmin can't reach the parent list. Instead I threaded a documented localized parent-ref escape hatch (US-035 AC3 explicitly sanctions this with a code comment): `KnowledgeBases` passes its existing `refresh` down as `onKbListChanged={() => void refresh()}` -> `<KbAdmin>` -> `<ExportBackup>`; `ExportBackup.onImportFile` calls `onKbListChanged?.()` right after a successful `importPortableKnowledgeBase` (AC1/AC2).
- AC3 (messaging) was ALREADY satisfied before this story: ExportBackup's `kb-import-result` line already renders `result.knowledgeBaseName` (the portableImportResultSchema carries both `knowledgeBaseId` + `knowledgeBaseName`).
- AC4 regression: new App.test.tsx test 'shows an imported Knowledge Base in the KB list without reload (US-044)' — renders <App/>, KB list GET returns [source] (owner role so the admin-gated KbAdmin import controls render); selects the source KB; uploads a minimal-but-valid portable export File to `kb-import-file`; the import POST (/api/knowledge-bases/import-portable, matched BEFORE the generic /api/knowledge-bases list matcher since it has no `${kbId}/`) flips a state flag so the next KB-list GET returns [source, restored]; asserts `kb-import-result` shows AND `kb-select-<importedKbId>` appears WITHOUT reload.
- GOTCHA: jsdom's File does NOT implement `.text()` (the component reads `await file.text()`), so the test polyfills it via `Object.defineProperty(file,'text',{value:()=>Promise.resolve(fileText)})`. Confirmed by a throwaway test that `new File([...]).text()` throws 'f.text is not a function' under jsdom.
- Verified: `pnpm verify` FULLY GREEN (format/typecheck/lint/test web27/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against an ISOLATED throwaway DB (jotmind_us044 in the compose container) + own tsx API on :3094 + own vite on :5194 (temp VITE_DEV_PORT/VITE_PROXY_TARGET hooks in vite.config, reverted after) so the concurrent :3001/:5173 dev stack was untouched: first-run setup -> create+select 'Backup Source KB' -> click `kb-export-json` (captured the real portable JSON download) -> upload that file via `kb-import-file` -> `kb-import-result` showed "Imported into new Knowledge Base 'Backup Source KB'…" AND the KB list grew from 1 to 2 entries WITHOUT reload + zero page errors (screenshot confirmed). dev-browser skill unavailable -> Playwright per prior convention; temp spec/screenshot/DB/processes cleaned up + vite.config reverted.
- **Learnings for future iterations:**
  - The selected-KB invalidation bus only covers panels INSIDE `<InvalidationProvider>` (the selected-KB workflow grid). Anything that mutates state owned by the `KnowledgeBases` parent (the KB list/selector itself) must use a parent-ref callback prop, NOT the bus — there is no `knowledgeBases` invalidation domain by design.
  - jsdom File lacks `.text()`/`.arrayBuffer()`; polyfill `.text()` on the File object in any web test that uploads a file the component reads as text.
  - portableImportResultSchema already carries `knowledgeBaseId` + `knowledgeBaseName`, so any "identify the imported KB" UI can use them directly.
---

## Sun 14 Jun 2026 23:58 +08 - US-045
Thread: https://ampcode.com/threads/T-019ec6cd-cf68-702c-8283-51cac0abfa60
- Wired the US-035 invalidation `aiPolicy` domain so server/user/KB AI policy changes refresh every visible effective-policy and AI-availability indicator without a page reload. Pure web wiring (apps/web/src) — NO API/schema/migration.
- invalidation.tsx: added exported `AI_POLICY_STALE_MESSAGE` ('AI policy changed. Rerun for current AI availability.'). The `aiPolicy` domain already existed in INVALIDATION_DOMAINS (US-035).
- AiPolicySettings.tsx: KbAiPolicy (INSIDE the per-KB bus) now `const invalidate = useInvalidate()`; KB-save publishes `invalidate(['aiPolicy'])` (AC3) and a new `useInvalidationEffect(['aiPolicy'], reload)` re-fetches the selected-KB effective policy on ANY layer change (AC1/AC2/AC3). AiPolicySettings (server/user, OUTSIDE the bus) gained an `onServerOrUserPolicyChanged?` prop called after a server OR user save (in addition to its own reload of the global overview).
- App.tsx: AuthedHome holds `aiPolicyVersion` state, passes `onServerOrUserPolicyChanged={() => bump}` to AiPolicySettings and `aiPolicyVersion` to KnowledgeBases. New in-bus bridge `<AiPolicyInvalidator version>` rendered inside <InvalidationProvider> publishes `invalidate(['aiPolicy'])` whenever the version changes (skips initial mount via useRef seen-check) — the cross-boundary path so server/user changes reach the per-KB subscribers (AC1/AC2). CommandBox now has a `policyStale` flag + `useInvalidationEffect(['aiPolicy'], () => setPolicyStale(true))` + `<p data-testid="command-ai-policy-stale">` banner (cleared on rerun) — AC4.
- Answers.tsx: same `policyStale` flag + `answers-ai-policy-stale` banner (AC4).
- Proposals.tsx (quick capture): `policyStale` flag + `useInvalidationEffect(['aiPolicy'], ...)` + `capture-ai-policy-stale` banner shown when `lastResult && policyStale`; cleared on KB switch + next capture (AC4).
- Embeddings UI doesn't exist yet (US-048) so AC4's embeddings surface is N/A this story.
- Files changed: apps/web/src/{invalidation.tsx, AiPolicySettings.tsx, App.tsx, Answers.tsx, Proposals.tsx, App.test.tsx}.
- AC regression: 2 new App.test.tsx tests. (1) 'marks AI-availability surfaces stale after a KB AI policy change (US-045 AC3/AC4)' — selects a KB (admin role so KB editor renders), runs a command, changes+saves the KB policy, asserts command-ai-policy-stale appears + kb-ai-policy-effective re-fetches to the new mode + rerun clears the banner. (2) 'refreshes selected-KB surfaces after a server AI policy change across the provider boundary (US-045 AC1/AC4)' — same setup, changes+saves the SERVER policy (outside the per-KB bus), asserts the cross-boundary escape hatch flips command-ai-policy-stale + the KB effective re-fetches. Both use a state-based fetch mock where the policy GETs flip after a PUT.
- Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web 31/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against an ISOLATED throwaway DB (jotmind_us045) + own tsx API on :3045 (WORKER_INLINE + mock AI) + own vite on :5145 (temp VITE_DEV_PORT/VITE_PROXY_TARGET hooks, reverted): first-run setup -> create+select KB -> run command (no banner) -> save KB policy local_only -> command-ai-policy-stale shown -> effective stayed 'No AI' (strictest-wins, server/user still off) -> raise user + server to local_only via AiPolicySettings -> kb-ai-policy-effective refreshed to 'Local only' WITHOUT reload + command-ai-policy-stale shown + zero page errors.
- **Learnings for future iterations:**
  - See the Codebase Patterns "Selected-KB invalidation bus wiring" entry (now updated with the US-045 `aiPolicy` wiring + the cross-boundary AiPolicyInvalidator bridge).
  - GOTCHA: effective AI policy is STRICTEST-WINS across server+user+KB (off<local_only<remote*). A KB-only raise to local_only leaves effective 'off' while server/user default to 'off' — to SEE effective change in a real browser you must raise ALL THREE layers. Unit tests mock the policy GET so they can assert the re-fetch wiring with a flipped effective regardless.
  - AiPolicySettings (server/user) renders OUTSIDE the per-KB <InvalidationProvider>; the only way to reach the per-KB bus from there is the documented escape hatch (prop callback -> AuthedHome version counter -> in-bus <AiPolicyInvalidator>), same boundary problem as US-044.
  - vite.config.ts now (temporarily, reverted) supports VITE_DEV_PORT + VITE_PROXY_TARGET env overrides for running an isolated browser-verify stack; revert after.
---

## Mon 15 Jun 2026 00:09 +0800 - US-046
Thread: https://ampcode.com/threads/T-019ec6db-7bb1-77b5-b261-b477f45ef059
- Exposed sanitized AI provider config + readiness in the UI (read-only, env-only).
- Schemas: added aiProviderStatusSchema + AI_PROVIDER_CLASSIFICATIONS + buildAiProviderStatus(config|null) in packages/schemas/src/ai.ts (copies ONLY display-safe fields; never apiKey, AC4). Builder: remote=openai|anthropic, demo=mock, verified=!remote, embeddingsSupported=kind!='anthropic'.
- API: new apps/api/src/ai/status.ts resolveAiProviderStatusFromEnv (reads AI_PROVIDER_CONFIG); GET /api/ai/provider added to createAiPolicyRouter (authed-only, injectable resolveProviderStatus).
- Web: api.ts getAiProviderStatus(); new exported <AiProviderStatusPanel/> in AiPolicySettings.tsx rendered at top of the AI privacy section.
- Files: packages/schemas/src/{ai.ts,ai.test.ts}, apps/api/src/ai/status.ts, apps/api/src/ai-policy/{index.ts,ai-policy.test.ts}, apps/web/src/{api.ts,AiPolicySettings.tsx,App.test.tsx}.
- **Learnings for future iterations:**
  - Provider config is ENVIRONMENT-ONLY (AI_PROVIDER_CONFIG JSON). There is NO persisted provider store/table yet — US-047 will decide whether to add one or keep env-only (AC5 of US-047: show no non-functional edit controls if deferred). US-046 already returns editable:false/configSource:'environment', so the UI states it cannot be edited in the browser.
  - To return provider info to the browser, ALWAYS build it via buildAiProviderStatus (schemas) — it is an allowlist of display-safe fields, so apiKey can never leak (mirrors toPortableProviderConfig's allowlist approach). Do NOT JSON-serialize a raw AiProviderConfig.
  - readiness 'verified' reuses the existing convention: remote (openai/anthropic) ship configured-but-untested (verified:false); local/mock are verified:true.
  - The GET /api/ai/provider route is authed-only (any signed-in user) since it carries no secrets — matches the GET /api/ai/policy/server visibility.
  - US-048 (embeddings status) + US-046 provider info both belong near KB AI policy/admin tooling; the AiProviderStatusPanel lives in the server/user AI privacy section (AuthedHome, OUTSIDE the per-KB bus).
---

## Mon 15 Jun 2026 00:26 +0800 - US-047
- Handled provider configuration editing affordances safely by keeping persisted provider editing deferred and making the environment-only setup path explicit.
- `<AiProviderStatusPanel/>` now points operators to the server `AI_PROVIDER_CONFIG` JSON/deployment secret manager and states that browser provider edit fields are intentionally not rendered, so API keys are not entered into non-functional UI.
- Added focused web regression coverage asserting the provider panel renders no textbox/select/button edit controls when backend status is `editable:false`/`configSource:'environment'`.
- Updated `AGENTS.md` with the reusable provider setup rule and marked `US-047` passing in `prd.json`.
- Browser verification: Playwright Chromium against the real React app with mocked API responses showed the OpenAI provider status, `AI_PROVIDER_CONFIG` guidance, and zero provider edit controls inside the panel. Temporary spec removed.
- Verified: `pnpm --filter @jotmind/web test -- --run App.test.tsx`; `pnpm verify` fully green.
- Files changed: `apps/web/src/AiPolicySettings.tsx`, `apps/web/src/App.test.tsx`, `AGENTS.md`, `prd.json`, `progress.txt`.
- **Learnings for future iterations:**
  - Until a persisted provider store/API exists, provider setup UI must remain read-only and environment-guided; do not add disabled or placeholder provider edit controls.
  - Tests for deferred edit affordances should assert the absence of form controls, not just the presence of explanatory copy.
  - Mocked Playwright browser verification for this UI only needs `/api/auth/me`, `/api/knowledge-bases`, `/api/ai/provider`, and `/api/ai/policy`; the `/me` fixture must include `createdAt`/`updatedAt` or the auth schema rejects it.
---
