# Ralph Progress Log
Started: Sat 13 Jun 2026 08:14:49 PM +08
---

## Codebase Patterns
- Environment has ONLY Node 18. Pin tooling to Node-18-compatible versions: Vite 5, Vitest 2, ESLint 9, TypeScript 5.x, pnpm 9. Latest pnpm needs Node 22+.
- pnpm is installed globally at ~/.npm-global/bin (npm prefix set to ~/.npm-global). Prepend `~/.npm-global/bin` to PATH in every shell: `export PATH="$HOME/.npm-global/bin:$PATH"`.
- Monorepo = pnpm workspace (`apps/*`, `packages/*`). Shared package `@jotmind/schemas` exports TS source (`./src/index.ts`), NOT built dist, so tsc (moduleResolution: Bundler) + Vite resolve directly with no build step.
- Do NOT add TS project `references` from apps to packages/schemas — composite refs make tsc resolve unbuilt dist/*.d.ts and fail with TS6305. Rely on package `exports` -> source.
- tsconfig.base.json is strict with `verbatimModuleSyntax` -> import types with `import { type X }`. Relative imports use `.js` extensions even in .ts/.tsx (ESM).
- Validation: `pnpm verify:quick` (format:check+typecheck+lint+test) for fast loop, `pnpm verify` for full (adds test:api, test:e2e). `pnpm test` excludes @jotmind/api (run via `pnpm test:api`).
- Playwright browsers must be installed once: `pnpm --filter @jotmind/web exec playwright install chromium`.
- Prettier ignores Ralph scaffolding (prd.json, progress.txt, prompt.md, .agents, etc.) via .prettierignore. Add new non-source top-level files there if they break format:check.
- DB stack = PostgreSQL 18 + Apache AGE 1.7.0 + pgvector 0.8.1 via infra/db/Dockerfile (FROM apache/age:latest, compile pgvector). pgvector MUST be >= v0.8.1 (older fails to build on PG 18). PG 18 docker volume MUST mount at /var/lib/postgresql (NOT /data) or the container exits.
- ORM = Drizzle (drizzle-orm + postgres-js + drizzle-kit) in apps/api. Schema: apps/api/src/db/schema.ts; generate: `pnpm --filter @jotmind/api db:generate`; apply: `db:migrate`. Commit generated apps/api/drizzle/** (SQL + meta/).
- DB access gated on DATABASE_URL: when unset, checkDatabaseHealth() -> configured:false and /api/health stays ok. DB-needing tests use describe.skipIf(!process.env.DATABASE_URL) so default test:api is green without infra.
- DB-backed routes: use an injectable store interface (e.g. AuthStore) + DB impl + in-memory fake; inject via createApp options (createApp({checkDatabase, authStore})). Lets route handlers be unit-tested with no live DB. Mirror this for new domains.
- @node-rs/argon2 `Algorithm` is an ambient const enum -> cannot value-import under verbatimModuleSyntax. Use numeric literal: `const X: Algorithm = 2 as Algorithm` (Argon2id===2). Same caution for any ambient const enum dependency.
- Auth: Argon2id passwords, PG-backed sessions (sessions table), HTTP-only `jm_session` cookie, CSRF synchronizer token (`jm_csrf` readable cookie + `x-csrf-token` header) on cookie-auth mutations. System account role (admin|member) is separate from KB-scoped roles (US-004+). Web mutations need credentials:include + x-csrf-token.
- dev-browser skill is NOT available in this env. For required browser verification of UI stories, use Playwright + Chromium (installed) against the running API (write a temp e2e spec, run, screenshot, then delete it).
- Canonical graph writes (entities US-008, claims US-009, etc.) must write the relational row, enqueueGraphOutbox(tx,...), AND an audit_events row in ONE transaction. dbGraphWriteStore (graph/store.ts) only does outbox; per-domain stores (dbEntityStore) add the audit insert. Reads filter `deleted_at IS NULL` and scope by knowledgeBaseId.
- Nested KB-scoped routes: a separate Router({mergeParams:true}) mounted at /api/knowledge-bases/:kbId/<thing> (AFTER the KB router, which has no matching nested route so it falls through). Reuse kbStore.getRole in a local requireKbRole(min): 404 for non-members (hide existence), 403 for too-low role. Reads need viewer; mutations need editor + requireCsrf.
- Web interaction tests: @testing-library/user-event is NOT installed — use fireEvent from @testing-library/react. For real-browser verification, write a TEMP playwright config with two webServers (API w/ DATABASE_URL env + reuseExistingServer:true, web dev), truncate DB users first (so first-run setup shows), run, screenshot, then delete the temp files.
- Background work = durable `jobs` table (US-007). Enqueue via dbJobStore.enqueue({type,...}); register handlers in apps/api/src/jobs/handlers.ts:defaultJobHandlers. Workers claim atomically via claimNext (FOR UPDATE SKIP LOCKED in a tx); runNextJob handles capped-exponential-backoff retry + dead-letter. Two run modes share runNextJob: `pnpm --filter @jotmind/api worker` (separate process) and startInProcessWorker (WORKER_INLINE=true in server.ts). Don't invent new background loops.
- Drizzle dynamic SQL: a helper that takes table COLUMNS to build conditions must type the param as `SQLWrapper` (or `AnyColumn|SQL`), NOT `SQL` (a PgColumn is not an SQL). `sql\`${col}::text ILIKE ${pattern}\`` works for text AND JSONB columns; cast `::text` to search JSONB array/object contents. Escape LIKE wildcards (\ % _) in user input. `db.execute<T>(sql)` returns the rows array directly; use information_schema/to_regclass to feature-detect tables. For req.query schemas use z.coerce.number + union/transform for booleans.
- Only entity/claim/note/source are GRAPH_TARGET_TYPES (project to AGE via outbox). Records of those kinds emit graph_outbox + audit in one tx; NON-projected records (e.g. source_excerpts) emit audit ONLY (no enqueueGraphOutbox). Add a kind to GRAPH_TARGET_TYPES in @jotmind/schemas graph.ts before enqueuing outbox for it. New KB-scoped domain = mirror entity/claim: store interface + db<Domain>Store + in-memory fake test + Router({mergeParams:true}) mounted AFTER the KB router + createApp seam.
- Web: App.tsx is ~1.7k lines — put new large components in their OWN file (e.g. apps/web/src/GraphViews.tsx) and import into App.tsx; don't keep growing App.tsx. Per-KB sibling components (<Entities>/<Claims>/<Capture>/<GraphViews>) each fetch their own data once per kb.id and DON'T live-sync — records created in one won't show in another until reload+reselect (browser tests must reload+reselect after creating). Watch for testid collisions across these sections (e.g. sources-empty) — scope RTL/Playwright queries to the section's container testid.

## Sat 13 Jun 2026 20:25 +08 - US-001
Thread: https://ampcode.com/threads/T-019ec0ea-b9f7-74a4-8859-43f08d85d8f4
- Created the typed pnpm monorepo foundation.
- apps/api: Express app with `/api/health` returning a schema-validated HealthStatus; src/app.ts (testable factory) + src/server.ts (binds 127.0.0.1:3001 by default).
- apps/web: React 18 + Vite 5 app rendering API health; proxies /api to :3001. Vitest (jsdom + testing-library) unit test + Playwright e2e smoke test.
- packages/schemas: shared Zod healthStatusSchema + parseOrThrow util, imported by both apps.
- Root tooling: package.json scripts (format, format:check, typecheck, lint, test, test:api, test:e2e, verify:quick, verify), eslint.config.js (flat), .prettierrc.json, .prettierignore, tsconfig.base.json, pnpm-workspace.yaml, .gitignore, README.md, AGENTS.md.
- Verified: `pnpm verify` fully green; API boots & curl /api/health returns valid JSON; web `vite build` succeeds; Playwright smoke test passes.
- **Learnings for future iterations:**
  - See Codebase Patterns above (Node 18 pinning, PATH for pnpm, schemas-resolve-from-source, no TS project references, .js import extensions).
  - The web e2e smoke test runs web-only; /api proxy errors in logs are expected/benign because the API isn't started during `pnpm test:e2e`. Future stories that need real API in e2e should start it (or run via root).
  - US-002 next: PostgreSQL + Apache AGE + pgvector via Docker Compose, Drizzle migrations, and a real DB-backed health check (extend /api/health).
---

## Sat 13 Jun 2026 20:42 +08 - US-002
Thread: https://ampcode.com/threads/T-019ec0f2-8f1d-770c-9dea-473056778dea
- Provisioned the canonical PostgreSQL store with Apache AGE + pgvector and wired DB health into the API.
- infra/db/Dockerfile: FROM apache/age:latest (PG 18 + AGE 1.7.0) and compiles pgvector v0.8.1. infra/db/init/01-extensions.sql creates both extensions on first volume init.
- docker-compose.yml (root): `db` service builds the image, binds 127.0.0.1:5432, named volume jotmind-db mounted at /var/lib/postgresql (PG18 convention), healthcheck via pg_isready.
- apps/api: added drizzle-orm + postgres + drizzle-kit. src/db/schema.ts (minimal app_meta table = initial schema), src/db/client.ts (lazy postgres-js pool + drizzle), src/db/health.ts (checkDatabaseHealth + isDatabaseHealthy), src/db/migrate.ts (creates extensions then runs migrations; CLI-runnable), drizzle.config.ts, drizzle/0000_*.sql + meta/. Scripts: db:generate, db:migrate.
- /api/health (apps/api/src/app.ts) now reports {database:{configured,connected,extensions:{age,vector},error?}}; status degraded if configured but unreachable/missing ext, ok otherwise. createApp accepts injectable checkDatabase for unit tests.
- packages/schemas/src/health.ts: added databaseExtensionsSchema + databaseHealthSchema; healthStatusSchema.database is OPTIONAL (keeps web mock + old callers valid).
- Tests: app.test.ts unit-covers ok/degraded/not-configured via injected mock; db.integration.test.ts is env-gated (describe.skipIf(!DATABASE_URL)) and verifies migrations create app_meta + extensions report true. Docs: README DB section + browser-storage-non-canonical/backup warning; .env.example.
- Files changed: infra/db/{Dockerfile,init/01-extensions.sql}, docker-compose.yml, .env.example, apps/api/{package.json,drizzle.config.ts,drizzle/**,src/db/**,src/app.ts,src/app.test.ts}, packages/schemas/src/health.ts, README.md, AGENTS.md, pnpm-lock.yaml.
- Verified: `pnpm verify` fully green (integration tests skip w/o DB). Brought up compose DB, confirmed extensions via init script, ran integration tests + live `curl /api/health` against it -> database.connected:true, age:true, vector:true. Cleaned up containers/temp build image.
- **Learnings for future iterations:**
  - See Codebase Patterns + AGENTS.md "Database" section. KEY GOTCHAS: pgvector must be >= v0.8.1 to build on PG 18; PG 18 docker volume MUST mount at /var/lib/postgresql (not /data) or the container exits on start.
  - apache/age:latest is PG 18 + AGE 1.7.0 and auto-creates the `age` extension on the default DB; still create it idempotently for clarity/other DBs.
  - Keep DB access gated on DATABASE_URL and health graceful (configured:false -> ok) so unit tests / no-DB runs stay green. US-005 will add the real canonical graph tables on top of this Drizzle setup; US-034 will make API tests run against a test PG container (then the integration test stops skipping in CI).
  - To run DB-backed tests locally: `docker compose up -d --build db` then `DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/jotmind pnpm --filter @jotmind/api test`.
---

## Sat 13 Jun 2026 20:53 +08 - US-003
Thread: https://ampcode.com/threads/T-019ec100-c051-7796-9f00-09e9846afce2
- Implemented local/server accounts + PostgreSQL-backed sessions with Argon2id, HTTP-only session cookies, CSRF, first-run admin setup, and admin-controlled account creation.
- packages/schemas/src/auth.ts: shared Zod schemas (accountRole admin|member, credentials, createAccount, publicUser, setupStatus, authState). Exported from index.ts.
- apps/api/src/db/schema.ts: added `users` (uuid pk gen_random_uuid, email unique, password_hash, role default member, timestamps) + `sessions` (text token pk, user_id FK cascade, csrf_token, expires_at, created_at). Migration drizzle/0001_colorful_vampiro.sql.
- apps/api/src/auth/password.ts: hashPassword/verifyPassword via @node-rs/argon2 (Argon2id). verifyPassword swallows malformed-hash errors -> false.
- apps/api/src/auth/store.ts: AuthStore interface + dbAuthStore (Drizzle-backed, lazy getDb per call). Injectable for tests.
- apps/api/src/auth/index.ts: createAuthRouter({store}) mounted at /api/auth in createApp. Endpoints GET /setup-status, POST /setup (first-run admin, 409 if any user), POST /login (401 generic, dummy-verify to blunt user enumeration), GET /me, POST /logout, POST /users (admin-only). issueSession sets jm_session (HttpOnly, Secure in prod/COOKIE_SECURE, SameSite=Lax) + jm_csrf (readable). requireAuth/requireAdmin/requireCsrf middleware; req.auth via global Express Request augmentation. CSRF = synchronizer token compared constant-time (timingSafeEqual).
- apps/api/src/app.ts: mount cookieParser() + auth router; createApp now accepts {authStore}.
- apps/web/src/api.ts: typed fetch client (credentials:include, x-csrf-token header on mutations). apps/web/src/App.tsx: phase machine (loading->setup|login->authed); SetupForm/LoginForm/AuthedHome/AccountCreator (admin-only). App.test.tsx rewritten to mock the auth endpoints.
- Files changed: packages/schemas/src/{auth.ts,index.ts}, apps/api/{package.json, src/db/schema.ts, drizzle/0001*+meta, src/app.ts, src/auth/*}, apps/web/src/{api.ts,App.tsx,App.test.tsx}, README.md (Authentication section), AGENTS.md (Authentication section), .env.example (COOKIE_SECURE), pnpm-lock.yaml.
- Verified: `pnpm verify` fully green. Brought up compose DB, ran db:migrate, ran live integration tests (auth store create user/session + Argon2id round-trip). Exercised full flow via curl cookie jar: setup-status true->false, setup 201 (HttpOnly cookie), 2nd setup 409, /me, create-account 403 without CSRF / 201 with, member 403 (not admin), logout 204 + /me 401. Playwright browser test: login form renders, login succeeds, "Signed in as admin@example.com" + admin Create-account panel shown, logout returns to Sign in (screenshot captured). Cleaned up temp spec + DB containers.
- **Learnings for future iterations:**
  - See AGENTS.md "Authentication" section + Codebase Patterns below. KEY: @node-rs/argon2 `Algorithm` is an ambient const enum -> cannot value-import under verbatimModuleSyntax; use `2 as Algorithm` (Argon2id===2).
  - Injectable-store pattern is the way to keep DB-backed routes unit-testable without a live DB: define an interface, ship a DB impl + in-memory fake, inject via createApp options. Mirror this for US-004+ (KB routes) and future stories.
  - createApp injection seams: `{ checkDatabase, authStore }`. Add new seams the same way.
  - dev-browser skill is NOT available in this environment; used Playwright+Chromium (already installed) against the running API for browser verification. Do the same for future UI stories.
  - To run DB-backed auth tests: `docker compose up -d --build db`, `DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/jotmind pnpm --filter @jotmind/api db:migrate`, then `DATABASE_URL=... pnpm --filter @jotmind/api test`.
  - US-004 (next) adds knowledge_base_id scoping + KB roles (owner/admin/editor/viewer) + role middleware + audit events; the system account role (admin/member) here is separate from KB-scoped roles.
---

## Sat 13 Jun 2026 21:05 +08 - US-004
Thread: https://ampcode.com/threads/T-019ec10c-85b6-70d6-bd9f-18b7089f516f
- Implemented Knowledge Base scope + role-based access control + audit events.
- DB (apps/api/src/db/schema.ts): knowledge_bases (uuid pk, name, description, created_by FK users ON DELETE restrict), knowledge_base_members (composite PK (knowledge_base_id, user_id), role text, timestamps), audit_events (uuid pk, nullable knowledge_base_id FK cascade + actor_user_id FK set null, action, target_type/target_id, JSONB metadata default '{}'). Migration drizzle/0002_rare_maddog.sql + meta.
- Shared schemas packages/schemas/src/knowledge-base.ts: KB_ROLES=['viewer','editor','admin','owner'] (least->most), KB_ROLE_RANK + kbRoleSatisfies(role,required), create/list/member/assign/audit Zod schemas. Exported from index.ts. KB roles are SEPARATE from the system account role (admin|member in auth.ts).
- Routes apps/api/src/kb/{store.ts,index.ts}: createKnowledgeBaseRouter({store,authStore}) mounted at /api/knowledge-bases in createApp. GET / (list own KBs), POST / (create -> creator becomes owner, CSRF), GET /:id (viewer+), GET /:id/members + POST /:id/members (admin+, CSRF on POST), GET /:id/audit (admin+). requireKbRole(min) middleware loads caller membership for :id, returns 404 for non-members (hides existence) + 403 for insufficient role.
- Reused/exported shared auth middleware from auth/index.ts: requireAuth(authStore), requireCsrf, asyncHandler (were private; now exported). createApp resolves authStore once and passes to both auth + kb routers; seams now {checkDatabase, authStore, kbStore}.
- dbKnowledgeBaseStore: createKnowledgeBase + assignRole write membership + audit_events in the SAME transaction as the canonical write. Creation records knowledge_base.created + knowledge_base.role_assigned(owner); assignRole upserts (onConflictDoUpdate) + records role_assigned.
- Web: apps/web/src/api.ts listKnowledgeBases/createKnowledgeBase (uses knowledgeBaseListSchema; web has no direct zod dep so array schema lives in @jotmind/schemas). apps/web/src/App.tsx KnowledgeBases section in AuthedHome: lists KBs with role, click-to-select, create form.
- Tests: 9 kb unit (in-memory KB store + in-memory auth store, full HTTP via supertest agent) + 1 kb integration (skipIf no DATABASE_URL) + 3 schema unit + 1 web list test. Added apps/api/vitest.config.ts with fileParallelism:false.
- Files changed: packages/schemas/src/{knowledge-base.ts,knowledge-base.test.ts,index.ts}, apps/api/src/db/schema.ts, apps/api/drizzle/0002*+meta, apps/api/src/auth/index.ts (export middleware), apps/api/src/app.ts, apps/api/src/kb/{store.ts,index.ts,kb.test.ts,kb.integration.test.ts}, apps/api/vitest.config.ts, apps/web/src/{api.ts,App.tsx,App.test.tsx}, AGENTS.md, prd.json.
- Verified: pnpm verify fully green (format:check, typecheck, lint, test, test:api, test:e2e; integration tests skip w/o DB). Against live compose DB: db:migrate applied 0002, kb+auth+db integration tests pass. Playwright+Chromium browser flow: first-run setup -> create "My First KB" -> appears as "My First KB (owner) — selected"; screenshot confirmed. Cleaned up temp spec + stopped DB container.
- **Learnings for future iterations:**
  - See AGENTS.md "Knowledge Bases, roles & audit" section. KEY: KB roles (owner/admin/editor/viewer) are NOT the system account role (admin/member). Use kbRoleSatisfies + KB_ROLE_RANK for role gating.
  - GOTCHA: DB integration test files share one database and TRUNCATE the same tables; vitest runs files in parallel by default -> one file's TRUNCATE wipes another's fixtures mid-test (FK violations). Fixed with apps/api/vitest.config.ts fileParallelism:false. Keep it for future *.integration.test.ts files.
  - GOTCHA: apps/web has no direct `zod` dependency. Don't `import { z } from 'zod'` in web; add array/derived schemas to @jotmind/schemas instead (e.g. knowledgeBaseListSchema).
  - PATTERN: record audit_events in the SAME transaction as the canonical mutation, and reuse exported auth middleware (requireAuth/requireCsrf/asyncHandler) + requireKbRole for KB-scoped routes. US-005+ graph writes should follow this (and add graph_outbox in the same tx for US-006).
  - To run a manual browser check: start DB (docker compose up -d --build db), migrate, start API detached with setsid (DATABASE_URL=... COOKIE_SECURE=false setsid pnpm --filter @jotmind/api dev <... &), then `pnpm --filter @jotmind/web exec playwright test <tmp spec>` (playwright auto-starts the vite web server which proxies /api -> :3001). Background API via plain `nohup ... &` got killed between tool calls; `setsid ... < /dev/null & disown` survived.
  - US-005 (next) adds the canonical graph tables (entities, claims, notes, sources, ...) all carrying knowledge_base_id referencing knowledge_bases.
---

## Sat 13 Jun 2026 21:20 +08 - US-005
Thread: https://ampcode.com/threads/T-019ec117-7377-754f-9fc8-eb19f5e01f67
- Added the canonical relational graph schema tables (source of truth; AGE is a derived projection fed via graph_outbox in US-006).
- apps/api/src/db/schema.ts: new tables schema_definitions, schema_versions, entities, claims, claim_arguments, notes, sources, source_excerpts, rule_definitions, proposals, jobs, graph_outbox (audit_events already existed from US-004). Migration drizzle/0003_massive_kronos.sql (+ meta snapshot).
- Conventions: UUIDv7 PKs via PG18-native uuidv7() default (NOT gen_random_uuid()); knowledge_base_id FK cascade; created_at/updated_at timestamptz; nullable deleted_at soft-delete; created_by FK users set-null; JSONB props/metadata/provenance/spec/payload default '{}'/'[]'. Operational tables (jobs, graph_outbox) have no soft-delete.
- entities/claims reference nullable schema_version_id (validated-against version); type/predicate are conceptual string types for cross-version search (US-028). claim_arguments are role-labeled and entity-ref OR literal (argument_kind + check constraint) -> multi-argument, not binary edges. notes & sources are first-class canonical records, not entity rows.
- DB check constraints (confidence 0..1, valid-time order, span order, source_excerpt single-origin, entity not-self-merge, status enums) + partial unique indexes (deleted_at IS NULL) for recreatable schema defs/rules + helpful indexes (kb/type, kb/predicate, claimable status/run_after/created_at, etc.). entities.merged_into_id is a self-FK (AnyPgColumn cast). graph_outbox + jobs carry projection/retry fields for US-006/US-007.
- packages/schemas/src/graph.ts: custom-property validation HOOK validateCustomProperties(propertySchema, properties) (US-005 AC3), PropertySchema/PropertyField types, BUILTIN_ENTITY_TYPES (Person/Event/Concept/Place/Character), and status enums (proposal/rule/job/outbox/schema-kind). Exported from index.ts. 8 unit tests in graph.test.ts.
- apps/api/src/db/graph-schema.integration.test.ts (skipIf no DATABASE_URL): asserts all 12 graph tables exist; inserts user/kb/note/source/2 entities/multi-arg claim/3 claim_arguments (incl. literal)/outbox event with UUIDv7 ids (verifies time-sortable ordering); asserts confidence check rejects value=2.
- Files changed: apps/api/src/db/schema.ts, apps/api/drizzle/0003_massive_kronos.sql + meta/{0003_snapshot.json,_journal.json}, apps/api/src/db/graph-schema.integration.test.ts, packages/schemas/src/{graph.ts,graph.test.ts,index.ts}, AGENTS.md, prd.json, progress.txt.
- Verified: pnpm verify fully green (format:check, typecheck, lint, test, test:api, test:e2e; integration tests skip w/o DB). Against live compose DB (docker compose up -d --build db): db:migrate applied 0003, full apps/api test run 36/36 pass incl. graph-schema integration, `\dt` confirms all 12 graph tables + audit_events. Cleaned up DB container + image.
- **Learnings for future iterations:**
  - See AGENTS.md "Canonical graph schema (US-005)" section. KEY: use uuidv7() default for canonical graph PKs (PG18 native); older auth/KB tables use gen_random_uuid() — don't mix expectations.
  - GOTCHA: a Zod field with `.optional().default(false)` makes the inferred OUTPUT type REQUIRE that field (default is applied on output), so object literals typed as the inferred type must include it. Use plain `.optional()` (no default) when you want the type to stay optional.
  - GOTCHA: drizzle-generated migration meta JSON + the new migration SQL are NOT prettier-ignored — run `pnpm format` after `db:generate` or format:check fails.
  - Self-referencing FK in drizzle needs `.references((): AnyPgColumn => table.col, {...})` (import type AnyPgColumn from drizzle-orm/pg-core).
  - drizzle check()/uniqueIndex().where()/index() are available in drizzle-orm 0.36; partial unique indexes use `.where(sql\`deleted_at IS NULL\`)`.
  - schema_version_id is nullable on entities/claims so US-008/009 (manual entity/claim CRUD) can run before US-027 (custom schemas). US-006 (next) wires graph_outbox events into the same tx as entity/claim/note/source writes + adds the projector seam.
---

## Sat 13 Jun 2026 21:30 +08 - US-006
Thread: https://ampcode.com/threads/T-019ec123-5e2b-719a-99eb-e5eb2a0cb7d4
- Implemented the graph outbox + projection seam so AGE can stay a derived projection.
- apps/api/src/graph/outbox.ts: enqueueGraphOutbox(tx, events) helper (accepts a tx via DbExecutor type so the graph_outbox insert is in the SAME transaction as the canonical write) + outboxEventType("<target>.<event>").
- apps/api/src/graph/store.ts: GraphWriteStore interface + dbGraphWriteStore. createEntity/createNote/createSource/createClaim each write canonical row(s) + matching graph_outbox event in one transaction; createClaim also writes claim_arguments in-tx. This is the write seam US-008/009/011 will build on.
- apps/api/src/graph/projector.ts: Projector interface {name, stubbed, project(event)}; stubProjector (logs, stubbed:true, NO AGE writes yet); ProjectionStore interface + dbProjectionStore; processOutbox() drains pending events ordered by (created_at, id) with FOR UPDATE SKIP LOCKED and marks each processed/failed (+attempts/last_error); rebuildProjection() repairs by reprojecting all non-deleted canonical records straight from relational tables (bypasses outbox); getProjectionStatus() returns {projector{name,stubbed}, counts{pending,processed,failed}}.
- apps/api/src/graph/index.ts: createGraphRouter mounted at /api/graph in createApp. GET /projection/status (any authed user), POST /projection/process + POST /projection/rebuild (system admin only + requireCsrf).
- Stubbed state visible (AC3): createApp logs the stub notice ONCE per process (module flag in app.ts to avoid flooding test output) and /api/graph/projection/status exposes stubbed:true.
- requireAdmin now EXPORTED from auth/index.ts (was private) for reuse by graph maintenance routes. createApp seams now {checkDatabase, authStore, kbStore, projectionStore, projector}.
- packages/schemas/src/graph.ts: added GRAPH_TARGET_TYPES (entity/claim/note/source), GRAPH_EVENT_TYPES (created/updated/deleted), and graphProjectionStatusSchema.
- Files changed: packages/schemas/src/graph.ts; apps/api/src/graph/{outbox,store,projector,index,graph.test,graph.integration.test}.ts; apps/api/src/auth/index.ts (export requireAdmin); apps/api/src/app.ts (mount /api/graph + once-per-process stub log + seams); AGENTS.md; prd.json; progress.txt.
- Tests: 11 graph unit (in-memory store + recording projector: outbox helper, processOutbox success/fail/no-reprocess, rebuild, status, API auth/non-admin-403/csrf-403/admin-drain) + 5 graph integration (skipIf no DATABASE_URL: same-tx outbox event for entity/note/source/claim, tx ROLLBACK leaves no outbox/claim when a check constraint fails, processOutbox with stub, rebuild count, soft-deleted excluded from rebuild).
- Verified: pnpm verify fully green; brought up compose DB, db:migrate, ran all 52 api tests incl the 5 graph integration tests -> pass. Tore down DB.
- **Learnings for future iterations:**
  - See new AGENTS.md "Graph outbox & projection seam (US-006)" section. KEY: never write a canonical graph row (entity/claim/note/source) without enqueuing the matching graph_outbox event in the SAME transaction — always go through dbGraphWriteStore (or call enqueueGraphOutbox(tx, ...) inside your own tx). Mirror this for future graph writes (updates/deletes use eventType 'updated'/'deleted').
  - The projector is STUBBED (stubProjector, stubbed:true). The real Apache AGE projector replaces it later: implement Projector.project to write AGE, set stubbed:false, and swap the default in createApp. processOutbox/rebuildProjection already handle batching/marking/repair — only project() changes.
  - DbExecutor type in outbox.ts = Database | tx param; lets helpers run under getDb() OR a tx. Reuse for other in-transaction helpers.
  - US-007 (next) adds the PostgreSQL-backed jobs foundation (jobs table already exists from US-005: status/attempts/max_attempts/run_after/failure_reason/owner_user_id). Follow the same injectable-store + claim-with-SKIP-LOCKED pattern used here for processOutbox.
---

## Sat 13 Jun 2026 21:40 +08 - US-007
Thread: https://ampcode.com/threads/T-019ec12c-75da-75f7-8b39-8f2262dd9cb9
- Implemented the durable PostgreSQL-backed jobs foundation (table already existed from US-005).
- apps/api/src/jobs/store.ts: JobStore interface + dbJobStore. claimNext() does FOR UPDATE SKIP LOCKED select inside a tx then sets status=running + attempts++ (atomic, multi-worker safe). enqueue/markSucceeded/markForRetry/markFailed/getById/list(filter)/countByStatus.
- apps/api/src/jobs/worker.ts: computeBackoffMs (capped exponential, base 1s/cap 5min). runNextJob (claim->run handler for job.type; success->markSucceeded; throw-> retry with runAfter=now+backoff until attempts>=maxAttempts then dead-letter via markFailed; missing handler-> immediate dead-letter). processAvailableJobs (drain). runWorkerLoop + startInProcessWorker (two run modes share runNextJob). Clock injectable via now:()=>Date for deterministic backoff tests.
- apps/api/src/jobs/handlers.ts: defaultJobHandlers registry (incl noop). worker-process.ts: separate-process entry (graceful SIGINT/SIGTERM, WORKER_POLL_INTERVAL_MS, closeDb on stop).
- apps/api/src/jobs/index.ts: createJobsRouter at /api/jobs -> GET / (list, ?status/?knowledgeBaseId/?limit), GET /stats, GET /:id (404). All system-admin only, read-only (no CSRF needed). toPublicJob serializer.
- Wiring: app.ts adds jobStore seam + mounts /api/jobs; server.ts starts in-process worker when WORKER_INLINE=true; package.json adds `worker` script. packages/schemas/src/jobs.ts (publicJob/jobListQuery/jobStats, reuses jobStatusSchema from graph.ts) + index export.
- Tests: jobs.test.ts (backoff, runNextJob success/no-handler/retry->deadletter w/ injected clock, processAvailableJobs, admin API 401/403/list+filter/400/stats/get+404) + jobs.integration.test.ts (skipIf no DATABASE_URL, TRUNCATE jobs/knowledge_bases/users).
- Files changed: apps/api/src/jobs/{store,worker,handlers,worker-process,index,jobs.test,jobs.integration.test}.ts, apps/api/src/{app,server}.ts, apps/api/package.json, packages/schemas/src/{jobs.ts,index.ts}, AGENTS.md.
- Verified: pnpm verify fully green; live compose DB (DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/jotmind) db:migrate + all 69 tests pass incl integration; separate-process worker smoke-tested end-to-end (inserted noop job -> `pnpm --filter @jotmind/api worker` claimed+ran it -> status=succeeded with result populated).
- **Learnings for future iterations:**
  - The jobs DB queue pattern (claimNext via FOR UPDATE SKIP LOCKED + tx) is the canonical way to add background work. Register new job types in apps/api/src/jobs/handlers.ts:defaultJobHandlers and enqueue via dbJobStore.enqueue(). US-018 (AI proposals), US-021 (embeddings), imports/exports, and the real AGE projector worker should all plug in here rather than inventing new loops.
  - GOTCHA when testing backoff: claimNext filters runnable jobs by `runAfter <= now`. If you enqueue with default runAfter (real now) but call runNextJob with an injected past `now`, the job is NOT runnable and you get undefined. Either enqueue with runAfter matching your injected base, or advance the injected clock forward across retries (see jobs.test.ts / jobs.integration.test.ts).
  - `claimNext` increments attempts, so inside a handler / in runNextJob, `job.attempts` already counts the current attempt. Dead-letter check is `attempts >= maxAttempts` (maxAttempts default 5 => 5 total attempts).
  - createApp injection seams are now {checkDatabase, authStore, kbStore, projectionStore, projector, jobStore}. Mirror the injectable-store pattern for any new DB-backed domain.
  - This was a backend-only story; US-007 ACs do not require browser verification (no UI). dev-browser still unavailable in this env.
---

## Sat 13 Jun 2026 21:52 +08 - US-008
Thread: https://ampcode.com/threads/T-019ec136-d2d1-7001-9672-c2e53bdf93ee
- Implemented manual create/edit of typed entities (built-in types Person/Event/Concept/Place/Character) with KB role gating, audit + graph outbox events, and a web UI.
- packages/schemas/src/graph.ts: added entitySchema, entityListSchema, createEntitySchema (type+name required, trims), updateEntitySchema (all-optional, .refine requires >=1 field, nullable description). Exported via index.ts (graph.js is re-exported). + graph.test.ts cases.
- apps/api/src/entities/store.ts: EntityStore interface + dbEntityStore. createEntity/updateEntity write the canonical `entities` row AND enqueueGraphOutbox(tx,...) AND an audit_events row in ONE transaction (entity.created/entity.updated). Reads (list/get) filter deleted_at IS NULL and scope by knowledgeBaseId.
- apps/api/src/entities/index.ts: createEntityRouter({store,kbStore,authStore}) as Router({mergeParams:true}); mounted at /api/knowledge-bases/:kbId/entities in app.ts (AFTER the KB router; KB router has no /:id/entities so it falls through). Local requireKbRole(min) uses kbStore.getRole -> 404 for non-members, 403 for too-low role. GET / + GET /:entityId require viewer; POST / + PATCH /:entityId require editor + requireCsrf.
- apps/api/src/app.ts: added entityStore seam + mount.
- apps/web/src/api.ts: listEntities/createEntity/updateEntity (credentials:include, x-csrf-token on mutations). apps/web/src/App.tsx: <Entities> component shown when a KB is selected; kbRoleSatisfies(kb.role,'editor') gates the form vs a read-only notice; type dropdown (BUILTIN_ENTITY_TYPES), name, comma-separated aliases/tags, description, JSON custom-properties textarea, edit-in-place. App.test.tsx: added editor-creates-entity + viewer-read-only tests (stateful fetch mock distinguishing method).
- Files changed: packages/schemas/src/{graph.ts,graph.test.ts}, apps/api/src/{app.ts, entities/{store.ts,index.ts,entities.test.ts,entities.integration.test.ts}}, apps/web/src/{api.ts,App.tsx,App.test.tsx}, AGENTS.md.
- Verified: `pnpm verify:quick` + `pnpm test:api` green (9 entity unit tests, 4 integration tests pass against live compose DB confirming audit+outbox rows). Browser verification via temp Playwright spec against real API+DB: first-run setup -> create KB (owner) -> select KB -> create Person "Ada Lovelace" (aliases/tags/JSON props) -> shows in list -> edit name -> updates. Screenshot confirmed. Temp pw config/spec deleted afterward.
- **Learnings for future iterations:**
  - See AGENTS.md "Entities" section. KEY: canonical graph writes need BOTH outbox AND audit events in the same tx; dbGraphWriteStore (graph/store.ts) only does outbox, so entity/claim stores add the audit insert themselves. Mirror dbEntityStore for US-009 (claims) and US-010 (delete/merge -> set deleted_at + emit deleted outbox + audit).
  - Nested KB-scoped routes: separate Router({mergeParams:true}) mounted at /api/knowledge-bases/:kbId/<thing>, reusing kbStore.getRole for requireKbRole. Mount AFTER the KB router.
  - @testing-library/user-event is NOT installed; use fireEvent from @testing-library/react for web interaction tests. For browser verification, write a temp playwright config with two webServers (API with DATABASE_URL env + reuseExistingServer:true, and web dev server), truncate DB users first so the app shows first-run setup, then delete the temp files.
  - US-009 (next) adds claims (multi-argument, claim_arguments rows, confidence, provenance) building on the same canonical+outbox+audit transaction pattern.
---

## Sat 13 Jun 2026 22:05 +08 - US-009
Thread: https://ampcode.com/threads/T-019ec142-3146-727d-a3fa-008938680ebf
- Implemented manual claim create/edit with multi-argument, provenance-aware claims (predicate, description, confidence, valid-time range, role-labeled arguments).
- packages/schemas/src/graph.ts: added claim shapes — CLAIM_ARGUMENT_KINDS, claimArgumentSchema, claimSchema (includes arguments), claimListSchema, createClaimArgumentSchema (superRefine: entity needs entityId+no value, literal needs value+no entityId; argumentKind defaults 'entity'), createClaimSchema (predicate + >=1 arg, refine validStart<=validEnd, confidence 0..1), updateClaimSchema (all-optional, >=1 field, optional args-replace >=1). 10 new schema unit tests.
- apps/api/src/claims/store.ts: ClaimStore interface + dbClaimStore. createClaim inserts claims row + claim_arguments (sequential position) + graph_outbox(claim.created) + audit_events in ONE tx. updateClaim updates metadata and, when fields.arguments present, REPLACES the full argument set (hard-delete + reinsert) so roles/order are editable; emits claim.updated. Returns ClaimWithArguments. Reads filter deleted_at IS NULL, scope by KB.
- apps/api/src/claims/index.ts: createClaimRouter mounted at /api/knowledge-bases/:kbId/claims (mergeParams, AFTER KB router). Role gating mirrors entity router (reads viewer+, mutations editor+ + requireCsrf, 404 for non-members). toClaim() serializes Date->ISO + nests arguments.
- apps/api/src/app.ts: added claimStore seam + mounted claim router after entity router.
- apps/web/src/api.ts: listClaims/createClaim/updateClaim. apps/web/src/App.tsx: <Claims> component (rendered after <Entities> for selected KB) — fetches claims + entities for entity-arg dropdowns, dynamic add/remove argument rows, literal-vs-entity per-argument, edit-in-place, editor-only form.
- Files changed: packages/schemas/src/{graph.ts,graph.test.ts}, apps/api/src/{app.ts,claims/{store.ts,index.ts,claims.test.ts,claims.integration.test.ts}}, apps/web/src/{api.ts,App.tsx}, AGENTS.md, prd.json, progress.txt.
- Tests: 11 claims router unit (in-memory ClaimStore fake records audit+outbox), 4 claims integration (skipIf no DATABASE_URL: multi-arg create tx, metadata+args replace update, confidence check-constraint rejection, list w/ args), 10 schema unit. Verified: pnpm verify fully green; live compose DB db:migrate + all 98 API tests pass (15 files); Playwright browser flow (setup -> KB -> 2 entities -> multi-arg claim with subject/object entity args + literal topic) + screenshot confirmed.
- **Learnings for future iterations:**
  - See AGENTS.md "Claims (manual create/edit, US-009)" section. Multi-argument claims: a claims row + N claim_arguments rows in the same tx. Each arg is entity (entityId set) OR literal (value JSONB set), kind enforced by claim_arguments_kind_check. Don't reduce to binary edges.
  - Editing argument roles = replace the whole argument set (hard-delete + reinsert with new positions). updateClaim only touches arguments when fields.arguments is present.
  - Zod: use .superRefine on the argument schema for the entity/literal XOR rule; createClaimArgumentSchema.argumentKind has .default('entity'). value columns are z.unknown() (JSONB).
  - Web gotcha: <Claims> only refetches entities when kb.id changes, so entities just created in <Entities> won't show in the claim form's entity dropdown until a page reload (re-select KB after reload). Verified in browser via reload+reselect. If a future story needs live cross-component sync, lift entity state up.
  - US-010 (next) = delete + merge graph records safely (soft-delete deletedAt, entity merge via mergedIntoId + provenance.historical_source_entities). Reuse the outbox+audit-in-one-tx pattern with eventType 'deleted'.
---

## Sat 13 Jun 2026 22:19 +08 - US-010
Thread: https://ampcode.com/threads/T-019ec14e-0078-70ac-b158-92be1097d0d0
- Implemented safe soft-delete + Archive&Pointer merge for graph records (entities, claims). Notes/Sources have no store yet (US-011) — their delete reuses this same soft-delete + outbox + audit pattern when added.
- apps/api/src/entities/store.ts: EntityStore gains deleteEntity (soft-delete, entity.deleted outbox+audit), getEntityImpact (claims referencing an entity, for confirmations), mergeEntities (Archive & Pointer). Merge folds aliases/tags (set-union) + source name as alias, properties target-wins, soft-deletes source with merged_into_id->survivor, retargets claim_arguments source->target, appends provenance.historical_source_entities, emits entity.updated+entity.deleted+claim.updated(per claim)+entity.merged audit — all one tx. Returns discriminated MergeEntitiesResult.
- apps/api/src/entities/index.ts: DELETE /:entityId, POST /:entityId/merge (mergeEntitySchema body {targetId}), GET /:entityId/impact. Reuse local requireKbRole (viewer reads / editor mutations + requireCsrf). same_entity->400, source/target_not_found->404.
- apps/api/src/claims/store.ts + index.ts: deleteClaim (soft-delete claim + its claim_arguments, claim.deleted outbox+audit) and DELETE /:claimId.
- packages/schemas/src/graph.ts: mergeEntitySchema, entityImpactSchema/entityImpactClaimSchema (+ types).
- apps/web/src/api.ts: deleteEntity, mergeEntity, getEntityImpact, deleteClaim. apps/web/src/App.tsx: <Entities> rows get Delete + "Merge into" survivor <select> + Merge (impact-aware window.confirm); <Claims> rows get Delete. mergeTargets state per entity.
- Tests: entities.test.ts + claims.test.ts in-memory fakes extended (deleteEntity/mergeEntities/getEntityImpact/deleteClaim) + router tests (delete/merge/impact role-gating, CSRF, 404/400). entities.integration.test.ts + claims.integration.test.ts: live-DB delete/merge/impact/provenance/retarget/outbox/audit assertions (truncate now includes claims/claim_arguments).
- Verified: pnpm verify:quick green; full `DATABASE_URL=... pnpm --filter @jotmind/api test` 114/114 green (integration no longer skipped); Playwright browser flow green (setup->KB->2 entities->claim->delete claim->merge entities (survivor remains)->delete entity).
- **Learnings for future iterations:**
  - See AGENTS.md "Delete & merge (US-010)". No migration needed: entities.merged_into_id + claims.provenance JSONB already existed from US-005 schema.
  - Soft-delete is the rule for ALL canonical graph records: set deletedAt, emit <target>.deleted outbox + audit in the same tx; reads already filter deletedAt IS NULL. Deleting a parent (claim) should soft-delete its children (claim_arguments) too.
  - Merge is Archive & Pointer (NOT a compound node): soft-delete source + merged_into_id, fold props/aliases/tags into survivor, retarget claim_arguments, append provenance.historical_source_entities. Use a discriminated result type so the router maps reasons to 400/404 cleanly.
  - Delete/merge confirmation impact (AC2) is served by GET /:entityId/impact (viewer) returning affected claims; the web confirm dialog uses it. The in-memory test fake returns empty impact (no claim tracking) — impact correctness is covered by the integration test.
  - dev-browser skill still unavailable: verify UI via a TEMP playwright config (two webServers: API w/ DATABASE_URL + reuseExistingServer:true, web dev) after truncating the DB so first-run setup shows; wait for auth-loading to be hidden before interacting; delete temp files + test-results after.
  - US-011 next: add Notes/Sources stores + routers; give them the same soft-delete (deleteNote/deleteSource) so US-010 AC ("note/source deletes are soft-deletes") is fully satisfied.
---

## Sat 13 Jun 2026 23:00 +08 - US-011
Thread: https://ampcode.com/threads/T-019ec16d-7a6f-724d-8c20-195fb8fc8937
- Implemented manual capture of Notes, Sources, and Source Excerpts/Citations (no AI required).
- Shared schemas (packages/schemas/src/graph.ts): note/source/sourceExcerpt public shapes + create/update; sourceExcerptViewSchema extends the excerpt with claim:{id,predicate}|null; createSourceExcerptSchema superRefine enforces exactly-one-origin (noteId XOR sourceId) and excerpt-or-span.
- API: 3 new modules mirroring entity/claim pattern — apps/api/src/notes/, sources/, source-excerpts/ (store interface + db<Domain>Store + index.ts router + in-memory-fake unit tests). Mounted at /api/knowledge-bases/:kbId/{notes,sources,source-excerpts} (mergeParams, after KB router). createApp seams: noteStore, sourceStore, sourceExcerptStore. Role gating mirrors entities (viewer read / editor mutate+CSRF / 404 for non-members).
- Notes & Sources are canonical graph records: writes emit graph_outbox (note.*/source.*) + audit in one tx. Source Excerpts are NOT a graph target type -> audit ONLY, no outbox. Excerpt store validates origin note/source AND linked claim exist in same KB (discriminated SourceExcerptResult -> 404), and joins claim predicate for views; list filters by ?noteId/?sourceId/?claimId.
- Web: api.ts clients (list/create/update/delete for notes & sources; list/create/delete for excerpts). New <Capture> component in App.tsx (after <Claims>): note + source create forms, per-item citation forms (excerpt text + optional claim dropdown), note/source views display original content + linked claims via excerpts.
- No DB migration — notes/sources/source_excerpts tables already existed from US-005.
- Files: packages/schemas/src/graph.ts; apps/api/src/{notes,sources,source-excerpts}/*; apps/api/src/app.ts; apps/web/src/{api.ts,App.tsx}; AGENTS.md.
- Verified: pnpm verify fully green. With live compose DB: db:migrate + full apps/api test suite = 139 passed (incl. capture.integration.test.ts). Browser: temp Playwright spec (first-run setup -> KB -> claim (literal arg) -> note -> source w/ JSON metadata -> citation linked to claim); note view showed excerpt "first algorithm" -> claim "pioneered". Screenshot confirmed. Temp e2e/config removed.
- **Learnings for future iterations:**
  - See AGENTS.md "Notes, Sources & Citations (capture, US-011)" + Codebase Patterns. KEY: only entity/claim/note/source are GRAPH_TARGET_TYPES — source_excerpts/audit-only (no outbox). Add new graph-projected kinds to GRAPH_TARGET_TYPES in @jotmind/schemas graph.ts before enqueuing outbox for them.
  - The <Capture> citation dropdown (like <Claims> entity dropdown) only refetches claims when kb.id changes; cross-component live sync isn't wired. For browser tests, avoid the entity-dropdown-in-claims refresh gotcha by using a literal claim argument.
  - US-012 (next): manual search & filters over entities/claims/notes/sources.
---

## Sat 13 Jun 2026 23:15 +08 - US-012
Thread: https://ampcode.com/threads/T-019ec17e-8740-750c-b22c-9f004bcf4d71
- Implemented manual token search + structured filters that work without any AI provider (No AI mode stays useful).
- API: new apps/api/src/search/ domain (store.ts SearchStore interface + dbSearchStore; index.ts createSearchRouter) mounted at /api/knowledge-bases/:kbId/search (mergeParams, AFTER KB router). createApp seam `searchStore`. GET / requires viewer; 404 for non-members (hides existence) -> search respects KB permissions (AC3).
- Token search unions four per-kind queries (entities, claims, notes, sources), each filtering deleted_at IS NULL + knowledge_base_id, AND-matching whitespace tokens via ILIKE across the kind's text + JSONB columns (JSONB cast ::text so aliases/tags/properties/provenance/metadata contents match). Covers AC1 (entities, claims, notes, sources, aliases, tags, descriptions).
- Filters (AC2): entity type + tag (entities only; tag via jsonb @> containment), claim predicate + confidenceMin/Max + hasProvenance (claims only), dateFrom/dateTo on createdAt (all kinds), kinds CSV restriction. resolveKinds() intersects so entity-only and claim-only filters can't both produce hits. place/person are entity types -> covered by the type filter.
- Vector search (AC4): getVectorSearchAvailability() checks information_schema.tables for an `embeddings` table; it doesn't exist yet so returns {available:false, reason}. Token search is unaffected. Endpoint returns {results, vectorSearch}.
- Shared schemas: packages/schemas/src/search.ts (searchQuerySchema with coercion + hasProvenance transform + confidence-range refine; searchResult/searchResponse/vectorSearchAvailability). Exported from index.ts.
- Web: apps/web/src/api.ts search() + <Search> component in App.tsx (rendered first when a KB is selected). Filter form (query, kind checkboxes, type, predicate, tag, confidence min/max, date range, hasProvenance) -> URLSearchParams; renders unified result list + vector-unavailable notice.
- Files changed: packages/schemas/src/{search.ts,search.test.ts,index.ts}, apps/api/src/search/{store.ts,index.ts,search.test.ts,search.integration.test.ts}, apps/api/src/app.ts, apps/web/src/{api.ts,App.tsx}, AGENTS.md, prd.json, progress.txt.
- Tests: 7 search API unit (in-memory store: auth/role gating, filter parsing, vector-unavailable) + 6 search integration (skipIf no DATABASE_URL: cross-kind token search, alias/tag match, KB-scoping + soft-delete exclusion, type/tag/predicate/confidence/provenance filters, vector unavailable) + 6 schema unit.
- Verified: pnpm verify fully green; live compose DB integration tests pass (152 passed with DATABASE_URL); Playwright+Chromium browser flow (setup -> create KB -> create entity Ada Lovelace -> search "ada" -> [entity] result + "Semantic (vector) search unavailable" notice) + screenshot confirmed; temp playwright config/spec/screenshot cleaned up.
- **Learnings for future iterations:**
  - Search is read-only + KB-scoped: mirror the entity/claim nested-router pattern (Router mergeParams, mounted AFTER KB router, local requireKbRole('viewer'), 404 for non-members). createApp seam added: searchStore.
  - drizzle: passing table COLUMNS into a helper that builds sql conditions needs the param typed as `SQLWrapper` (or `AnyColumn|SQL`), NOT `SQL` (PgColumn is not an SQL). `sql\`${col}::text ILIKE ${pattern}\`` works for both text and JSONB columns; cast ::text to search JSONB array/object contents. Escape LIKE wildcards (\ % _) in user tokens.
  - db.execute<T>(sql) returns the rows array directly (postgres-js driver); use information_schema.tables / to_regclass to feature-detect tables (used for the AC4 embeddings check).
  - searchQuerySchema reads from req.query (all strings): use z.coerce.number for numerics and a union+transform for the boolean hasProvenance ('true'/'false'->boolean). CSV params (kinds) parsed in the router, not the schema.
  - Future vector/embeddings story: create an `embeddings` table and flip getVectorSearchAvailability to return available:true; then add the semantic-search branch alongside the existing token search (don't replace it).

## Sat 13 Jun 2026 23:25 +08 - US-013
Thread: https://ampcode.com/threads/T-019ec18a-37ee-7313-81d8-dd1bbc641a06
- Implemented multiple graph views over the same Knowledge Base (Network, Timeline, Table, Detail, Sources & Notes) with click-navigation. Frontend-only: all data comes from existing list endpoints (listEntities/listClaims/listNotes/listSources/listSourceExcerpts) — no new API or schema, no migration.
- New file apps/web/src/GraphViews.tsx exporting <GraphViews kb csrfToken>; imported into <KnowledgeBases> in App.tsx and rendered first when a KB is selected (before <Search>). Kept it in its own file because App.tsx is already ~1.7k lines.
- One shared `selected` Selection ({kind,id}) drives all views so clicking anything navigates to the same detail/source context. Tablist switches 5 views:
  - Network: dependency-free SVG circular layout (entities on a circle, claim entity-arguments drawn as predicate-labeled edges). Nodes are clickable -> entity detail.
  - Timeline: chronological cards from claims (validStart??createdAt), notes (createdAt), and Event-typed entities; newest first; cards clickable.
  - Table: entities/claims toggle, sortable column-header buttons, text filter, tag filter (clicking a tag anywhere filters the entity table).
  - Detail: selected entity/claim -> related claims + sources/citations + inline-editable entity metadata (description/tags) via updateEntity; related items + arg entities + tags + citations all navigate.
  - Sources & Notes: original note/source content (blockquote) + linked claims via excerpts; highlights the selected note/source.
- Files changed: apps/web/src/GraphViews.tsx (new), apps/web/src/App.tsx (import + render), apps/web/src/App.test.tsx (new graph-views nav test + `within` import), AGENTS.md (new "Graph views (US-013)" section), prd.json, progress.txt.
- Verified: pnpm verify:quick fully green (8 web tests incl new "renders graph views and navigates from the table to entity detail"; schemas 33; typecheck+lint+format clean). Full API suite 152/152 against live compose DB (DATABASE_URL=...). Browser: temp Playwright+Chromium spec (truncate users -> first-run setup -> create+select KB -> add 2 entities -> reload+reselect -> Network node visible -> Timeline -> Table row click -> entity Detail w/ "Related claims" + "Sources & citations" -> Sources & Notes view) passed; screenshot confirmed. Temp pw.tmp.config.ts/e2e-tmp/test-results removed.
- **Learnings for future iterations:**
  - See AGENTS.md "Graph views (US-013)". KEY: <GraphViews> fetches its data once per kb.id (same cross-component sync gotcha as <Claims>/<Capture>) — records created in sibling sections need a reload+reselect to appear. Browser tests must reload+reselect after creating records.
  - Testid collision: <Capture> and <GraphViews> both use `sources-empty`. Scope queries to getByTestId('graph-views') (or a table row) when a testid isn't unique page-wide. View tabs: view-tab-{network,timeline,table,sources}; nodes/rows: network-node-<id>, table-row-<id>.
  - Network view avoids a heavy graph-layout dependency by using a simple SVG circular layout; fine for small KBs. If a future story needs large/interactive graphs, consider a real layout lib then (US-026 AGE traversal may inform this).
  - Large web components belong in their own files now that App.tsx is ~1.7k lines; import into App.tsx. Don't keep growing App.tsx.
---

## Sat 13 Jun 2026 23:32 +08 - US-014
Thread: https://ampcode.com/threads/T-019ec194-ab8d-77cf-ba1b-84f01e353091
- Enforced/consolidated audit + permissions for manual graph flows. Most of US-014 was already satisfied by US-004-013: every graph mutation (entity/claim/note/source create/update/delete, entity merge, KB role assignment) writes an audit_events row with actorUserId in the SAME tx as the canonical write; viewers are read-only (mutations gated editor+requireCsrf, reads gated viewer); KB audit summary endpoint (GET /:id/audit, admin+) already existed.
- Gap closed (AC4 "admins/owners can inspect ... job status"): added KB-scoped job status endpoint GET /api/knowledge-bases/:id/jobs (admin+). The KB router now takes an injectable jobStore and reuses jobStore.list({knowledgeBaseId}) + toPublicJob from jobs/index.ts. createApp passes options.jobStore into createKnowledgeBaseRouter.
- Web: new apps/web/src/KbAdmin.tsx (own file — App.tsx is huge) rendered in <KnowledgeBases> after <Capture>, gated by kbRoleSatisfies(kb.role,'admin') (returns null for non-admins). Shows audit summary + job status. Added listKbAudit/listKbJobs to apps/web/src/api.ts.
- Files changed: apps/api/src/kb/index.ts (jobStore seam + /:id/jobs route), apps/api/src/app.ts (pass jobStore to KB router), apps/api/src/kb/kb.test.ts (in-memory JobStore fake + 3 tests: admin 200 scoped, viewer 403, non-member 404), apps/web/src/api.ts, apps/web/src/KbAdmin.tsx (new), apps/web/src/App.tsx, AGENTS.md, prd.json, progress.txt.
- Verified: pnpm verify fully green (typecheck/lint/format/test + e2e). KB jobs endpoint unit tests pass (kb.test.ts 12 tests). Ran kb integration test against live compose DB (passed). Browser-verified the admin panel via a TEMP Playwright config/spec (API w/ DATABASE_URL + web dev, reuseExistingServer): first-run setup -> create KB -> select -> assert <kb-admin> shows "knowledge_base.created" audit event + "No background jobs" job section; screenshot confirmed; temp files deleted.
- **Learnings for future iterations:**
  - When reusing the KB router's ownerAgent across createApp instances in tests, the agent is bound to ONE app instance — don't rebuild the app after the agent is created with different store injections, or the agent will hit the old app. Use a mutable array the in-memory store reads lazily (store.list reads jobRows[] at call time) so you can seed data after KB creation while keeping the SAME app/agent.
  - The KB select button testid is `kb-select-<uuid>` (the KB id, NOT the name); in Playwright select by role+name text inside getByTestId('kb-list') instead.
  - System-wide job API (/api/jobs) stays system-admin only; the KB-scoped /:id/jobs is the per-KB view for KB admins/owners. Later stories adding settings should add a viewer-blocked settings mutation + audit row to fully satisfy AC1's "setting changes" (settings don't exist yet — US-016).
---
