{
  "project": "JotMind",
  "branchName": "ralph/jotmind-graph-note-redo",
  "description": "JotMind Graph-Note Redo - Privacy-first, self-hostable graph-based personal knowledge app with notes, sources, typed entities, provenance-aware claims, custom schemas, AI proposal review, and restricted Datalog reasoning.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Create the monorepo foundation",
      "description": "As a developer, I need a typed monorepo foundation so autonomous agents can add features consistently.",
      "acceptanceCriteria": [
        "Monorepo contains `apps/web`, `apps/api`, and shared package boundaries for schemas/types/utilities.",
        "`pnpm` package scripts include `format`, `format:check`, `typecheck`, `lint`, `test`, `test:api`, `test:e2e`, `verify:quick`, and `verify`.",
        "React + Vite web app and Express API app boot locally.",
        "Shared Zod schema package can be imported by both web and API.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 1,
      "passes": true,
      "notes": "pnpm workspace monorepo: apps/web (React+Vite 5), apps/api (Express), packages/schemas (shared Zod). All scripts present; pnpm verify green (format:check, typecheck, lint, test, test:api, test:e2e). Pinned to Node 18-compatible tooling."
    },
    {
      "id": "US-002",
      "title": "Provision PostgreSQL with required extensions",
      "description": "As an operator, I need reproducible database setup so the app has a reliable canonical store.",
      "acceptanceCriteria": [
        "Docker Compose or equivalent starts PostgreSQL with Apache AGE and `pgvector` available.",
        "Drizzle migrations can create the initial schema in a clean test database.",
        "API health check reports database connectivity and required extension availability.",
        "Docs warn that browser storage is non-canonical and database volume/backups preserve data.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 2,
      "passes": true,
      "notes": "Reference PG 18 image (infra/db/Dockerfile: apache/age + compiled pgvector v0.8.1) with AGE 1.7.0 + pgvector 0.8.1; docker-compose.yml service `db` (volume at /var/lib/postgresql for PG18, init script creates extensions, localhost-bound). Drizzle ORM in apps/api (src/db/schema.ts app_meta, drizzle/ migrations, db:generate/db:migrate scripts, migrate runner creates extensions then migrates). /api/health now reports database connectivity + age/vector availability via checkDatabaseHealth (graceful configured:false when no DATABASE_URL -> stays ok). Env-gated integration test verifies migrations + extensions against live DB (skips without DATABASE_URL). Docs: README DB section + non-canonical browser-storage/backup warning, .env.example. pnpm verify green; integration tests + live /api/health verified against compose DB."
    },
    {
      "id": "US-003",
      "title": "Implement local/server accounts and sessions",
      "description": "As an operator, I need secure local/server user accounts so Knowledge Bases can have authenticated access control without mandatory cloud accounts.",
      "acceptanceCriteria": [
        "First-run setup creates an initial admin user.",
        "Passwords are hashed with Argon2id.",
        "Browser auth uses server-side PostgreSQL-backed sessions with secure HTTP-only cookies.",
        "Cookie-authenticated mutations require CSRF protection.",
        "Account creation is admin-controlled after first-run setup.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 3,
      "passes": true,
      "notes": "Local/server accounts + PG-backed sessions. apps/api/src/auth/: password.ts (Argon2id via @node-rs/argon2; Algorithm const enum referenced numerically due to verbatimModuleSyntax), store.ts (injectable AuthStore interface + dbAuthStore), index.ts (createAuthRouter: /setup-status, /setup first-run admin (409 once any user exists), /login, /me, /logout, /users admin-only). Sessions table (token in HTTP-only jm_session cookie, Secure in prod/COOKIE_SECURE) + users table (UUID pk, email unique, role admin|member). CSRF = synchronizer token (jm_csrf readable cookie + x-csrf-token header compared constant-time to session.csrfToken) on all cookie-auth mutations. cookieParser mounted in createApp; createApp({authStore}) injection seam mirrors {checkDatabase}. Web: src/api.ts typed client (credentials:include + csrf header) + App.tsx setup/login/authed+admin account-creator UI. Shared Zod schemas in packages/schemas/src/auth.ts. Tests: 15 unit (in-memory store) + 1 DB integration (skipIf no DATABASE_URL). Migration drizzle/0001. Verified: pnpm verify green; live DB migrate + integration tests pass; full HTTP flow via curl cookie jar (setup/login/me/csrf-403/admin-create/member-403/logout); Playwright browser login flow + screenshot confirmed (dev-browser skill unavailable in env, used Playwright+Chromium instead)."
    },
    {
      "id": "US-004",
      "title": "Create Knowledge Base scope and roles",
      "description": "As a user, I want selectable Knowledge Bases with role-based permissions so different graph scopes can be managed safely.",
      "acceptanceCriteria": [
        "Data model uses `knowledge_base_id` for scoped records.",
        "Users can create/select a Knowledge Base in minimal UI or API.",
        "Roles exist: owner, admin, editor, viewer.",
        "Backend middleware enforces role checks on Knowledge Base routes.",
        "Audit events record role assignments and Knowledge Base creation.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 4,
      "passes": true,
      "notes": "Knowledge Base scope + KB roles. DB (apps/api/src/db/schema.ts): knowledge_bases (uuid pk, name, description, created_by FK users), knowledge_base_members (composite PK (kb_id,user_id), role text), audit_events (nullable kb_id/actor_user_id, action, target_type/id, JSONB metadata). Migration drizzle/0002. Shared schemas packages/schemas/src/knowledge-base.ts: KB_ROLES owner>admin>editor>viewer, kbRoleSatisfies/KB_ROLE_RANK, create/list/member/assign/audit Zod schemas (separate from system account role admin|member). Routes apps/api/src/kb/index.ts createKnowledgeBaseRouter({store,authStore}) at /api/knowledge-bases: GET / (list own), POST / (create->creator owner, CSRF), GET /:id (viewer+), GET+POST /:id/members (admin+, CSRF on POST), GET /:id/audit (admin+). requireKbRole(min) middleware loads membership for :id, 404 for non-members (hides existence) + 403 insufficient role. Reuses exported auth middleware requireAuth/requireCsrf/asyncHandler. dbKnowledgeBaseStore writes audit + membership in same tx as canonical write (creation records knowledge_base.created + role_assigned owner; assignRole upserts + records role_assigned). createApp seams now {checkDatabase,authStore,kbStore}. Web: api.ts listKnowledgeBases/createKnowledgeBase + App.tsx KnowledgeBases section (list w/ role, select, create form). Tests: 9 kb unit (in-memory store) + 1 kb integration (skipIf no DATABASE_URL) + 3 schema unit + web list test. Added apps/api/vitest.config.ts fileParallelism:false (shared-DB integration files TRUNCATE same tables). Verified: pnpm verify green; live DB migrate+integration pass; Playwright browser flow (setup->create KB->owner+selected) + screenshot confirmed (dev-browser unavailable, used Playwright+Chromium)."
    },
    {
      "id": "US-005",
      "title": "Create canonical graph schema tables",
      "description": "As a developer, I need canonical relational tables for graph records so later UI, AI, and reasoning features share one consistent data model.",
      "acceptanceCriteria": [
        "Tables exist for entities, claims, claim arguments, notes, sources, source excerpts/citations, schema definitions/versions, rule definitions, proposals, audit events, jobs, and `graph_outbox`.",
        "Canonical records include stable UUIDv7 identifiers, `knowledge_base_id`, timestamps, soft-delete fields, and audit metadata where appropriate.",
        "Custom properties are stored in JSONB with validation hooks.",
        "Entities/claims reference schema versions used for validation.",
        "Notes and Sources are canonical records, not merely entity rows.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 5,
      "passes": true,
      "notes": "Canonical graph schema tables added in apps/api/src/db/schema.ts (migration drizzle/0003_massive_kronos.sql): schema_definitions, schema_versions, entities, claims, claim_arguments, notes, sources, source_excerpts, rule_definitions, proposals, jobs, graph_outbox (audit_events already existed from US-004). All canonical graph records use PG18-native UUIDv7 PKs (uuidv7() default), knowledge_base_id FK, created_at/updated_at, nullable deleted_at soft-delete, created_by; JSONB properties/metadata/provenance/spec default '{}'/'[]'. Entities/claims carry nullable schema_version_id (validated-against version; nullable so US-008/009 can precede custom schemas US-027) + conceptual string type/predicate for cross-version search. Claims are multi-argument via claim_arguments (role-labeled, entity ref OR literal value, check-constrained) — not binary edges. Notes & Sources are first-class canonical records, not entity rows. DB check constraints (confidence 0..1, valid-time order, span order, excerpt single-origin, entity not-self-merge, status enums) + partial unique indexes (deleted_at IS NULL) for recreatable schema defs/rules + helpful indexes. graph_outbox has projection-state (status/attempts/last_error/run_after) for US-006; jobs has status/attempts/max_attempts/run_after/failure_reason/owner_user_id for US-007. Custom-property validation HOOK validateCustomProperties(propertySchema, properties) + built-in entity types + status enums in packages/schemas/src/graph.ts (8 unit tests). New graph-schema.integration.test.ts (skipIf no DATABASE_URL) verifies all 12 tables exist, inserts entities/multi-arg claim/notes/sources/outbox with UUIDv7 ids, and confirms the confidence check constraint rejects out-of-range values. Verified: pnpm verify fully green; live compose DB db:migrate applied 0003, all integration tests pass, \\dt confirms all tables."
    },
    {
      "id": "US-006",
      "title": "Add graph outbox and projection seam",
      "description": "As a developer, I need graph writes and projection seams established early so AGE can remain derived without rewriting canonical write paths later.",
      "acceptanceCriteria": [
        "Entity/claim/note/source writes create `graph_outbox` events in the same transaction as canonical relational writes.",
        "Projector interface can consume outbox events and mark projection state.",
        "Stub projector is allowed but must be visible in logs/status as stubbed.",
        "Rebuild/repair interface exists to regenerate projection from relational tables.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 6,
      "passes": true,
      "notes": "Graph outbox + projection seam. apps/api/src/graph/: outbox.ts (enqueueGraphOutbox(tx,events) helper + outboxEventType -> \"<target>.<event>\"; DbExecutor type accepts tx so outbox insert is in the SAME transaction as the canonical write), store.ts (GraphWriteStore interface + dbGraphWriteStore: createEntity/createNote/createSource/createClaim each write canonical row(s) + graph_outbox event in one tx — the seam US-008/009/011 build on; claims also write claim_arguments in-tx), projector.ts (Projector interface {name,stubbed,project}; stubProjector logs + stubbed:true, no AGE writes yet; ProjectionStore interface + dbProjectionStore; processOutbox drains pending events ordered by (created_at,id) FOR UPDATE SKIP LOCKED, marks processed/failed+attempts+last_error; rebuildProjection repairs by reprojecting all non-deleted canonical records straight from relational tables bypassing outbox; getProjectionStatus -> {projector{name,stubbed},counts{pending,processed,failed}}), index.ts (createGraphRouter mounted /api/graph: GET /projection/status authed, POST /projection/process + /projection/rebuild admin+CSRF). Stubbed state visible: createApp logs stub notice once/process (module flag, avoids test flood) + status endpoint exposes stubbed:true. requireAdmin now exported from auth/index.ts. createApp seams now {checkDatabase,authStore,kbStore,projectionStore,projector}. Shared schemas packages/schemas/src/graph.ts: GRAPH_TARGET_TYPES/GRAPH_EVENT_TYPES + graphProjectionStatusSchema. Tests: 11 graph unit (in-memory store/projector: outbox helper, processOutbox success/fail/no-reprocess, rebuild, status, API auth/admin/csrf/drain) + 5 graph integration (skipIf no DATABASE_URL: same-tx outbox write for all 4 kinds, tx rollback leaves no outbox/claim on check-constraint failure, processOutbox with stub, rebuild count, soft-deleted excluded from rebuild). Verified: pnpm verify fully green; live compose DB db:migrate + all 52 api tests (incl 5 graph integration) pass."
    },
    {
      "id": "US-007",
      "title": "Add PostgreSQL-backed jobs foundation",
      "description": "As an operator, I need durable jobs so projection, AI, embeddings, imports, and exports do not depend on process-local state.",
      "acceptanceCriteria": [
        "PostgreSQL-backed job table supports status, attempts, retry-after, failure reason, owner user, and `knowledge_base_id` when applicable.",
        "Worker can run in-process and as a separate process mode.",
        "Failed jobs retry with capped exponential backoff and then move to failed/dead-letter state.",
        "Basic admin/status API exposes job status with permission checks.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 7,
      "passes": true,
      "notes": "Durable PostgreSQL-backed jobs foundation. jobs table already existed (US-005); added apps/api/src/jobs/: store.ts (JobStore interface + dbJobStore; claimNext = FOR UPDATE SKIP LOCKED inside tx then status=running + attempts++ so multiple workers never double-claim; enqueue/markSucceeded/markForRetry/markFailed/getById/list/countByStatus), worker.ts (computeBackoffMs capped exponential base 1s cap 5min; runNextJob runs handler for job.type, success->markSucceeded, throw->markForRetry with runAfter=now+backoff until attempts>=maxAttempts then markFailed dead-letter, missing handler->immediate dead-letter; processAvailableJobs drain; runWorkerLoop + startInProcessWorker for the two run modes), handlers.ts (defaultJobHandlers registry incl noop), worker-process.ts (separate-process entry, graceful SIGINT/SIGTERM, WORKER_POLL_INTERVAL_MS). index.ts createJobsRouter at /api/jobs: GET / (list, ?status/?knowledgeBaseId/?limit), GET /stats (per-status counts), GET /:id (404) -- all system admin only (read-only). Run modes: pnpm --filter @jotmind/api worker (separate process) + WORKER_INLINE=true starts startInProcessWorker in server.ts. Shared schemas packages/schemas/src/jobs.ts (publicJob, jobListQuery, jobStats; reuses jobStatusSchema from graph.ts). createApp seam now includes jobStore. Tests: jobs.test.ts (backoff math, runNextJob success/no-handler/retry-then-deadletter with injected clock, processAvailableJobs summary, admin API auth/403/list+filter/400/stats/get+404) + jobs.integration.test.ts (skipIf no DATABASE_URL, TRUNCATEs jobs/knowledge_bases/users). Verified: pnpm verify fully green; live compose DB db:migrate + 69 tests pass (incl integration); separate-process worker smoke-tested end-to-end (claimed+ran noop job -> succeeded with result)."
    },
    {
      "id": "US-008",
      "title": "Manually create and edit entities",
      "description": "As an editor, I want to create and edit typed entities so I can maintain my graph without relying on AI.",
      "acceptanceCriteria": [
        "Editors can create built-in entity types at minimum: Person, Event, Concept, Place, and Module-specific Character where relevant.",
        "Editors can edit name, aliases, description, tags/groups, and JSONB custom properties.",
        "Forms validate required fields and schema constraints before save.",
        "Viewers can read entities but cannot mutate them.",
        "Entity writes create audit events and graph outbox events.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 8,
      "passes": true,
      "notes": "Entities CRUD under /api/knowledge-bases/:kbId/entities (Router mergeParams, mounted after KB router). apps/api/src/entities/{store.ts(EntityStore+dbEntityStore),index.ts(createEntityRouter)}. create/updateEntity write canonical entities row + enqueueGraphOutbox + audit_events row in ONE tx (entity.created/updated). Role gating via kbStore.getRole: reads need viewer, mutations need editor + requireCsrf, 404 for non-members. Built-in types via BUILTIN_ENTITY_TYPES. Shared Zod: entitySchema/entityListSchema/createEntitySchema/updateEntitySchema in @jotmind/schemas graph.ts. Web: <Entities> panel in App.tsx (shown when KB selected; editor+ form vs read-only notice; comma aliases/tags + JSON props). createApp seam entityStore. Verified: verify:quick + test:api green; 4 integration tests pass vs live DB (audit+outbox confirmed); Playwright browser flow (setup->KB->create+edit Person) passed w/ screenshot."
    },
    {
      "id": "US-009",
      "title": "Manually create and edit claims",
      "description": "As an editor, I want to create and edit claims so relationships and observations are represented as provenance-aware graph facts.",
      "acceptanceCriteria": [
        "Editors can create claims with predicate, description, confidence, optional valid time range, and one or more role-labeled Claim Arguments.",
        "Claims support multi-argument relations and are not reduced to binary edges in the canonical model.",
        "Editors can edit claim metadata and argument roles after creation.",
        "Claim forms validate predicate and argument requirements before saving.",
        "Claim writes create audit events and graph outbox events.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 9,
      "passes": true,
      "notes": "Manual claim create/edit. apps/api/src/claims/: store.ts (ClaimStore + dbClaimStore, returns ClaimWithArguments=ClaimRow+arguments[]) and index.ts (createClaimRouter) mounted at /api/knowledge-bases/:kbId/claims (mergeParams, AFTER KB router). createApp seam claimStore. Claims are multi-argument (NOT binary edges): claim row carries predicate + optional description/confidence/validStart/validEnd/properties; role-labeled args in separate claim_arguments table (entity ref OR literal value, kind check-constrained, sequential position). createClaim inserts claim+args+graph_outbox(claim.created)+audit_events in ONE tx; updateClaim updates metadata and, when arguments present, REPLACES the full set (hard-delete+reinsert) so roles/order are editable; emits claim.updated. Reads filter deleted_at IS NULL, scope by KB. Role gating mirrors entity router: reads viewer+, mutations editor+ + requireCsrf, 404 for non-members. Shared Zod in @jotmind/schemas/graph.ts: claimSchema/claimListSchema, createClaimArgumentSchema (superRefine entity-needs-entityId / literal-needs-value, argumentKind defaults entity), createClaimSchema (predicate + >=1 arg, validStart<=validEnd refine), updateClaimSchema (all-optional, >=1 field, optional args-replace >=1). Web: api.ts listClaims/createClaim/updateClaim + <Claims> component in App.tsx (after <Entities>, fetches claims+entities for entity-arg dropdowns, dynamic add/remove argument rows, editor-only form). Tests: 11 claims router unit (in-memory ClaimStore fake records audit+outbox) + 4 claims integration (skipIf no DATABASE_URL, verifies multi-arg create, metadata+args replace update, confidence check-constraint rejection, list w/ args) + 10 schema unit. Verified: pnpm verify fully green; live compose DB db:migrate + all 98 API tests pass (15 files); Playwright browser flow (setup->KB->2 entities->multi-arg claim subject/object/literal topic) + screenshot confirmed (dev-browser unavailable, used Playwright+Chromium, temp files cleaned up)."
    },
    {
      "id": "US-010",
      "title": "Delete and merge graph records safely",
      "description": "As an editor, I want safe delete and merge flows so I can correct graph mistakes without corrupting provenance.",
      "acceptanceCriteria": [
        "Entity/claim/note/source deletes are soft-deletes by default.",
        "Delete confirmation explains affected claims or linked records.",
        "Entity merge preserves aliases, properties, notes, sources, citations, related claims, audit events, and provenance.",
        "Entity merge uses Archive & Pointer behavior: the archived entity is soft-deleted, `merged_into_id` points to the surviving entity, and no nested compound graph node is created.",
        "Claims/edges that referenced the archived entity are retargeted to the surviving entity and append `provenance.historical_source_entities` with the archived entity ID.",
        "Merge creates graph outbox events and an audit event.",
        "Viewers cannot delete or merge.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 10,
      "passes": true,
      "notes": "Soft-delete + Archive&Pointer merge for entities/claims. Deletes (dbEntityStore.deleteEntity / dbClaimStore.deleteClaim) set deletedAt, emit <target>.deleted outbox + audit in one tx (claim delete also soft-deletes claim_arguments). Entity merge (dbEntityStore.mergeEntities): source soft-deleted with merged_into_id->survivor (no compound node), aliases/tags set-unioned + source name kept as alias, properties target-wins; claim_arguments retargeted source->target with provenance.historical_source_entities appended; emits entity.updated+entity.deleted+claim.updated per claim + entity.merged audit. Routes: DELETE/POST :id/merge (editor+CSRF), GET :id/impact (viewer) for confirmations; claim DELETE (editor+CSRF). Viewers 403, non-members 404. Web App.tsx: Delete/Merge buttons + impact-aware window.confirm. No migration (merged_into_id/provenance from US-005). pnpm verify:quick green; full API+integration tests green vs live DB; Playwright browser flow verified (setup->KB->entities->claim->delete claim->merge->delete entity)."
    },
    {
      "id": "US-011",
      "title": "Capture Notes and Sources manually",
      "description": "As a user, I want to store fragmented text before extraction so original material remains available and citable.",
      "acceptanceCriteria": [
        "Users can create Notes from freeform text.",
        "Users can create Sources for imported/captured text or structured material metadata.",
        "Source Excerpts/Citations can reference spans or excerpts in Notes/Sources.",
        "Source/note views display original content and linked extracted/manual claims.",
        "No AI provider is required for capture.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 11,
      "passes": true,
      "notes": "Notes/Sources/Source-Excerpts capture. 3 store+router modules (apps/api/src/{notes,sources,source-excerpts}/) mirror the entity/claim pattern, mounted at /api/knowledge-bases/:kbId/{notes,sources,source-excerpts}; createApp seams noteStore/sourceStore/sourceExcerptStore. Notes & Sources are canonical graph records (outbox+audit in one tx); Source Excerpts are NOT a projection target (audit only), reference exactly one origin (note XOR source) + optional claim, validated same-KB. Excerpt list returns SourceExcerptWithClaim (claim:{id,predicate}) and filters by ?noteId/?sourceId/?claimId for note/source views (AC4). Shared Zod shapes in @jotmind/schemas graph.ts. Web <Capture> component (App.tsx) after <Claims>: note/source create forms + per-item citation forms linking to claims, shows original content + linked claims. No DB migration (tables existed from US-005). pnpm verify green (139 API tests incl. DB integration); browser-verified via temp Playwright spec + screenshot."
    },
    {
      "id": "US-012",
      "title": "Provide manual search and filters",
      "description": "As a user, I want search and filters to work without AI so the app remains useful in `No AI` mode.",
      "acceptanceCriteria": [
        "Full-text token search covers entities, claims, notes, sources, aliases, tags, and descriptions.",
        "Graph filters support entity type, claim predicate, date range, place, person, tag, confidence, and provenance where data exists.",
        "Search respects Knowledge Base permissions.",
        "If embeddings do not exist, vector search is omitted or shown as unavailable without breaking search.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 12,
      "passes": true,
      "notes": "Manual token search + structured filters, no AI required. apps/api/src/search/: store.ts (SearchStore interface + dbSearchStore) + index.ts (createSearchRouter). Mounted at /api/knowledge-bases/:kbId/search (mergeParams, AFTER KB router); createApp seam searchStore. GET / requires viewer (respects KB perms, 404 for non-members). Token search unions per-kind queries over entities/claims/notes/sources (filtering deleted_at IS NULL + kb), AND-matching whitespace tokens via ILIKE across name/aliases/description/tags/type/properties (entities), predicate/description/properties/provenance (claims), title/content/properties (notes), title/sourceType/uri/content/metadata/properties (sources); JSONB cast ::text so array/object contents match. Filters: type/tag (entities only; tag via jsonb @> containment), predicate/confidenceMin/confidenceMax/hasProvenance (claims only), dateFrom/dateTo (createdAt all kinds), kinds CSV. Results merged, sorted createdAt desc, capped limit (default 50). Vector search availability (AC4): getVectorSearchAvailability() checks information_schema for embeddings table; none exists so returns available:false + reason, token search unaffected. Shared schemas packages/schemas/src/search.ts: searchQuerySchema (coerces numeric/boolean params, hasProvenance transform, confidence-range refine), searchResult/searchResponse schemas. Web: api.ts search() + <Search> component in App.tsx (rendered first per selected KB; filter form + unified results + vector-unavailable notice). Tests: 7 search API unit (in-memory store) + 6 search integration (skipIf no DATABASE_URL, verifies cross-kind token search, alias/tag match, KB-scoping, soft-delete exclusion, type/tag/predicate/confidence/provenance filters, vector unavailable) + 6 schema unit. Verified: pnpm verify fully green; live DB integration tests pass; Playwright+Chromium browser flow (setup->create KB->create entity->search ada->result + vector-unavailable notice) + screenshot confirmed."
    },
    {
      "id": "US-013",
      "title": "Build basic graph views",
      "description": "As a user, I want multiple views over the same graph so I can browse relationships, chronology, tables, details, and original sources.",
      "acceptanceCriteria": [
        "Network view displays entities and claim-derived relationships for a selected Knowledge Base/Module/filter.",
        "Timeline/card view displays dated notes, claims, and events chronologically when dates exist.",
        "Table view displays entities or claims with sortable/filterable columns.",
        "Detail view displays a selected entity or claim with related claims, sources, citations, and editable metadata.",
        "Source/note view displays original captured/imported material and linked claims.",
        "Clicking entities, claims, tags, or citations navigates to relevant detail/source context.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 13,
      "passes": true,
      "notes": "Multiple graph views over the same KB (frontend-only; reuses existing list endpoints, no new API/schema). New apps/web/src/GraphViews.tsx <GraphViews> imported into <KnowledgeBases> (rendered first when a KB is selected). Five views via a tablist sharing ONE selection state so all navigate to the same detail/source context: Network (dependency-free SVG circular layout, entities as nodes + claim entity-arguments as predicate-labeled edges), Timeline (claims by validStart??createdAt, notes by createdAt, Event-typed entities; newest first), Table (entities/claims toggle, sortable column-header buttons, text filter, tag filter), Detail (selected entity/claim -> related claims + citations + inline-editable entity metadata via updateEntity), Sources & Notes (original note/source content + linked claims from excerpts). Clicking entities/claims/tags/citations navigates (navigate/navigateTag). Gotcha: GraphViews fetches once per kb.id (cross-component sync) -> reload+reselect to see records created in sibling sections; reuses sources-empty testid (scope queries to graph-views). Verified: pnpm verify:quick green (8 web tests incl new graph-views nav test); full API suite 152/152 against live compose DB; Playwright+Chromium browser flow (setup -> KB -> 2 entities -> reload+reselect -> Network node visible -> Timeline -> Table row -> entity Detail w/ Related claims + Sources & citations -> Sources & Notes view) + screenshot confirmed; temp pw config/spec removed."
    },
    {
      "id": "US-014",
      "title": "Enforce audit and permissions in manual graph flows",
      "description": "As a Knowledge Base owner/admin, I want graph mutations audited and permissioned so shared self-hosted use is trustworthy.",
      "acceptanceCriteria": [
        "Entity, claim, note, source, merge, delete, role, and setting changes create Audit Events with authenticated user IDs.",
        "Viewers can browse/search but cannot mutate content, accept proposals, run imports, or manage settings.",
        "Editors can create/edit/merge/soft-delete graph content within their Knowledge Base.",
        "Admins/owners can inspect Knowledge Base audit summaries and job status.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 14,
      "passes": true,
      "notes": "Consolidation/enforcement story: audit + role gating were already in place per-domain (US-004-013). Verified all graph mutations (entity/claim/note/source create/update/delete, entity merge, KB role assignment) write audit_events with actorUserId in the same tx; viewers are read-only (mutations require editor+CSRF, reads require viewer). Added KB-scoped job status endpoint GET /api/knowledge-bases/:id/jobs (admin+, injects jobStore, reuses jobStore.list+toPublicJob) so KB admins/owners can inspect their KB jobs without system-admin. Web: apps/web/src/KbAdmin.tsx panel (audit summary + job status, gated by kbRoleSatisfies(kb.role,admin)) + listKbAudit/listKbJobs in api.ts. pnpm verify green; KB jobs endpoint unit-tested (admin 200 scoped, viewer 403, non-member 404); browser-verified the admin panel against live DB via Playwright (screenshot)."
    },
    {
      "id": "US-015",
      "title": "Configure AI provider adapter layer",
      "description": "As a developer, I need all AI integrations behind adapters so UI and graph repositories never call vendor SDKs directly.",
      "acceptanceCriteria": [
        "LLM and embedding providers share typed adapter interfaces.",
        "Local inference supports Ollama-native HTTP APIs and OpenAI-compatible local HTTP endpoints and works in V1 without remote credentials.",
        "Remote LLM support includes OpenAI and Anthropic behind adapters; V1 may ship these as configured-but-untested skeletons if policy/config validation is complete.",
        "Provider config schemas use Zod and separate secrets from portable Knowledge Base exports.",
        "Normal tests use deterministic mock LLM/embedding providers.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 15,
      "passes": true,
      "notes": "AI provider adapter layer. Shared Zod config + message shapes in packages/schemas/src/ai.ts: AI_PROVIDER_KINDS (ollama, openai-compatible, openai, anthropic, mock); chatMessage/llmCompletionRequest+Result/embeddingRequest+Result; provider config as a discriminatedUnion(kind) with a *portable* schema + a *full* schema extending secret fields (AI_PROVIDER_SECRET_FIELDS=[apiKey]). toPortableProviderConfig() strips secrets AND re-parses portable schema (drops unknown keys so secrets cannot round-trip into KB exports, AC4). Adapter layer apps/api/src/ai/: types.ts (LlmProvider/EmbeddingProvider/AiProvider interfaces + AiProviderError), mock.ts (deterministic MockLlmProvider echo + MockEmbeddingProvider FNV-1a fixed-dim vectors, AC5), ollama.ts (OllamaProvider /api/chat + /api/embeddings, no creds, AC2), openai-compatible.ts (OpenAiCompatibleProvider /chat/completions + /embeddings, optional Bearer key; reused for both local endpoints and remote openai, AC2/AC3), anthropic.ts (AnthropicProvider /v1/messages, LLM-only skeleton, AC3), http.ts (postJson via global fetch -> AiProviderError), factory.ts (createLlmProvider/createEmbeddingProvider switch on kind; anthropic embedding throws; parseProviderConfig validates). No DB/migration/router/UI (foundation only; consumed by US-016/017/018/021). Tests: 9 schema unit (config validation, required-key for openai/anthropic, secret separation) + 8 api unit (mock determinism, factory per-kind, fetch-mocked ollama/openai-compatible incl Bearer header + error mapping). Verified: pnpm verify fully green (typecheck/lint/format/test/test:api/e2e)."
    },
    {
      "id": "US-016",
      "title": "Implement layered AI privacy settings",
      "description": "As a privacy-conscious user/admin, I want layered AI policy so private Knowledge Base data is not sent externally without explicit permission.",
      "acceptanceCriteria": [
        "Fresh installs default to `No AI`.",
        "Server, Knowledge Base, and user AI policies are evaluated with strictest-policy-wins semantics.",
        "Remote modes include `Remote per request` and `Remote always allowed`; always-allowed is per-user and per-Knowledge Base.",
        "Remote embeddings require separate consent.",
        "Remote call confirmations show provider, model, feature, and categories of content sent.",
        "Remote AI call audit metadata excludes full prompts, full note content, API keys, and full model responses by default.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 16,
      "passes": true,
      "notes": "Layered AI privacy policy: server/knowledge_base/user layers resolved strictest-policy-wins via resolveAiPolicy (packages/schemas/src/ai.ts; AI_POLICY_MODE_RANK off<local_only<remote_per_request<remote_always). Fresh installs default to No AI (DEFAULT_AI_POLICY, missing rows). remoteEmbeddings is separate per-layer consent (remoteEmbeddingsAllowed only when remote allowed AND every layer consents). Single ai_policies table (migration drizzle/0004) with partial unique indexes (one server row; unique scope+scopeId) + scope/mode/scope-null check constraints. ai-policy/store.ts (AiPolicyStore + dbAiPolicyStore, UPSERT + audit_events ai_policy.updated in same tx) and ai-policy/index.ts (createAiPolicyRouter at /api/ai, createAiPolicyKbRouter at /api/knowledge-bases/:kbId/ai/policy AFTER KB router). Routes: server GET any-authed/PUT admin+CSRF; /me self GET+PUT CSRF; /policy effective server+user; KB GET viewer/PUT KB-admin+CSRF, 404 for non-members. Confirmation+audit helpers (remoteCallConfirmationSchema AC5, buildRemoteAiAuditMetadata allowlist AC6 — never leaks prompts/content/keys/responses) ready for US-017/020. Web AiPolicySettings.tsx (<AiPolicySettings> server+user, <KbAiPolicy> KB-scoped) + api.ts helpers. Tests: 25 schema unit, 11 API router unit (in-memory stores), 3 DB integration (pass against live PG). pnpm verify green; Playwright browser verification confirmed setup->No-AI default->edit user/server->strictest effective."
    },
    {
      "id": "US-017",
      "title": "Capture text into AI proposal review queue",
      "description": "As a user, I want captured text to generate reviewable graph proposals when AI is configured, without losing original material.",
      "acceptanceCriteria": [
        "Quick-capture stores original text as Note or Source before extraction.",
        "With configured deterministic mock or local AI, extraction creates AI Proposals for candidate entities/claims.",
        "Remote extraction adapters may be present as configured-but-untested skeletons and must be visibly documented as unverified until tested by an operator.",
        "With no AI, capture stores the note/source and shows setup/unavailable state without blocking manual review/search.",
        "Rejected proposals remain linked to the source note as rejected/dismissed extraction candidates.",
        "Runtime mock output, if exposed, is behind dev/demo flag and labeled `Mock AI / deterministic demo output`.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 17,
      "passes": true,
      "notes": "Quick-capture into AI proposal review queue. Backend apps/api/src/proposals/ (store.ts ProposalStore+dbProposalStore; index.ts createProposalRouter at /api/knowledge-bases/:kbId/proposals GET viewer+ with status/sourceNoteId/sourceSourceId filters, AND createCaptureRouter at /api/knowledge-bases/:kbId/capture POST editor+CSRF + toProposal mapper). Capture stores text as Note/Source FIRST (AC1, never lost), then resolves AI availability + runs extraction; captureResponseSchema returns stored note/source + extraction{status: created|empty|unavailable|error}. Extraction behind GraphExtractor interface (extraction/index.ts, mirrors US-015 adapter layer): MockGraphExtractor deterministic demo (demo:true, proper-noun Concept entities + mentioned_with claim), LlmGraphExtractor wraps LlmProvider with strict-JSON prompt validated by proposalChangesSchema (invalid->[], repair retry deferred to US-019), remote providers (openai/anthropic) unverified skeletons (verified:false, AC3). resolveExtractorFromEnv reads AI_PROVIDER_CONFIG JSON, null when unset (No-AI AC4); mock gated by AI_DEMO_EXTRACTION=true or NODE_ENV!=production (dev/demo flag AC6). computeExtractionAvailability folds resolveAiPolicy (server+user+KB strictest-wins, US-016): unavailable when no extractor, mode off, or remote w/o remoteAllowed. Proposals NEVER mutate canonical records + NOT a graph projection target: createProposal records audit_events proposal.created in tx but NO graph_outbox (review/accept is US-018). Rejected-stays-linked (AC5) deferred to US-018 review actions; proposals carry sourceNoteId/sourceSourceId link now. Shared schemas packages/schemas/src/proposals.ts (proposalChangesSchema discriminated union create_entity/create_claim w/ local ref keys, proposalSchema, captureRequest/ResponseSchema, MOCK_EXTRACTION_LABEL). Web apps/web/src/Proposals.tsx <Proposals> (after <Capture>): quick-capture form + capture outcome (extracted/empty/unavailable/error + demo label) + read-only pending-proposal list w/ candidate changes; api.ts quickCapture/listProposals. Tests: 9 schema unit + 15 extraction unit (mock determinism, parse/validate, remote-unverified, env resolution) + 13 proposals+capture API unit (in-memory stores: auth/404/AC1/AC2/AC3/AC4/AC6/empty/error/viewer-403/csrf/list) + 2 proposals integration (skipIf no DATABASE_URL). Verified: pnpm verify fully green; live compose DB db:migrate + all 206 API tests pass (incl 2 proposals integration); Playwright+Chromium browser flow (setup->KB->capture no-AI=unavailable->set local_only on server/user/KB->capture 'Ada Lovelace met Charles Babbage in London.'->4 demo-labeled proposal changes in queue) + screenshot confirmed (dev-browser unavailable, used Playwright; temp spec removed). NOTE: most of this story's backend+schemas were already in the worktree uncommitted from a prior iteration; this iteration added the missing web UI (<Proposals>), fixed a lint error, formatted, and verified end-to-end."
    },
    {
      "id": "US-018",
      "title": "Review and apply graph proposals",
      "description": "As an editor, I want to review, edit, accept, or reject proposed graph changes before they modify my Knowledge Base.",
      "acceptanceCriteria": [
        "Proposals display structured create/update/delete/merge changes with affected entities, claims, predicates, arguments, confidence, and source citations.",
        "Users can accept, reject, or edit proposals at item level and batch level.",
        "Accepted AI-created claims preserve provenance, citation/source, timestamp, confidence, provider/model where available, and user-confirmation metadata.",
        "Invalid proposal payloads cannot be executed.",
        "Viewers cannot create, accept, reject, or edit proposals.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 18,
      "passes": true,
      "notes": "Review & apply graph proposals. The proposal ROUTER (apps/api/src/proposals/index.ts) applies accepted changes via applyProposalChanges() calling injected entityStore.createEntity/claimStore.createClaim (each goes through the normal canonical write path: graph_outbox + audit in one tx); the ProposalStore only mutates the proposal row. createProposalRouter now takes entityStore/claimStore opts (wired in app.ts). Endpoints (all editor+requireCsrf, viewers read-only/403): PATCH /:proposalId (replace changes, re-validated by proposalChangesSchema so invalid payloads can't be saved/executed AC4 -> proposal.updated audit), POST /:proposalId/accept (body {itemIndexes?,note?}; default=whole batch, itemIndexes=item-level subset AC2; re-parses stored changes, pre-checks every claim entity-arg ref resolves to a created ref OR existing entity via entityStore.getEntity BEFORE mutating -> unresolved=422 AC4; creates entities first mapping ref->id then claims; 201 w/ {proposal,createdEntityIds,createdClaimIds}), POST /:proposalId/reject (body {reason?,dismiss?} -> rejected/dismissed, stays linked to source US-017 AC5). reviewProposal/updateProposalChanges act only on status='pending' (else undefined->404/409, no double-review). AC3 provenance: dbClaimStore.createClaim gained optional `provenance` field persisted to claims.provenance JSONB; accept records {origin:'ai_proposal',proposalId,sourceNoteId,sourceSourceId,provider,model,acceptedBy,acceptedAt,confirmationNote?}. Shared Zod in @jotmind/schemas proposals.ts: editProposalSchema/acceptProposalSchema/rejectProposalSchema/acceptProposalResultSchema. Web: api.ts acceptProposal/rejectProposal/editProposal + <ProposalItem> in Proposals.tsx now has per-item checkboxes + Accept all/Accept selected/Reject/Edit(JSON). Tests: 6 schema unit + 12 new proposals API unit (accept w/ provenance+ref resolution, item-subset, out-of-range 400, unresolved-ref 422, reject stays linked, edit, invalid-edit 400, viewer 403, csrf, 404, already-reviewed 409) + 3 new integration (reviewProposal accepted+audit+no-double-review, updateProposalChanges pending-only, claim provenance persists). Verified: pnpm verify fully green; live compose DB db:migrate + all 220 API tests pass (incl 5 proposals integration); Playwright+Chromium browser flow (setup->server/user/KB local_only->capture 'Ada Lovelace met Charles Babbage in London.'->4 mock proposal changes->Accept all->queue empties; DB confirmed 3 entities + 1 claim w/ origin=ai_proposal provenance + proposal accepted/reviewed) + screenshot. No DB migration (claims.provenance column existed from US-005)."
    },
    {
      "id": "US-019",
      "title": "Add universal command/search interface",
      "description": "As a user, I want one command/search box for natural-language queries, filters, and edit requests so I can quickly access graph information.",
      "acceptanceCriteria": [
        "Manual search works in the command box when AI is disabled.",
        "AI command outputs are constrained to strict Zod schemas before presenting results or proposals.",
        "Invalid schema outputs are discarded and do not execute.",
        "One repair retry may be attempted for invalid structured output.",
        "Low-confidence/invalid structured parsing falls back to full-text/vector search results.",
        "Structured interpretations and fallback results are visually separated.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 19,
      "passes": true,
      "notes": "Universal command/search box. Backend apps/api/src/command/: interpreter.ts (CommandInterpreter interface + MockCommandInterpreter deterministic demo that parses type:/predicate:/tag:/kind: hints into search filters, always 'search' intent confidence 0.9; LlmCommandInterpreter wraps an LlmProvider with strict-JSON prompt validated by commandInterpretationSchema and does EXACTLY ONE repair retry inside interpret() then discards->null on failure, AC3/AC4; resolveCommandInterpreterFromEnv mirrors resolveExtractorFromEnv reading AI_PROVIDER_CONFIG, mock gated by AI_DEMO_EXTRACTION/non-prod) and index.ts (createCommandRouter mounted /api/knowledge-bases/:kbId/command, mergeParams AFTER KB router; single POST / is READ-ONLY -> viewer + NO CSRF, non-members 404). createApp seam commandInterpreter (default resolveCommandInterpreterFromEnv()), reuses searchStore+aiPolicyStore seams. Endpoint ALWAYS runs manual token search (fallback, works with AI off AC1); when AI available (interpreter present + resolveAiPolicy server+user+KB strictest-wins != off, remote needs remoteAllowed) it interprets the query, AC2 outputs constrained to commandInterpretationSchema (discriminatedUnion intent search|create), confidence < COMMAND_CONFIDENCE_THRESHOLD(0.4) discarded->fallback AC5; a search interpretation is executed via searchStore to fill interpretedResults; create interpretation returned as constrained proposal PREVIEW only (no mutation, apply via capture/proposals US-017/018). interpretation + interpretedResults vs fallback are SEPARATE response fields AC6. Shared schemas packages/schemas/src/command.ts (commandRequest/commandSearchFilters/commandInterpretation/commandAiInfo/commandResponse). Web: api.ts runCommand() + <CommandBox> in App.tsx (rendered FIRST per selected KB) showing AI status (+Mock label/repaired/low-confidence flags), a 'Structured interpretation' section, and a separate 'Search results' fallback section. Tests: 8 schema unit + 13 interpreter unit (mock determinism + hint parsing, parse valid/invalid/schema-fail, LLM repair-retry success/no-repair/discard, factory/env resolution) + 11 command API unit (auth/404/400, no-AI fallback AC1, policy-off no-attempt, structured-separate AC2/AC6, repaired AC4, invalid-discarded AC3/AC5, low-confidence AC5, interpreter-throws fallback, create no-second-search). Verified: pnpm verify fully green (format/typecheck/lint/test/test:api/e2e); browser-verified via Playwright+Chromium against live compose DB with mock AI provider (setup->KB->entity->command no-AI=fallback only->set local_only on server/user/KB->command 'type:Person Ada'->structured interpretation w/ interpreted result separate from fallback + 'Mock AI / deterministic demo output' label) + screenshot; temp pw config/spec removed. NOTE: reused an already-running tsx-watch API on :3001 (left by a prior iteration, same DB + mock AI env) for browser verification rather than killing a possibly-concurrent process."
    },
    {
      "id": "US-020",
      "title": "Provide provenance-aware AI answers",
      "description": "As a user, I want AI answers to cite graph claims and source notes so I can verify why an answer was produced.",
      "acceptanceCriteria": [
        "Configured mock/local AI answers include citations to claims, Notes, Sources, or Source Excerpts used as evidence; remote adapters may remain unverified skeletons for V1.",
        "No-AI mode shows setup/unavailable state and offers manual search results where applicable.",
        "Each cited claim displays predicate, connected entities, confidence, and provenance metadata.",
        "Users can click citations to open claim/source/note detail views.",
        "Answers distinguish known facts, inferred facts, uncertain claims, and missing information.",
        "Viewer answer generation is read-only and only allowed when policy permits it.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 20,
      "passes": true,
      "notes": "Provenance-aware AI answers. apps/api/src/answers/ mirrors the command box (US-019): evidence.ts (AnswerEvidenceStore + dbAnswerEvidenceStore COMPOSES searchStore+claimStore+entityStore, no new SQL — token search scoped to claim/note/source, then enriches each claim hit with arguments + entity NAMES so a cited claim carries predicate/connected-entities/confidence/provenance, AC3), answerer.ts (AnswerGenerator interface + MockAnswerGenerator deterministic demo + LlmAnswerGenerator strict-JSON with ONE repair retry inside generate(); remote providers verified:false skeletons AC1; resolveAnswerGeneratorFromEnv mirrors resolveCommandInterpreterFromEnv reading AI_PROVIDER_CONFIG, mock gated by AI_DEMO_EXTRACTION/non-prod), index.ts (createAnswerRouter mounted /api/knowledge-bases/:kbId/answers, mergeParams AFTER KB router; single POST / READ-ONLY -> viewer + NO CSRF, non-members 404). createApp seams answerGenerator (default resolveAnswerGeneratorFromEnv()) + answerEvidenceStore; reuses searchStore/aiPolicyStore. Manual token search ALWAYS runs as fallback so No-AI mode still offers results (AC2). AI available only when generator exists + resolveAiPolicy(server+user+KB strictest-wins) != off (remote needs remoteAllowed) => viewer generation gated by policy (AC6). Answer statements labeled known|inferred|uncertain|missing (AC5 — mock classifies by confidence <0.5=>uncertain and provenance.origin rule/inferred=>inferred). Router CLAMPS each statement's citation refs to real evidence refs so the model can't fabricate citations (AC1/AC3). Shared schemas packages/schemas/src/answers.ts: answerRequestSchema, answerCitationSchema (ref-indexed evidence), answerStatementSchema, generatedAnswerSchema, answerResponseSchema. Web: api.ts answerQuestion() + <Answers> in own file apps/web/src/Answers.tsx (rendered after <CommandBox>); clicking a citation toggles an inline detail panel showing predicate/entities/confidence/provenance (AC4). Tests: 8 schema unit + 16 answerer unit (mock determinism + 4 factKinds, parse valid/invalid/schema-fail, LLM no-repair/repair/discard, remote unverified, factory+env) + 10 answers API unit (auth/404/400, No-AI unavailable+fallback AC2, policy-off no-attempt, cited answer AC1/AC3, fabricated-ref clamping, generator-null/throws fallback, viewer-allowed AC6) + 2 evidence integration (skipIf no DATABASE_URL: claim enrichment predicate/entities/confidence/provenance, note citation). Verified: pnpm verify fully green; live compose DB all 272 API tests pass (incl 2 evidence integration); curl smoke (local_only all 3 layers -> cited known-fact answer w/ Mock label + separate fallback; user policy off -> available:false reason 'AI is disabled by policy' answer null but fallback still returns AC2); Playwright+Chromium browser flow against running API+mock AI (login -> KB -> ask 'knows' -> known-fact statement + Mock AI label + citation detail w/ predicate/entities/confidence 0.9) + screenshot confirmed; temp pw spec removed. NOTE: reused an already-running tsx-watch API on :3001 (DATABASE_URL + AI_PROVIDER_CONFIG mock + AI_DEMO_EXTRACTION) per prior-iteration convention instead of killing a possibly-concurrent process."
    },
    {
      "id": "US-021",
      "title": "Index embeddings through jobs",
      "description": "As a user, I want optional vector search/RAG indexing so AI answers can use relevant graph context when consent permits it.",
      "acceptanceCriteria": [
        "Embeddings are stored in PostgreSQL using `pgvector`.",
        "Embedding jobs index entities, claims, notes, sources, aliases, tags, and recent activity as configured.",
        "Remote embeddings require separate opt-in and remote scope controls.",
        "Existing embeddings can power vector search even when generation providers are currently unavailable.",
        "Job status is visible to users for their Knowledge Base indexing tasks.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 21,
      "passes": true,
      "notes": "Optional vector embeddings indexed through durable jobs. New embeddings table (drizzle 0005) stores pgvector vectors (AC1): vector column declared WITHOUT a fixed dimension (custom Drizzle type `vector` in schema.ts marshals number[] <-> pgvector text '[1,2,3]') so one table holds any provider's dim (mock=8); unique idx (target_type,target_id,model), kb idx, target_type/dimensions check constraints; NOT soft-deleted (derived index). apps/api/src/embeddings/: store.ts (EmbeddingStore + dbEmbeddingStore: upsert onConflict(target,target,model), deleteByTarget, getStats counts/model/dims/lastIndexedAt, hasEmbeddings, vectorSearch via raw `embedding <=> $vec::vector` cosine), content.ts (buildEntity/Claim/Note/SourceContent + hashContent sha256; entity content FOLDS aliases+tags AC2), targets.ts (EmbeddingTargetSource + dbEmbeddingTargetSource reads entity/claim/note/source WHERE deleted_at IS NULL), provider.ts (resolveEmbeddingProviderFromEnv mirrors resolveExtractorFromEnv reading AI_PROVIDER_CONFIG; ConfiguredEmbeddingProvider{provider,kind,model,demo,remote}; anthropic->null no embed endpoint; isRemoteProviderKind), indexer.ts (runEmbeddingIndex: resolveAiPolicy server+KB+requestedBy strictest-wins -> skip when mode=off; remote provider requires resolved.remoteEmbeddingsAllowed AC3; embeds all targets one batch + upserts idempotently; returns {status,reason,indexed,counts}), index.ts (createEmbeddingsRouter at /api/knowledge-bases/:kbId/embeddings AFTER KB router: GET / viewer = EmbeddingStatus{vectorSearchAvailable=stats.total>0 INDEPENDENT of provider AC4, generationAvailable=provider+policy AC3, counts, model, providerKind}; POST /reindex editor+CSRF enqueues durable job EMBEDDING_INDEX_JOB_TYPE='embeddings.index' -> 202 {jobId,status}, visible via existing KB jobs endpoint US-014 AC5). Job handler registered in jobs/handlers.ts (self-contained: dbEmbeddingStore+dbEmbeddingTargetSource+dbAiPolicyStore+resolveEmbeddingProviderFromEnv) so it runs in BOTH in-process and worker-process modes. createApp seam embeddingStore (+reuses aiPolicyStore/jobStore). search/store.ts getVectorSearchAvailability now checks `EXISTS(SELECT 1 FROM embeddings)` rows (table always exists now) decoupled from provider (AC4). Shared schemas packages/schemas/src/embeddings.ts (EMBEDDING_TARGET_TYPES, embeddingStatusSchema, reindexEmbeddingsSchema, embeddingIndexJobPayloadSchema, embeddingIndexResultSchema). Tests: 11 schema unit + 8 content + 7 provider + 7 indexer (AC2 all-4-kinds, targetTypes filter, no-provider/off/remote-no-consent skip, remote-with-consent indexes) + 8 router (auth/404/status AC4 available-with-no-provider/generation gating/viewer-403/csrf/enqueue 202) + 4 integration (skipIf no DATABASE_URL: pgvector round-trip + nearest-neighbor score~1, upsert-replace, indexer vs real rows, deleteByTarget). Verified: pnpm verify fully green; live compose DB db:migrate (0005) + all 306 API tests pass incl 4 embeddings integration. NO browser verification (AC list has only 'Tests pass'/'Typecheck passes', no UI). NOTE: query-time vector search wiring into the search endpoint is deferred (needs provider to embed the query); availability/data persistence is the AC4-testable behavior and is implemented."
    },
    {
      "id": "US-022",
      "title": "Install built-in rule packs",
      "description": "As a user, I want built-in rules for showcase Modules so reasoning is useful without writing rules from scratch.",
      "acceptanceCriteria": [
        "Personal relationship Module includes at least one social-group or event-participation inference rule pack.",
        "Reading/character-map Module includes at least one family/relationship inference or timeline consistency rule pack.",
        "Rule packs are installed as Module bundle content inside a Knowledge Base.",
        "Built-in rules are versioned and auditable when enabled/disabled.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 22,
      "passes": true,
      "notes": "Built-in rule packs installable as Module bundle content. Catalog is static shared content in @jotmind/schemas rules.ts: BUILTIN_RULE_MODULES (Module->packs->rules) + findBuiltinRulePack(); reuses ruleStatusSchema/RULE_STATUSES from graph.ts (do NOT redefine). Two showcase Modules: 'personal-relationship' w/ an event-participation pack (acquainted_via_event + co_member_via_group; AC1 social-group/event-participation inference) and 'reading-character-map' w/ a family-relationship pack (sibling + grandparent; AC2 family/relationship inference). Rule text is canonical Prolog-like (knows(?a,?b) <- claim(...), arg(...) form) for US-023/024 to parse/run later. apps/api/src/rules/: store.ts (RuleStore + dbRuleStore) + index.ts (createRuleRouter). Mounted at /api/knowledge-bases/:kbId/rules (mergeParams, AFTER KB router); createApp seam ruleStore. Rules are NOT graph projection targets -> writes record audit_events ONLY (no graph_outbox). Built-in provenance {moduleId,packId,ruleKey,packVersion} stashed in rule_definitions.compiled JSONB under a builtin key; readBuiltinMeta + toRule surface moduleId/packId. Install (AC3/AC4): installRulePack inserts each rule status='disabled', version=pack.version (versioned), idempotent skip when (kb,name,version) exists (matches rule_definitions_kb_name_version_uq partial unique idx), returns {installed,skipped} + audit rule.installed per rule. setRuleStatus toggles enabled/disabled + audit rule.enabled/rule.disabled (auditable AC4). Routes: GET /packs (catalog) + GET / (installed) viewer; POST /install + PATCH /:ruleId editor+CSRF; non-members 404; viewers read-only 403. /packs handler is synchronous (plain (req,res)=>{}, NOT asyncHandler). No DB migration (rule_definitions existed from US-005). Web apps/web/src/Rules.tsx <Rules> (after <Proposals> for selected KB): built-in Modules/packs list w/ Install buttons + installed-rules list w/ Enable/Disable toggles (editor-gated, read-only notice for viewers); api.ts listBuiltinRulePacks/listRules/installRulePack/setRuleStatus. Tests: 7 schema unit + 9 rules API unit (in-memory stores: catalog, install disabled+audit, idempotent skip, enable/disable+audit, 404 unknown pack, 400 bad status, viewer 403, csrf 403, non-member 404) + 3 rules integration (skipIf no DATABASE_URL: install versioned+disabled+audit, idempotent re-install, enable/disable audit). Verified: pnpm verify fully green (format/typecheck/lint/test/test:api/test:e2e); live compose DB all 318 API tests pass incl 3 rules integration; curl end-to-end (setup->KB->packs->install reading-character-map/family-relationship 2 disabled v1 rules->enable->audit rule.installed x2 + rule.enabled); Playwright+Chromium browser flow (login->select KB->Rules section shows both Modules->install event-participation->Enable a rule->[enabled]) + screenshot confirmed. Reused running tsx-watch API on :3001 (same DB); temp pw spec/screenshot removed."
    },
    {
      "id": "US-023",
      "title": "Support restricted text rule authoring",
      "description": "As an advanced user, I want to author simple restricted Datalog-like rules through a Prolog-like text UI so I can define custom inference.",
      "acceptanceCriteria": [
        "Canonical text syntax supports explicit variables, e.g. `knows(?a, ?b) <- claim(?c, \"knows\"), arg(?c, \"subject\", ?a), arg(?c, \"object\", ?b).`",
        "Low-level predicates include equivalents of `entity(id, type, name)`, `claim(id, predicate)`, and `arg(claimId, role, entityId)`.",
        "Parser/compiler enforces Datalog Range-Restriction Safety: any head variable must appear in at least one positive, non-negated relational atom in the rule body.",
        "Parser/compiler rejects existential tokens, skolem functions, blank nodes, and any rule head that would create a relation involving entities not already present in the database.",
        "Rule validation runs before save/enable.",
        "Invalid rules may be saved as drafts but cannot be enabled or executed.",
        "Rules have no filesystem, network, process, arbitrary JavaScript, provider API, unbounded/arbitrary recursion, or unrestricted negation access.",
        "Bounded recursion validates and enforces a configurable depth/iteration cap.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 23,
      "passes": true,
      "notes": "Restricted Datalog-like rule authoring. Shared parser/compiler/validator in @jotmind/schemas rules.ts: parseRule(text,{recursionCap?}) tokenizes + parses head(?a,?b) <- atom1,atom2,... . into a CompiledRule AST (head/body atoms with var|literal terms; never eval'd -> no fs/network/process/JS/provider, AC7). Built-in low-level predicates RULE_BUILTIN_PREDICATES: entity/3, claim/2, arg/3 (AC2). Validator enforces: built-in arity; range-restriction safety (every head var must appear in a positive non-negated body atom, AC3); rejects skolem/function terms f(...), blank nodes _:x, anonymous/existential vars ?_, quantifier keywords exists/forall/some/any/u2203/u2200 (AC4); head cannot redefine a built-in predicate; body atoms must be built-in OR the rule's own head (bounded DIRECT recursion only, AC7/AC8); restricted negation (negated-atom vars must also be positively bound). Configurable recursion cap clamped [RULE_RECURSION_CAP_MIN=1..MAX=64] default 16 via clampRecursionCap. New shared schemas: ruleTerm/ruleAtom/ruleBodyAtom/compiledRule, createRuleSchema, updateRuleSchema (>=1 field), validateRuleSchema, ruleValidationResultSchema; ruleDefinitionSchema gained valid/validationErrors/recursionCap. Store apps/api/src/rules/store.ts: createRule (always status='draft' v1, duplicate name -> {ok:false,reason:'duplicate_name'}, AC6 invalid saves as draft) + updateRule (re-validates; an ENABLED rule that becomes invalid is auto-demoted to draft). Both stash {authored:{ast,validation,recursionCap}} in rule_definitions.compiled JSONB (parallel to US-022 builtin key); readAuthoredCap reads it. No migration (rule_definitions from US-005). Router apps/api/src/rules/index.ts: POST /validate (viewer+, read-only, no CSRF, live preview), POST / create + PUT /:ruleId edit (editor+CSRF); existing PATCH /:ruleId status toggle now GATES enable on validity -> enabling invalid rule = 422 w/ validationErrors (AC5/AC6, validation runs before enable). toRule re-parses ruleText as single source of truth so valid/validationErrors/recursionCap surface consistently for built-in AND authored rules. Web apps/web/src/Rules.tsx: 'Author a Custom Rule' form (Validate + Save-as-draft, editor-only) + per-installed-rule valid/invalid badge + error list; api.ts validateRule/createRule/updateRule. Tests: 12 schema parser unit (canonical example, all 3 builtins+arity, range-restriction, skolem/blank-node/existential rejection, builtin-redefine, unknown-predicate, bounded recursion+cap, clamp, empty-body/missing-dot, unsafe negation) + 12 API router unit (validate viewer read-only, create-as-draft+audit, invalid-saves-as-draft+enable-422, enable-valid, duplicate-409, PUT edit+re-validate, 404, viewer-403, csrf-403, empty-text-400) + 2 integration (skipIf no DATABASE_URL: create persists ast+validation+cap+audit; duplicate-name + update re-validates). Verified: pnpm verify fully green (format/typecheck/lint/test/test:api/e2e); live compose DB all 24 rules tests (incl 5 integration) pass; Playwright+Chromium browser flow (setup->KB->author invalid rule=invalid badge->fix=valid->save draft->[draft] ✓ valid->Enable->[enabled]) + screenshot confirmed; temp spec/screenshot removed. dev-browser unavailable -> used Playwright per prior convention; reused running tsx-watch API on :3001 (same DB), truncated users for first-run setup."
    },
    {
      "id": "US-024",
      "title": "Run rules with traces",
      "description": "As a user, I want rule runs to produce traceable inferred results so I can understand why an inference exists.",
      "acceptanceCriteria": [
        "Rule execution runs on the backend against stored Knowledge Base data.",
        "Rule Runs enforce depth/iteration limits for bounded recursion and report limit-exceeded errors safely.",
        "Datalog-to-SQL/AGE translation injects hidden `deleted_at IS NULL` predicates into every clause so soft-deleted records are excluded from rule execution.",
        "Unless a rule explicitly references valid time, rule execution automatically applies current-time bounds: `valid_start <= NOW() AND (valid_end IS NULL OR valid_end >= NOW())`.",
        "Rule Runs record status, start/end timestamps, errors, and triggering user/job.",
        "Inferred Results show source rules and source claims/arguments used.",
        "Rule errors are displayed without corrupting claims, schemas, or rules.",
        "Inferred Results are labeled as inferred and are not stored as Claims by default.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 24,
      "passes": true,
      "notes": "Run rules with traces. Pure in-memory engine apps/api/src/rules/engine.ts executeRule(CompiledRule, RuleFacts) — bottom-up semi-naive evaluation of the US-023 AST (entity/3, claim/2, arg/3 builtins + bounded DIRECT head recursion), NEVER eval'd. Positive atoms joined first then stratified negation; each derived head tuple accumulates a trace (source claimIds/entityIds/argumentIds, AC6). Bounded by rule.recursionCap iterations; if new tuples still derive at the cap it sets limitExceeded (AC2). NOTE: single-rule recursion can't bootstrap a base (no disjunction) so it's effectively inert — the cap is a safety guard. Facts loaded by run-store.ts dbRuleRunStore.loadFacts which INJECTS the hidden safety predicates: deleted_at IS NULL on entities/claims/claim_arguments (AC3) + current valid-time bounds on claims (validStart IS NULL OR <=now()) AND (validEnd IS NULL OR >=now()) (AC4). GOTCHA: each OR fragment MUST be wrapped in its own parens in the sql template — without parens, SQL AND-binds-tighter-than-OR precedence silently lets expired/not-yet-valid claims through (caught by integration test). New tables (drizzle 0006): rule_runs (status running/completed/failed, started/finished_at, error, result_count, iterations, limit_exceeded, triggered_by user + job_id — AC5; operational, no soft-delete) and inferred_results (rule_run_id cascade, predicate, arguments jsonb [{name,value,entityName}], trace jsonb {claimIds,entityIds,argumentIds}; derived, no soft-delete). Inferred results are LABELLED 'inferred' (constant) and NOT written to claims (AC8) — they are NOT graph projection targets. Router apps/api/src/rules/index.ts adds: POST /:ruleId/run (editor+CSRF — a run is a write; only status='enabled' AND re-parsed-valid rules execute, else 409/422 AC: US-023 invalid-cannot-run), GET /runs + GET /runs/:runId (viewer, read-only). Run handler wraps execution in try/catch so any failure records a failed run with 0 results and never mutates canonical data (AC7); limitExceeded => status='failed' + error. createApp seam ruleRunStore. Shared schemas packages/schemas/src/rules.ts: ruleRunStatusSchema, inferredResult{Argument,Trace,}Schema, ruleRunSchema, ruleRunResultSchema, ruleReferencesValidTime() seam (always false — dialect has no valid-time predicate). Web apps/web/src/Rules.tsx: per-enabled-rule Run button (disabled unless enabled+valid) + 'Last Rule Run' panel showing status/result-count/iterations, each result [inferred] predicate(args by entityName) + 'derived from N claim(s), M argument(s)' trace; api.ts runRule/listRuleRuns/getRuleRun. Tests: 7 engine unit (derivation, trace AC6, literal-predicate filter, empty, head-literal slot, recursive-inert termination, cap bounds iterations) + 9 router unit (run+trace+inferred-label, persists+triggeredBy AC5, not-enabled 409, invalid 422, 404, viewer 403, csrf 403, non-member 404, viewer-reads-runs) + 3 integration (loadFacts excludes soft-deleted AC3, applies valid-time bounds AC4, persists run+results+audit AC5/AC6). Verified: pnpm verify fully green (format/typecheck/lint/test/test:api/e2e); live compose DB db:migrate (0006) + all 349 API tests pass incl 3 new integration; Playwright+Chromium browser flow (setup->KB->2 Persons+Event->2 attended claims->install personal-relationship/event-participation->enable acquainted_via_event->Run-> 4 [inferred] acquainted results w/ 'derived from 2 claim(s), 4 argument(s)' traces) + screenshot confirmed; temp config/spec removed. dev-browser unavailable -> Playwright per convention."
    },
    {
      "id": "US-025",
      "title": "Accept inferred results as claims",
      "description": "As an editor, I want to accept an inferred result as a stored claim so important derived knowledge can become explicit while preserving provenance.",
      "acceptanceCriteria": [
        "Editors can convert an Inferred Result into an Accepted Inferred Claim after confirmation.",
        "Accepted Inferred Claims preserve source rule, rule run, source claims, timestamp, and accepting user.",
        "Accepted Inferred Claims are distinguishable from user-entered and AI-extracted claims.",
        "Viewers cannot accept inferred results.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 25,
      "passes": true,
      "notes": "Accept inferred results as claims. New route POST /api/knowledge-bases/:kbId/rules/runs/:runId/results/:resultId/accept (editor+CSRF; viewers 403 AC4; non-members 404) on the existing rule router. Mirrors the US-018 proposal-accept pattern: the conversion happens IN THE ROUTER via the injected claimStore.createClaim so the new claim goes through the canonical write path (graph_outbox + audit in one tx); RuleRouterOptions gained a claimStore seam wired in app.ts. RuleRunStore gained getResult(kbId,runId,resultId) (scoped to both run AND KB) + the in-memory fake implements it. Argument mapping: each resolved head arg of the inferred result -> claim arg, entityName!==null => entity arg (entityId=value) else literal arg; role = head var name. AC2/AC3 provenance recorded on the created claim: {origin:'inferred' (INFERRED_CLAIM_ORIGIN constant, distinguishes from user-entered (no origin) + AI-extracted ('ai_proposal')), ruleId, ruleName, ruleRunId, inferredResultId, sourceClaimIds, sourceEntityIds, sourceArgumentIds, acceptedBy, acceptedAt, confirmationNote?} via CreateClaimInput.provenance JSONB. Shared schemas @jotmind/schemas rules.ts: acceptInferredResultSchema ({confirmationNote?}), acceptInferredResultResultSchema ({claimId,result}), INFERRED_CLAIM_ORIGIN. Web: api.ts acceptInferredResult() + per-result 'Accept as claim' button (editor-only) in the Rules.tsx 'Last Rule Run' panel that flips to a '✓ accepted as claim' badge; testids rules-run-accept-<id>/rules-run-accepted-<id>. Tests: 4 schema unit + 6 API router unit (accept w/ provenance+entity args AC1/AC2/AC3, viewer 403 AC4, csrf 403, 404 unknown run, 404 unknown result, non-member 404) + 1 integration (getResult scoped to run+KB). Verified: pnpm verify:quick green (format/typecheck/lint/test) + test:api 356 pass + test:e2e green; live compose DB all 356 API tests pass incl 4 rules-run integration; curl end-to-end (setup->KB->2 Persons+Event->2 attended claims->install personal-relationship/event-participation->enable acquainted_via_event->run->accept) -> DB confirmed claim w/ full origin=inferred provenance; Playwright+Chromium browser flow (login->select KB->Run->4 [inferred] results->Accept as claim->'✓ accepted as claim' badge) + screenshot confirmed. dev-browser skill unavailable -> Playwright per prior convention; reused running tsx-watch API on :3001 (same DB). No DB migration (claims.provenance existed from US-005)."
    },
    {
      "id": "US-026",
      "title": "Add AGE-backed traversal where needed",
      "description": "As a user, I want graph traversal features to use the derived graph projection where it adds value while preserving relational canonical truth.",
      "acceptanceCriteria": [
        "Projector writes entity/claim/argument graph structures to Apache AGE from `graph_outbox`.",
        "Multi-argument claims are projected as claim nodes connected to entity nodes by role-labeled edges.",
        "Projection rebuild/repair can recreate AGE state from relational tables.",
        "Rebuild/repair updates `graph_projection_status` through `rebuilding`, `synchronized`, and `failed` states without locking the app.",
        "While rebuilding, AGE-dependent network/traversal views are disabled with an `Indexing Graph...` UI state, and core search/table/detail pages use relational PostgreSQL fallback queries.",
        "Traversal-dependent views/rules can query AGE through service abstractions.",
        "Projection lag/failure is visible through job/status UI and does not corrupt canonical records.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 26,
      "passes": true,
      "notes": "Real Apache AGE projector replaces the US-006 stub (createApp default projector = ageProjector unless GRAPH_PROJECTOR=stub). AGE access layer apps/api/src/graph/age.ts: withAge(fn) runs all AGE work inside getSqlClient().begin() (pins one pooled connection) after LOAD 'age' + SET search_path + idempotent create_graph('jotmind_graph'); session.cypher(body,columns,params) runs Cypher. CRITICAL: AGE's 3rd cypher() arg must be a BARE bind param inferred as agtype ($1::agtype is rejected); postgres-js sends ${JSON.stringify(params)} as an inferred-agtype bind param so user text is escaped + injection-safe (verified live + integration test). age-projector.ts ageProjector: project(event) always RELOADS the canonical row (never trusts outbox payload) -> idempotent create/update/delete/rebuild; model (AC1/AC2) (:Entity{id,kbId,...}), (:Claim{...,literalArgs}), (:Claim)-[:ARGUMENT{argumentId,role,position}]->(:Entity) per entity arg (FIXED :ARGUMENT label + role property, NOT dynamic labels); note/source = no-op. prepareRebuild(kbId?) clears AGE (DETACH DELETE). Projector interface gained optional prepareRebuild. AC4 status: singleton graph_projection_status table (drizzle 0007, id='default' check) state rebuilding/synchronized/failed + rebuild metadata; ProjectionStatusStore + dbProjectionStatusStore (UPSERT 'default'); createApp seam projectionStatusStore. graphProjectionStatusSchema extended w/ state + lifecycle fields. rebuildProjection now: markRebuilding -> prepareRebuild -> reproject canonical -> markSynchronized (or markFailed+rethrow). AC4/AC7 non-locking: POST /api/graph/projection/rebuild (sys admin+CSRF) ENQUEUES GRAPH_REBUILD_JOB_TYPE='graph.projection.rebuild' job (202 {job}), handler in jobs/handlers.ts runs rebuildProjection w/ ageProjector (self-contained, both worker modes); AGE is separate from canonical so app writes/relational reads never block. AC6 traversal.ts GraphTraversalService (ageTraversalService/createAgeTraversalService({statusStore})): getEntityNeighborhood returns nodes+role-labeled edges via AGE; throws GraphProjectionUnavailableError when state rebuilding/failed so callers fall back (never raw Cypher outside this service). AC5/AC7 web: api.ts getGraphProjectionStatus(); GraphViews.tsx loads it best-effort (core relational views work even if status fails) -> while rebuilding the Network tab shows network-indexing 'Indexing Graph…' panel (timeline/table/detail/sources keep using relational data); graph-projection-failed + graph-projection-lag(pending>0) banners. GOTCHA: graph_projection_status FK->knowledge_bases means TRUNCATE knowledge_bases CASCADE (integration tests) wipes the row; get() returns synchronized default when missing. Shared schemas packages/schemas/src/graph.ts: GRAPH_PROJECTION_STATES, graphProjectionStateSchema, extended graphProjectionStatusSchema, GRAPH_REBUILD_JOB_TYPE, graphRebuildJobPayloadSchema. Tests: graph.test.ts updated (in-memory ProjectionStatusStore fake + rebuild state-machine/prepareRebuild/failed unit tests + rebuild-enqueues-job API test) + 8 NEW age-projector.integration.test.ts (param-escaping AC, entity projection AC1, multi-arg role-labeled edges AC2, idempotency, rebuild AC3/AC4, soft-delete removal, traversal AC6, rebuilding-unavailable AC5). Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test=schemas123+web10/test:api 297pass+70skip/e2e); live compose DB all 367 API tests pass incl 8 new AGE integration; browser-verified via Playwright+Chromium against running tsx-watch API on :3001 (synchronized=network node renders; set graph_projection_status='rebuilding' -> 'Indexing Graph…' panel + network-view gone + Table tab still shows entity = relational fallback) + screenshots; live curl: admin /projection/process processed the Ada Lovelace entity -> AGE MATCH (e:Entity) confirms the projected node (projector age:jotmind_graph, stubbed:false). dev-browser skill unavailable -> Playwright per prior convention; temp specs/screenshots removed."
    },
    {
      "id": "US-027",
      "title": "Build custom schema minimum UI/API",
      "description": "As an advanced user, I want to define custom entity types and claim predicates so JotMind can adapt to new domains.",
      "acceptanceCriteria": [
        "Users can create custom entity types with display name, description, and JSONB property schema validation.",
        "Users can create custom claim predicates with allowed argument roles and compatible entity types.",
        "Entities/claims validate against active schema versions before save.",
        "Schema definitions are Knowledge Base scoped and included in portable JSON export.",
        "Schema version references exist on stored graph records.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 27,
      "passes": true,
      "notes": "Custom schema minimum UI/API. schema_definitions + schema_versions tables existed from US-005 (NO migration). apps/api/src/schema-defs/: store.ts (SchemaStore + dbSchemaStore: createDefinition inserts definition + v1 active version in one tx + schema.created audit; (kb,kind,name) unique -> duplicate_name; getActiveVersionByName for entity/claim validation; listDefinitions) + index.ts (createSchemaRouter at /api/knowledge-bases/:kbId/schema, mergeParams AFTER KB router). Schemas are NOT graph projection targets -> audit-only writes (mirrors rules store). Routes: GET / + GET /export (portable JSON AC4; US-032 builds full export) + GET /:defId viewer; POST / editor+CSRF (409 duplicate); non-members 404, viewers 403. createApp seam schemaStore (resolves dbSchemaStore by default + passes to entity/claim routers). AC1 entity types carry propertySchema (validated by existing validateCustomProperties); AC2 claim predicates carry spec.argumentRoles (name/required/entityTypes/allowLiteral). AC3/AC5: entity+claim ROUTERS validate against active schema + stamp schemaVersionId via shared checkEntityAgainstSchema/checkClaimAgainstSchema (exported from schema-defs/index.ts); 400+issues on mismatch; no custom schema => unconstrained (schemaVersionId null). Validation runs on create AND update (update loads existing record to fill unchanged half). claim router also takes entityStore to resolve entity types for compatible-type checks. UpdateEntityFields/UpdateClaimFields gained schemaVersionId. Shared schemas packages/schemas/src/schema-defs.ts: schemaArgumentRoleSchema, predicateSpecSchema, schemaVersionSchema, schemaDefinitionSchema(+activeVersion), createSchemaDefinitionSchema, schemaExportSchema, validateEntityProperties/validatePredicateArguments (shared API+web). Reuses schemaDefinitionKindSchema from graph.ts (do NOT redefine). Web: apps/web/src/SchemaDefinitions.tsx <SchemaDefinitions> (after <Search> for selected KB) lists entity types/predicates + editor-only create form (propertySchema/spec as JSON); api.ts listSchemaDefinitions/createSchemaDefinition. GOTCHA: entity/claim routers don't default schemaStore (skip validation when absent) but createApp always injects dbSchemaStore, so entities.test.ts/claims.test.ts inject reusable createMemorySchemaStore() exported from schema-defs.test.ts. Tests: 14 schema unit + 9 schema API unit (auth/404/AC1/AC2/dup-409/400/csrf/viewer-403/list+export AC4) + 5 entity validation unit (validate+stamp+unconstrained) + 4 claim validation unit + 3 schema integration (create v1+audit+getActiveVersionByName, duplicate, list scoped). Verified: pnpm verify:quick green + test:api 328 + test:e2e green; live compose DB all 401 API tests pass incl 3 schema + entity/claim validation integration; Playwright+Chromium browser flow (setup->KB->Custom Schemas section->create entity_type Person requiring numeric born->appears in list 'v1 fields: born') + screenshot. dev-browser skill unavailable -> Playwright per prior convention; reused running tsx-watch API on :3001 (same DB)."
    },
    {
      "id": "US-028",
      "title": "Support schema version evolution basics",
      "description": "As an advanced user, I want schema changes to avoid silently corrupting existing data.",
      "acceptanceCriteria": [
        "Compatible changes may update labels/descriptions/view metadata or loosen validation without rewriting existing data.",
        "Breaking changes create or activate a new schema version.",
        "Existing data may remain valid under its referenced old schema version.",
        "Cross-version search and reasoning retrieve records by conceptual string type (for example `type = \"Person\"`) rather than requiring matching `schema_version_id`.",
        "Filters/rules evaluating JSONB fields treat properties absent from older schema versions as `null` and do not throw validation or runtime errors.",
        "Validation warnings for old/mismatched data appear in schema/admin views and affected detail pages.",
        "AI-assisted schema migrations, if present, create reviewable proposals and never auto-apply.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 28,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-029",
      "title": "Polish personal relationship Module",
      "description": "As a user managing relationships, I want a built-in Module for people, interactions, events, and places so I can remember social context over time.",
      "acceptanceCriteria": [
        "Module includes default schemas for Person, Event, Place, Note/Source views, and relationship claims.",
        "Person profiles support names, aliases, birthday uncertainty, contact/social fields, notes, tags/groups, and relationship strength.",
        "Users can record events where people were met, introduced, interacted, or mentioned.",
        "Users can query location-aware recall, e.g. “Who do I know in Shanghai?” through search/command where data exists.",
        "Remote sharing defaults to minimal, and Remote RAG context sharing requires additional prominent confirmation.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 29,
      "passes": true,
      "notes": "Built-in personal-relationship Module (installable bundle of default schemas + rule packs). Catalog is static shared content @jotmind/schemas modules.ts: BUILTIN_MODULES + findBuiltinModule(); each Module declares entityTypes (name/displayName/description/propertySchema), claimPredicates (name/displayName/description/spec), and rulePacks ({moduleId,packId} refs into BUILTIN_RULE_MODULES). personal-relationship ships Person/Event/Place entity types (AC1) + relationship predicates attended/met/introduced/interacted_with/mentioned (AC3) + knows + lives_in (Person->Place, AC4 location recall) + the event-participation rule pack. AC2 Person propertySchema: birthday + birthdayPrecision (uncertainty), email/phone/social/homepage (contact/social), relationshipStrength (number); names/aliases/tags(groups)/notes are first-class ENTITY fields (not custom props) so none are required (name-only Person stays valid). propertySchema only supports string/number/boolean/date (US-005) -> no new field types. Install happens IN THE ROUTER (no module store, no migration), mirroring the US-018 proposal-accept pattern: apps/api/src/modules/index.ts createModuleRouter reuses injected schemaStore.createDefinition + ruleStore.installRulePack so created content goes through canonical write paths (audit + idempotency). Mounted /api/knowledge-bases/:kbId/modules (mergeParams, AFTER KB router); createApp reuses resolved schemaStore + ruleStore/kbStore/authStore. GET / (viewer) = catalog + per-KB installed status (installedEntityTypes/installedClaimPredicates/fullyInstalled); POST /install (editor+CSRF, {moduleId}) is IDEMPOTENT (schema dup_name -> item skipped; rule packs reuse US-022 idempotent installer). Non-members 404, viewers 403 on install, 400 invalid body, 404 unknown module. AC5: remote sharing minimal by default (US-016 DEFAULT_AI_POLICY off); enabling 'Allow remote embeddings (Remote RAG context sharing)' in AiPolicySettings.tsx PolicyEditor now triggers a prominent window.confirm before checking (decline keeps it off; turning OFF needs no confirmation). Web: apps/web/src/Modules.tsx <Modules> (after <Search> for selected KB) lists modules + Install button (editor) + '✓ installed' badge; api.ts listModules/installModule. Tests: 13 schema unit (modules.test.ts) + 8 API router unit (catalog+installed status, install schemas+rule packs, idempotent re-install -> all skipped + fullyInstalled, 404 unknown module, 400 invalid, viewer 403, csrf 403, non-member 404). modules.test.ts imports createMemorySchemaStore from schema-defs.test.ts (established pattern) so that file's describe also runs (23 total). Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas165+web10/test:api 369pass+76skip/e2e); browser-verified via Playwright+Chromium against the live compose DB + running tsx-watch API on :3001 (truncated users for first-run setup -> setup -> create KB -> Modules section shows personal-relationship with Person/Event/Place + 7 relationship claims -> Install -> '✓ installed' badge -> reload+reselect -> Custom Schemas shows Person+attended; AC5: select remote_per_request + click remote-embeddings -> prominent 'Remote RAG context sharing' confirm dialog -> dismiss -> stays unchecked) + screenshot; DB confirmed 3 entity_type + 7 claim_predicate schema_definitions + 2 disabled rules. dev-browser skill unavailable -> Playwright per prior convention; temp config/spec/screenshot removed. No DB migration (schema_definitions/rule_definitions exist from US-005)."
    },
    {
      "id": "US-030",
      "title": "Polish reading / character-map Module",
      "description": "As a reader or writer, I want a built-in Module for character maps so I can track characters, plot events, and logical relationships.",
      "acceptanceCriteria": [
        "Module includes default schemas for Character/Person, Event, Place, Concept, Source views, and plot/relationship claims.",
        "Users can record first appearance, attributes, event participation, and character relationships.",
        "Network view displays a character relationship graph for a selected work or tag/filter.",
        "Timeline/card view displays plot events by dates or sequence numbers.",
        "Built-in rule pack demonstrates family/relationship inference or timeline consistency checks.",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 30,
      "passes": true,
      "notes": "Reading/character-map Module (US-030) is CATALOG-ONLY content + 2 small GraphViews enhancements (no API/migration). Added reading-character-map to BUILTIN_MODULES (@jotmind/schemas modules.ts): entity types Character (AC1; propertySchema firstAppearance + attributes role/species/gender/affiliation/status, AC2 first-appearance+attributes; names/aliases/tags first-class so none required), Event/Plot Event (date + numeric sequence AC4 + location/chapter), Place (locality/region/country), Concept (category) — AC1 Character/Event/Place/Concept (Source views = existing Notes/Sources capture, not entity types). claimPredicates (plot/relationship, AC2): parent_of(parent,child) MATCHING the family-relationship rule pack atoms (AC5), appears_in(character,event) event participation, first_appears_in(character,context-literal) first appearance, knows/married_to/allied_with/enemy_of (Character-Character), located_at(character,place). rulePacks refs the existing reading-character-map/family-relationship pack from US-022 (AC5). Install router (modules/index.ts) + Modules.tsx are generic so the new module is auto-installable/browsable; all 4 types already in BUILTIN_ENTITY_TYPES (graph.ts) so entity-create select offers them. GraphViews.tsx: NetworkView gained a tag/work filter (network-tag-filter select -> filters visibleEntities by tag, edges between visible only, network-filter-empty when none) for AC3 (character relationship graph for a selected work/tag); TimelineView gained an order toggle (timeline-order: date|sequence) for AC4 — plot-Event entities read an optional numeric sequence JSONB prop via readSequence() shown as #N; sequence mode sorts ascending, unsequenced fall back newest-date-first. Tests: 5 new schema unit (modules.test.ts: AC1 Character/Event/Place/Concept, AC2 Character first-appearance+attributes, AC4 Event numeric sequence validation, AC2/AC5 parent_of+plot predicates, AC5 rule-pack ref) — modules.test now 20, schemas 170 total; existing modules API tests use arrayContaining/toContain so a 2nd module is fine. Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas170+web10/test:api 369pass+76skip/test:e2e); browser-verified via Playwright+Chromium against live compose DB + running tsx-watch API on :3001 (truncate users -> setup -> create KB -> Modules shows reading-character-map w/ Character/Concept -> Install -> ✓ installed -> reload+reselect -> Custom Schemas shows Character + parent_of; created Frodo/Sam(tag lotr)+Sherlock(tag holmes) Characters + 2 Plot Events w/ {sequence:1|2} -> Network tab tag-filter=holmes shows only Sherlock node (Frodo hidden) AC3 -> Timeline tab order=sequence shows #1 Leaving the Shire before #2 Council of Elrond AC4) + screenshots confirmed. dev-browser skill unavailable -> Playwright per prior convention; temp spec/screenshots removed. No DB migration (schema_definitions/rule_definitions exist from US-005)."
    },
    {
      "id": "US-031",
      "title": "Import portable and structured data through reviewable flows",
      "description": "As a user, I want to import existing notes and structured data without uncontrolled graph mutation.",
      "acceptanceCriteria": [
        "Users can import plain text or Markdown as Notes/Sources.",
        "Users can import structured CSV/table data and map columns to entity or claim fields.",
        "Imports generate reviewable proposed changes before mutating graph records.",
        "Import jobs expose user-visible status and failure details.",
        "Permission checks restrict import mutation proposals to editors/admins/owners.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 31,
      "passes": true,
      "notes": "Reviewable data import. Imports NEVER mutate the graph directly: POST /api/knowledge-bases/:kbId/imports (apps/api/src/imports/index.ts, editor+CSRF AC5, non-members 404) enqueues a durable IMPORT_JOB_TYPE='import.process' job (202 {jobId,status}); handler in jobs/handlers.ts calls the self-contained runImport (imports/runner.ts) which parses the payload into candidate ProposalChange[] and stores them as ONE pending Proposal of kind 'import' (AC3) — candidates then flow through the existing US-018 review/accept path. GET /imports lists THIS KB's import jobs (viewer+) so status/failure is USER-visible (AC4), distinct from the system-admin /api/jobs endpoint. Pure shared parsing in @jotmind/schemas imports.ts: parseCsv (RFC-4180-ish: quoted fields w/ embedded commas/quotes/newlines, CRLF, trailing-newline trim) + buildImportChanges(request)->{changes,rowCount}. Text/Markdown -> create_note OR create_source change (AC1, target note|source); CSV entity mapping -> one create_entity per row (nameColumn + fixed type|typeColumn + propertyColumns w/ string|number|boolean coercion + tagColumns, unique import-N refs, skips nameless rows); CSV claim mapping -> one create_claim per row (role->column, kind entity(ref=value)|literal). Mapping problems throw ImportParseError -> runImport returns {status:'error',proposalId:null,error} (job still SUCCEEDS but result.status='error'+message shown); a genuinely thrown error (e.g. missing requestedBy) dead-letters the job w/ failureReason (both surfaced in the imports list AC4). EXTENDED the proposal change discriminated union (packages/schemas/src/proposals.ts) with create_note/create_source; applyProposalChanges (proposals/index.ts) now also calls noteStore.createNote/sourceStore.createSource (injected seams, wired in app.ts) on accept, and acceptProposalResultSchema gained createdNoteIds/createdSourceIds (.default([])). NO new table/migration — reuses jobs (US-007) + proposals (US-017). Shared schemas imports.ts: IMPORT_JOB_TYPE, importRequestSchema (discriminatedUnion text|markdown|csv), csvMappingSchema (discriminatedUnion entity|claim), importJobPayloadSchema, importJobResultSchema, importEnqueueResponseSchema. Web apps/web/src/Imports.tsx <Imports> (after <Capture>): text/Markdown form (format+target+title+content) + CSV->entities form (type+nameColumn+content) + recent-imports status list (status/result/itemCount/failureReason); api.ts createImport/listImports; describeChange in Proposals.tsx handles all 4 ops. Tests: 17 schema unit (parseCsv quoting/CRLF, importRequestSchema valid/invalid, buildImportChanges text->note/source AC1 + csv entity name/type/props/tags/typeColumn/skip-nameless/missing-column-throws/no-data-throws AC2 + csv claim entity+literal args AC2) + 9 imports API unit (auth 401, non-member 404, viewer 403 AC5, csrf 403, invalid 400, editor enqueue 202 AC4/AC5 w/ payload assert, GET lists+filters import jobs AC4, runImport text->note proposal AC1/AC3, csv->entities proposal AC2/AC3, unparseable-csv->error-result-no-proposal AC4) + 1 proposals API unit (accept import proposal creates notes+sources US-031). Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas187+web10/test:api 456pass incl all integration/test:e2e); browser-verified via Playwright+Chromium against running tsx-watch API on :3001 (truncate users -> setup -> KB 'Import Demo' -> text import 'My imported note' + CSV import Ada Lovelace/Grace Hopper -> imports-list shows 2 queued jobs AC4 -> drained job queue (tmp tsx processAvailableJobs since :3001 has no inline worker) -> reload -> imports show 'succeeded' AC4 + Proposals queue shows 'import' proposals: 'New note: My imported note' + 'New Person: Ada Lovelace' + 'New Person: Grace Hopper' AC3) + screenshot confirmed; temp drain script/specs/screenshots removed. dev-browser skill unavailable -> Playwright per prior convention. NOTE: the dev :3001 API runs no worker, so a real deployment needs WORKER_INLINE or the worker-process to drain import jobs (same as US-021/US-026 durable jobs)."
    },
    {
      "id": "US-032",
      "title": "Export and backup appropriately",
      "description": "As a Knowledge Base owner/admin, I want portable export and operator backup paths so I can avoid lock-in and maintain deployments.",
      "acceptanceCriteria": [
        "Owner/admin UI supports full-fidelity portable JSON export/import for a Knowledge Base.",
        "JSON export includes entities, claims, claim arguments, notes, sources, citations, schemas, schema versions, rules, provenance, and audit metadata as appropriate.",
        "Markdown export is clearly labeled human-readable and not guaranteed full-fidelity round trip.",
        "CSV/table export is labeled structured subset.",
        "Secrets such as API keys are excluded from portable graph exports.",
        "PostgreSQL dump/restore is documented as server operator/admin maintenance, not normal per-user portable export.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 32,
      "passes": true,
      "notes": "Portable export/import + backup docs. NO DB migration (all data exists from US-005). Shared schemas @jotmind/schemas exports.ts: portableKnowledgeBaseExportSchema (format='jotmind.kb.portable', formatVersion=1, fidelity='portable-graph', labels{json/markdown/csv}, secretPolicy{excludesProviderSecrets:true, excludedSecretFields=AI_PROVIDER_SECRET_FIELDS=['apiKey']}, knowledgeBase, data{schemas,entities,claims,notes,sources,citations,rules}, audit{note,events}) + portableImportResultSchema (counts+warnings); reuses entitySchema/claimSchema/noteSchema/sourceSchema/sourceExcerptViewSchema/auditEventSchema/ruleStatusSchema/propertySchemaSchema (do NOT redefine). portableSchemaDefinitionSchema carries FULL version history (versions[]) NOT just activeVersion (AC2). New schema-store read method dbSchemaStore.listDefinitionsWithVersions(kbId) (+ createMemorySchemaStore fake in schema-defs.test.ts updated) returns SchemaDefinitionWithVersions{activeVersion,versions[] asc}. apps/api/src/exports/: service.ts (buildPortableExport reads all stores; sanitizeJsonb DEEP-strips secret fields from every JSONB col AC5; importPortableKnowledgeBase), render.ts (renderMarkdownExport AC3 / renderEntitiesCsv AC4 — both LABELED non-round-trip), index.ts (createKbExportRouter + createPortableImportRouter). Export router mounted /api/knowledge-bases/:kbId/export (mergeParams AFTER KB router): GET /json|/markdown|/csv ALL require ADMIN+ KB role (backup is an admin op; non-members 404, viewers 403) with Content-Disposition attachment + correct content-types. IMPORT router mounted /api/knowledge-bases/import-portable (sibling path; KB router has no matching POST so it falls through): POST / authed+CSRF parses portableKnowledgeBaseExportSchema (400 invalid). Import ALWAYS creates a NEW KB owned by importer (no merge/dedupe) reusing canonical create paths (entityStore.createEntity/claimStore.createClaim w/ provenance/noteStore/sourceStore/sourceExcerptStore/schemaStore.createDefinition+updateDefinition/ruleStore). ID REMAPPING: preflight validates all refs (claim entity-args -> exported entities, entity/claim schemaVersionId -> exported versions, citations -> notes/sources/claims) and THROWS PortableImportError->400 BEFORE creating the KB (no partial KB on bad ref). Schema VERSION REPLAY: createDefinition makes v1, then each later exported version replayed via updateDefinition (every stored version is a breaking change in this dialect so it adds a new active version); versionMap maps old version id -> new version id by version NUMBER so records keep referencing their correct (possibly old) version. Rules: built-in rules (moduleId+packId) whose pack still exists via findBuiltinRulePack are reinstalled once (preserve provenance) then status set; others recreated via createRule (authored). Audit rows are EXPORTED for reference but NOT replayed (import generates its own audit; audit.note says so). AC6: docs/backup-restore.md documents portable JSON (full-fidelity)/Markdown/CSV + PostgreSQL pg_dump/restore as OPERATOR maintenance (not per-user export); README links it. Web: apps/web/src/KbAdmin.tsx <ExportBackup> (inside admin-gated <KbAdmin>, now takes csrfToken from App.tsx): 3 download <a href> (kb-export-json/-markdown/-csv) + file input (kb-import-file) that parses+POSTs via importPortableKnowledgeBase -> kb-import-result badge; api.ts portableJsonExportUrl/markdownExportUrl/csvExportUrl/importPortableKnowledgeBase. Tests: 6 schema unit (exports.test.ts) + 19 API unit (exports.test.ts in-memory fakes: auth 401, non-member 404, viewer 403, full JSON buckets+AC5 secret-not-leaked, markdown AC3 label, csv AC4 label/header, import csrf 403/invalid 400, import->new KB w/ remapped entity+claim+schemaVersion refs AC1, preflight bad-ref 400 no-KB-created). Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas193+web10/test:api 405pass+76skip/test:e2e); live compose DB curl e2e on :3001 (setup->KB->entity->claim->note->export/json 1e/1c/1n + audit 5 + secretPolicy + claim-arg==entity-id; markdown/csv labeled; import->NEW kb id != old, claim arg points to NEW entity; custom Widget schema breaking-update v1->v2 export shows versions[1,2] -> import replays 2 schemaVersions -> re-export shows [1,2]); Playwright+Chromium browser flow (login->select KB->Administration->Export & backup section w/ 3 download links correct hrefs + import file input) + screenshot confirmed. dev-browser skill unavailable -> Playwright per prior convention. GOTCHA: schema update route body is top-level {propertySchema|spec}, NOT wrapped."
    },
    {
      "id": "US-033",
      "title": "Harden deployment and operations",
      "description": "As an operator, I want clear self-hosted deployment paths so JotMind can run locally, on LAN, or on a user-managed server safely.",
      "acceptanceCriteria": [
        "Reference Docker Compose includes stateless Node API container and PostgreSQL container with AGE and `pgvector`.",
        "API can serve built frontend assets for simple single-origin deployment.",
        "Frontend can also be deployed as static assets with configurable API base URL.",
        "Backend binds to localhost by default; LAN binding requires explicit opt-in and authenticated access.",
        "CORS uses configurable allowed origins and never allows all origins by default.",
        "Docs distinguish local development, local/LAN production-style use, and VPS/cloud self-hosting.",
        "Public internet exposure docs require HTTPS or trusted TLS reverse proxy, allowed origins, strong auth, and hardening warnings.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 33,
      "passes": true,
      "notes": "Deployment hardening. CRITICAL: prod API runs via tsx (apps/api start script changed node dist/server.js -> tsx src/server.ts) because @jotmind/schemas exports TS SOURCE so compiled dist fails ERR_UNKNOWN_FILE_EXTENSION '.ts' (do NOT change schemas exports -> would break the no-build-step Vite/Vitest/tsc/tsx resolution + green pnpm verify). AC1: docker-compose.yml gained an 'api' service (build infra/api/Dockerfile multi-stage node:18+corepack pnpm, builds web assets, runtime runs tsx; depends_on db healthy; command runs db:migrate then start; WORKER_INLINE=true; port published to 127.0.0.1 by default w/ API_BIND/API_PORT overrides) alongside the existing PG18+AGE+pgvector db service; .dockerignore added so host node_modules/dist don't clobber the image. Stateless (all state in PostgreSQL). AC2 single-origin: app.ts new AppOptions.webDistPath (default env WEB_DIST_PATH); when set, AFTER all /api routers + /api/health it adds explicit app.use('/api',=>404 JSON {error:'Not found'}) so unknown API routes are NEVER swallowed, THEN express.static(webDist) + regex SPA fallback /^\\/(?!api(?:\\/|$)).*/ -> index.html. AC3 configurable web base URL: apps/web/src/api.ts API_BASE_URL=import.meta.env.VITE_API_BASE_URL (BUILD-time, trailing-slash stripped) + apiUrl()/apiFetch() helpers; all 65 fetch('/api...') calls + the 3 export-URL helpers routed through them (empty base = relative /api, single-origin/Vite-proxy default). AC4 binding: server.ts keeps HOST default 127.0.0.1 (loopback) + logs a security WARNING on non-loopback bind; LAN/public = explicit opt-in (HOST=0.0.0.0 / API_BIND). All data routes already require auth+CSRF (US-014). AC5 CORS deny-by-default: new apps/api/src/cors.ts parseAllowedOrigins(env) (never '*', empty default) + createCorsMiddleware (echoes ACAO only for EXACT allowlisted origin, Allow-Credentials true, preflight 204 allowed / 403 denied); wired before routes via createApp({allowedOrigins}) default CORS_ALLOWED_ORIGINS. AC6/AC7 docs: docs/deployment.md (local dev / local+LAN production-style / VPS-cloud modes + env table + public-internet hardening checklist: HTTPS/TLS reverse proxy, exact allowed origins, COOKIE_SECURE, strong auth, firewall PG, backups), linked from README new Deployment section; .env.example gained WORKER_INLINE/WEB_DIST_PATH/CORS_ALLOWED_ORIGINS/API_BIND/API_PORT/VITE_API_BASE_URL. Tests: 10 cors.test.ts unit (parse empty/trim/no-wildcard; middleware allow/deny/preflight-204/preflight-403/empty-allowlist-403/same-origin-passthrough) + 6 app.test.ts (CORS deny-by-default + allowlisted echo; unknown /api 404 JSON; static asset served + SPA fallback to index.html from a temp dir + /api 404 not swallowed + health still works). Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas193+web10/test:api 422pass+76skip/test:e2e); live single-origin instance (WEB_DIST_PATH + compose DB on :3010/:3011): /api/health JSON, / -> index.html, /some/spa/route 200 text/html, /api/nope 404 JSON, asset 200 application/javascript; CORS curl: preflight allowed origin 204+ACAO+credentials+methods+headers, denied origin 403 no ACAO, GET denied origin no ACAO; Playwright+Chromium loaded http://127.0.0.1:3011/ single-origin -> 200, rendered JotMind Sign in / Email / Password UI, zero page errors + screenshot. dev-browser skill unavailable -> Playwright per prior convention; temp script/screenshot removed. NOTE: Docker image build not run here (no registry/network); Dockerfile authored to the proven tsx runtime path."
    },
    {
      "id": "US-034",
      "title": "Complete V1 verification and hardening",
      "description": "As the product owner, I want V1 to have reliable verification so autonomous implementation can be trusted.",
      "acceptanceCriteria": [
        "`pnpm verify` runs `format:check`, `typecheck`, `lint`, `test`, `test:api`, and `test:e2e`.",
        "`pnpm verify` runs with real AI adapter keys blocked and all AI/embedding assertions use deterministic local mock providers only.",
        "Local inference/Ollama checks are excluded from `pnpm verify` and covered only by the manual `pnpm verify:local-ai` command.",
        "Critical UI flows have Playwright e2e coverage: login/setup, Knowledge Base selection, manual entity/claim CRUD, search/views, AI proposal review with mock provider, reasoning run, JSON export/import.",
        "API tests run against a test PostgreSQL container with migrations applied.",
        "Interactive browser verification is attempted for UI stories if tooling is available; unavailable tooling is recorded as deferred manual checks in `fix_plan.md` and does not block V1 when Playwright is green.",
        "Stubs remaining in V1 are documented, visible to users/admins when runtime-facing, and not on critical promised paths.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 34,
      "passes": true,
      "notes": "V1 verification & hardening. AC1: `pnpm verify` already chained format:check/typecheck/lint/test/test:api/test:e2e (root package.json) — kept as the authoritative NO-INFRA gate (stays green w/o DB/API/Ollama). AC2: `test:api` now runs with JOTMIND_VERIFY=true, loading new Vitest setupFile apps/api/src/test/verify-env.ts (registered in apps/api/vitest.config.ts) which ALWAYS strips real provider key env (OPENAI/ANTHROPIC/OPENAI_COMPATIBLE/AI_API_KEY) and, under JOTMIND_VERIFY, rejects any non-`mock` AI_PROVIDER_CONFIG or leaked apiKey — so every AI/embedding assertion uses the deterministic mock only (7 verify-env unit tests). Guard checks the STARTING env at file load, so tests that set AI_PROVIDER_CONFIG=mock dynamically are unaffected. AC3: `pnpm verify:local-ai` (root -> apps/api `vitest run --config vitest.local-ai.config.ts`) runs new apps/api/src/ai/ollama.local-ai.test.ts (skipIf !OLLAMA_BASE_URL); the default vitest.config.ts EXCLUDES `**/*.local-ai.test.ts` (vitest exclude blocks even an explicit CLI path -> needed the separate config) so Ollama checks never run in `pnpm verify`. AC4: new apps/web/e2e/v1-flows.real.spec.ts (Playwright `real-chromium` project, gated by E2E_REAL_API=1 in apps/web/playwright.config.ts) covers login/setup, logout/login, KB create+select, entity CRUD, claim CRUD (2 entity args), search + table/timeline views, AI proposal review w/ mock provider (set server+user+kb AI policy local_only -> capture -> mock proposal -> accept), rule authoring+enable+run (status completed), and portable JSON export+import. webServer array boots `@jotmind/api start` (mock AI injected via webServer env block — NOT inline shell, JSON braces/commas break env assignment) + vite dev; e2e/global-setup.ts runs db:migrate + db:reset:test first. GOTCHA fixed: per-KB sibling components fetch once per kb.id & don't live-sync, so the spec reloads+reselects the KB after creating entities before the Claims form reads them. AC5: `pnpm test:api:db` (compose DB) applies migrations then runs the FULL API suite incl all 18 integration files (verified 505 pass, 0 skipped). AC6: fix_plan.md documents the gates + records dev-browser unavailability as deferred manual checks (Playwright substitutes; doesn't block V1 while green). AC7: fix_plan.md documents remaining stubs (stubProjector non-default + now surfaced in GraphViews UI via new `graph-projector-stubbed` banner; mock AI labeled in capture UI; deferred query-time vector search shows *-vector-unavailable notices; AI schema-migration not implemented) — none on a critical promised path. New root scripts: test:api:db, test:e2e:real, verify:db, verify:local-ai; apps/api scripts db:reset:test + verify:local-ai; apps/web script test:e2e:real. Verified: `pnpm verify` FULLY GREEN (format/typecheck/lint/test schemas+web10/test:api 429pass+76skip/test:e2e smoke); `pnpm verify:local-ai` skips cleanly w/o Ollama; `pnpm test:api:db` 505/505 pass against compose DB (AC5); `pnpm test:e2e:real` the full v1-flows.real spec PASSES against compose DB + real API + mock AI (AC4). No DB migration (verification-only). dev-browser skill unavailable -> Playwright per prior convention."
    },
    {
      "id": "US-035",
      "title": "Establish selected-KB invalidation model",
      "description": "As a developer, I want a shared selected-KB invalidation mechanism so sibling panels can refresh consistently after mutations.",
      "acceptanceCriteria": [
        "Introduce one selected-KB invalidation approach such as a lightweight domain-keyed invalidation bus, selected-KB data context, or query/cache invalidation pattern.",
        "Invalidation domains include entities, claims, notes, sources, source excerpts, graph views, proposals, jobs, audit, schemas, rules, modules, AI policy, search results, command results, and answers.",
        "Avoid ad hoc parent refs for individual sibling panels unless a localized escape hatch is documented in code comments.",
        "Existing panel-local refresh behavior still works when a panel is used in isolation.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 35,
      "passes": true,
      "notes": "Selected-KB invalidation bus (lightweight domain-keyed pub/sub) in apps/web/src/invalidation.tsx. Exports: InvalidationDomain union + INVALIDATION_DOMAINS list (AC2: entities/claims/notes/sources/sourceExcerpts/graphViews/proposals/jobs/audit/schemas/rules/modules/aiPolicy/searchResults/commandResults/answers), createInvalidationBus() (Map<domain,Set<listener>>, dedupes a multi-domain listener to one fire per invalidate() call), InvalidationProvider (React context; useMemo one bus per mount), useInvalidate() (stable invalidate(domains) publisher), useInvalidationEffect(domains, onInvalidate) (subscribes; latest callback via ref, resubscribes only when domains key changes). Default context = NOOP_BUS so panels render in isolation w/o a provider (AC4 panel-local refresh unaffected; hooks are safe no-ops). Wired into App.tsx: <InvalidationProvider key={selectedKb.id}> wraps the workflow-grid so the bus resets per selected KB; no ad hoc parent refs added (AC3 - later stories US-036..US-045 wire individual panels). Bus does NOT own/cache data (signals \"domain changed, reload if you care\"); each panel keeps its existing fetch logic. Tests: 8 unit (invalidation.test.tsx): bus notify/no-cross-notify/dedupe-multi-domain/unsubscribe/all-domains-covered + hooks publish->subscribe within provider/no-cross-notify/safe-no-op-without-provider. Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web18/test:api 429pass+76skip/test:e2e). GOTCHA: pre-existing format:check failures on HEAD from the non-Ralph open-design ui redo commits (app.css/App.tsx/GraphViews.tsx + plugin-source json) blocked verify; fixed by prettier --write on the 3 source files + added plugin-source to .prettierignore (scaffolding). NOTE: eslint config has NO react-hooks/exhaustive-deps rule, so an eslint-disable for it ERRORS (\"Definition for rule was not found\") - dont add that disable; use a domainsRef + join-key dep instead."
    },
    {
      "id": "US-036",
      "title": "Refresh claim entity dropdowns after entity mutations",
      "description": "As a KB editor, I want entities created or changed in the Entities panel to be available immediately in claim argument controls so that I can keep building the graph without reloading.",
      "acceptanceCriteria": [
        "Creating an entity in `Entities` invalidates the entity data used by `Claims`.",
        "Editing, deleting, or merging an entity invalidates entity and claim-dependent data because merges can retarget claim arguments server-side.",
        "The claim argument entity dropdown shows newly created entities without page reload or KB reselect.",
        "Add a regression test for add entity -> claim argument dropdown updates.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 36,
      "passes": true,
      "notes": "Wired the US-035 invalidation bus into the Entities (publisher) + Claims (subscriber) panels in apps/web/src/App.tsx. Entities now calls useInvalidate(): createEntity -> invalidate(['entities']) (AC1/AC3); updateEntity/deleteEntity/mergeEntity -> invalidate(['entities','claims']) because edits change how an entity reads in claim arg controls and deletes/merges retarget claim arguments server-side (AC2). Claims subscribes via useInvalidationEffect(['entities','claims'], () => void refresh()); its existing refresh() already reloads BOTH listClaims + listEntities, so a single subscription refreshes the claim list AND the entity argument <select> options without a page reload or KB reselect (AC3). No API/schema/migration changes — pure web wiring of the existing bus. Both panels live inside <InvalidationProvider key={selectedKb.id}> (US-035) so the bus is shared per selected KB. AC4 regression: new App.test.tsx test 'shows a newly created entity in the claim argument dropdown without reload (US-036)' — selects a KB, asserts claim-arg-entity-0 has no 'Ada Lovelace' option, POSTs an entity via the Entities form (state-based fetch mock flips entities GET from [] to [created]), then waits for the option to appear in the dropdown via within(getByTestId('claim-arg-entity-0')). Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web19/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against the live compose DB + running tsx-watch API on :3001 (truncate users -> first-run setup -> create+select KB -> claim-arg-entity-0 has 0 'Frodo Baggins' options -> create Frodo Baggins entity -> dropdown option count becomes 1, no reload) + screenshot. dev-browser skill unavailable -> Playwright per prior convention; temp spec/screenshot removed."
    },
    {
      "id": "US-037",
      "title": "Refresh graph views after graph record mutations",
      "description": "As a KB user, I want graph views to reflect entity, claim, note, source, and citation changes immediately so that table, network, detail, source/note, and timeline views stay trustworthy.",
      "acceptanceCriteria": [
        "Entity create/update/delete/merge invalidates `GraphViews` data.",
        "Claim create/update/delete invalidates `GraphViews` data.",
        "Note, source, and source excerpt create/update/delete invalidates `GraphViews` data.",
        "GraphViews preserves existing projection-status banners and AGE fallback behavior while refreshing relational data.",
        "Add regression coverage for add entity/claim -> graph table or network updates.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 37,
      "passes": true,
      "notes": "Wired the US-035 invalidation bus so GraphViews refreshes after graph-record mutations. GraphViews.tsx (apps/web/src) now imports useInvalidate/useInvalidationEffect and SUBSCRIBES via useInvalidationEffect(['entities','claims','notes','sources','sourceExcerpts','graphViews'], () => void refresh()) — its existing refresh() reloads listEntities/listClaims/listNotes/listSources/listSourceExcerpts AND best-effort getGraphProjectionStatus, so all 4 views (network/timeline/table/sources) refresh while projection-status banners + AGE fallback are preserved (AC4 — refresh() keeps projection best-effort, no behavior change). PUBLISHERS in App.tsx: Entities now publishes 'graphViews' alongside its existing US-036 domains (create -> ['entities','graphViews']; update/delete/merge -> ['entities','claims','graphViews'], AC1). Claims gained const invalidate=useInvalidate() and publishes ['claims','graphViews'] on create/update AND delete (AC2). Capture gained invalidate and publishes on note create ['notes','graphViews'], source create ['sources','graphViews'], citation/excerpt create ['sourceExcerpts','graphViews'], note delete ['notes','sourceExcerpts','graphViews'], source delete ['sources','sourceExcerpts','graphViews'], excerpt delete ['sourceExcerpts','graphViews'] (AC3 — deletes also invalidate sourceExcerpts since citations are removed). GraphViews' own DetailView entity edit (onChanged) now also invalidate(['entities','claims','graphViews']) so sibling Entities/Claims panels refresh too. No API/schema/migration changes — pure web wiring of the existing bus. AC5 regression: new App.test.tsx test 'shows a newly created entity in graph views without reload (US-037)' — selects a KB (mocks entities/claims/notes/sources/source-excerpts/graph/projection/status), asserts network has 0 network-node-* nodes, POSTs an entity via the Entities form (state-based fetch mock flips entities GET from [] to [created]), then waits for [data-testid^=network-node-] to appear in GraphViews without reload/reselect. Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web20/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium real-chromium project against live compose DB + tsx API on :3001 (setup -> create+select KB -> GraphViews shows 0 network nodes -> create 'Grace Hopper' entity in Entities panel -> network-node count becomes 1 WITHOUT reload). dev-browser skill unavailable -> Playwright per prior convention; temp spec/screenshot removed."
    },
    {
      "id": "US-038",
      "title": "Refresh capture citation controls after claim mutations",
      "description": "As a KB editor, I want newly created claims to appear immediately in capture citation controls so that I can cite notes and sources without reloading.",
      "acceptanceCriteria": [
        "Claim create/update/delete invalidates the claims list used by `Capture` citation forms.",
        "New claims appear in citation dropdowns without page reload or KB reselect.",
        "Deleted claims no longer appear as selectable citation targets after refresh.",
        "Add a regression test for add claim -> capture citation dropdown updates.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 38,
      "passes": true,
      "notes": "Wired the US-035 invalidation bus into the Capture panel as a SUBSCRIBER of the 'claims' domain so newly created/updated/deleted claims appear in the citation claim dropdowns without a page reload or KB reselect. apps/web/src/App.tsx <Capture> already PUBLISHED notes/sources/sourceExcerpts/graphViews (US-037) but did NOT subscribe to claim changes; added useInvalidationEffect(['claims'], () => void refresh()) right after the per-kb.id useEffect. Capture's existing refresh() reloads listNotes/listSources/listClaims/listSourceExcerpts, so a single 'claims' subscription refreshes the claim list backing the citation-claim-<id> <select> in renderCitationForm (AC1/AC2) AND removes deleted claims as selectable targets after refresh (AC3). PUBLISHERS already in place from US-037: the Claims panel invalidates ['claims','graphViews'] on create/update (submit) AND delete — no publisher changes needed. No API/schema/migration changes — pure web wiring of the existing bus. AC4 regression: new App.test.tsx test 'shows a newly created claim in capture citation dropdowns without reload (US-038)' — selects a KB with one existing note (so the citation form + dropdown render), asserts citation-claim-<noteId> has no 'knows' option, creates a claim via the Claims form (predicate 'knows', a literal arg so no entity is needed; state-based fetch mock flips the claims GET from [] to [createdClaim] after POST), then waits for the 'knows' option to appear in the citation dropdown via within(getByTestId('citation-claim-<noteId>')). GOTCHA: the mock note + createdClaim fixtures must satisfy the @jotmind/schemas noteSchema/claimSchema (note needs properties:{}; claim needs properties:{} + each argument needs a valid uuid id + numeric position) or noteListSchema/claimListSchema.parse throws and the panel silently shows empty. Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web21/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against the live compose DB + tsx API on :3001 (truncate users -> first-run setup -> create+select KB -> create a Note so the citation form renders -> citation-claim dropdown has 0 'knows' options -> create a 'knows' claim with a literal arg in the Claims panel -> dropdown gains the 'knows' option WITHOUT reload). dev-browser skill unavailable -> Playwright per prior convention; temp spec removed."
    },
    {
      "id": "US-039",
      "title": "Refresh graph panels after proposal acceptance",
      "description": "As a KB editor, I want accepted proposals to update graph panels immediately so that AI capture and import review feel connected to the canonical graph.",
      "acceptanceCriteria": [
        "Accepting a proposal invalidates proposals, entities, claims, notes, sources, source excerpts, graph views, search results, answers, and audit data as applicable.",
        "Entities created by an accepted proposal appear in the Entities panel without reload.",
        "Claims created by an accepted proposal appear in Claims and GraphViews without reload.",
        "Notes or sources created by an accepted proposal appear in Capture/GraphViews without reload.",
        "Add regression coverage for accept proposal -> entities/claims/graph views update.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 39,
      "passes": true,
      "notes": "Wired the US-035 invalidation bus so accepting a proposal refreshes all sibling graph panels + derived AI surfaces without reload. PUBLISHER: Proposals.tsx <ProposalItem> gained const invalidate=useInvalidate(); the accept() handler now awaits acceptProposal then invalidate(['proposals','entities','claims','notes','sources','sourceExcerpts','graphViews','searchResults','answers','audit']) (AC1 — covers every domain an accept can affect since a batch accept creates entities/claims/notes/sources via the canonical write path; reject/edit unchanged, the panel's own onReviewed/refresh handles proposal-list removal). SUBSCRIBERS: (1) Entities (App.tsx) was publisher-only — added useInvalidationEffect(['entities'], ()=>void refresh()) so accepted-proposal entities appear in the Entities panel without reload (AC2). (2) Capture (App.tsx) subscription widened from ['claims'] to ['claims','notes','sources','sourceExcerpts'] so accepted-proposal notes/sources appear in Capture without reload (AC4). Claims already subscribes ['entities','claims'] (AC3) and GraphViews already subscribes ['entities','claims','notes','sources','sourceExcerpts','graphViews'] (AC3/AC4) from US-036/037/038 — no change needed there; its refresh() reloads all relational data + best-effort projection status so banners/AGE fallback preserved. No API/schema/migration changes — pure web wiring of the existing bus. AC5 regression: new App.test.tsx test 'shows entities/claims/graph nodes from an accepted proposal without reload (US-039)' — selects a KB with one pending create_entity proposal (Alan Turing), asserts no network-node-<id> in GraphViews, clicks proposal-accept-<id> (state-based fetch mock: accept endpoint flips `accepted=true` so entities GET returns [createdEntity] + proposals GET returns []), then waits for network-node-<entityId> to appear in GraphViews without reload/reselect. Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test schemas193+web22/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against live compose DB + a tsx API on :3005 (WORKER_INLINE+AI_DEMO_EXTRACTION+mock provider; :3001 was occupied by concurrent dev, so ran my own API on :3005 + temp vite proxy→3005 on :5175 since SameSite=Lax cookies need same-origin): first-run setup -> create+select KB -> set all 3 AI policy layers (ai-policy-server/-user/kb-ai-policy -mode+-save) to local_only -> quick-capture 'Ada Lovelace met Charles Babbage in London.' -> mock AI produced a pending proposal -> recorded network-node count -> clicked proposal-accept -> network-node count increased WITHOUT reload + zero page errors; screenshot confirmed entities (Ada Lovelace/Charles Babbage/London) + a mentioned_with claim present and 'No pending proposals' after accept. dev-browser skill unavailable -> Playwright per prior convention; temp spec/screenshot removed + vite.config proxy reverted to :3001."
    },
    {
      "id": "US-040",
      "title": "Keep import jobs, proposal queue, and job status in sync",
      "description": "As a KB editor, I want import job completion to update related job and proposal UI so that I know when imported candidates are ready for review.",
      "acceptanceCriteria": [
        "Import enqueue invalidates jobs and audit data.",
        "Import job completion either polls or provides a clear refresh path that updates both import status and the proposal queue.",
        "When an import creates a pending proposal, the Proposals panel can show it without page reload or KB reselect.",
        "`KbAdmin` job status does not remain permanently stale after selected-KB jobs change.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 40,
      "passes": true,
      "notes": "Wired the US-035 invalidation bus so import enqueue/completion keeps the Imports, Proposals, and KbAdmin panels in sync without reload. NO API/schema/migration changes — pure web wiring (apps/web/src). PUBLISHER: Imports.tsx submit() now calls invalidate(['jobs','audit']) right after a successful createImport (AC1 — enqueue creates a durable job + audit event). COMPLETION DETECTION (AC2): Imports.refresh() now diffs each job's status against a lastStatusRef Map; when a job transitions from non-terminal -> terminal (TERMINAL_JOB_STATUSES = succeeded/failed/cancelled) it calls invalidate(['proposals','jobs','audit']) so a freshly-produced pending proposal surfaces. Two refresh paths drive this: (a) the existing 'Refresh status' button (imports-refresh — the 'clear refresh path' of AC2) and (b) a NEW poll: while any job is non-terminal (hasPendingJobs), a setInterval(refresh, 3000) runs and is torn down once all jobs are terminal (no infinite loop — completion sets lastStatusRef terminal so the next refresh detects nothing). Imports also subscribes useInvalidationEffect(['jobs'], refresh) so external job changes refresh its list (self-limiting). SUBSCRIBERS: Proposals.tsx (outer component) added useInvalidationEffect(['proposals'], refresh) so an import-created pending proposal appears in the queue without reload/reselect (AC3). KbAdmin.tsx refactored its inline Promise.all useEffect into a useCallback load() + useEffect(load) and added useInvalidationEffect(['jobs','audit'], load) so job/audit status no longer stays permanently stale after selected-KB jobs change (AC4). All three panels already live inside <InvalidationProvider key={selectedKb.id}> (App.tsx). GOTCHA: publicJobSchema requires runAfter/payload/ownerUserId/knowledgeBaseId (not runAt) — test fixtures must match or publicJobSchema.parse throws and listImports/listKbJobs silently error. JobStatus type is exported via @jotmind/schemas graph.js. AC regression: new App.test.tsx test 'shows a proposal from a completed import without reload (US-040)' — selects an owner KB, asserts a running import job renders + proposals-empty, flips a state-based fetch mock (importFinished) so imports/jobs GET return 'succeeded' and proposals GET returns [proposal], clicks imports-refresh, and waits for proposal-<id> to appear without reload. Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web23/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against live compose DB + my own tsx API on :3055 with WORKER_INLINE=true (the dev :3001 API runs NO worker so imports never process) + a temp vite on :5180 pointed at it via a temporary VITE_PROXY_TARGET env hook in vite.config (reverted after): first-run setup -> create+select KB -> proposals-empty -> CSV import (Ada Lovelace/Charles Babbage) -> inline worker built the proposal -> proposal item appeared AND kb-jobs-list showed the import job, all without reload. dev-browser skill unavailable -> Playwright per prior convention; temp config/spec removed + vite.config reverted."
    },
    {
      "id": "US-041",
      "title": "Refresh claims and graph views after accepting inferred rule results",
      "description": "As a KB editor, I want claims created from rule inferences to appear in Claims and GraphViews immediately so that accepted reasoning results become visible canonical graph data.",
      "acceptanceCriteria": [
        "Accepting an inferred result invalidates claims, graph views, search results, answers, and audit data.",
        "The new inferred claim appears in the Claims panel without reload.",
        "The new inferred claim appears in GraphViews without reload.",
        "The Rules panel continues to mark the accepted inferred result as accepted.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 41,
      "passes": true,
      "notes": "Wired the US-035 invalidation bus so accepting an inferred rule result refreshes Claims + GraphViews (and derived AI surfaces) without reload. PUBLISHER: Rules.tsx (apps/web/src) gained `import { useInvalidate }` + `const invalidate = useInvalidate()`; the existing accept(runId,resultId) handler now, AFTER acceptInferredResult succeeds and AFTER setAcceptedResults marks the result accepted (AC4 unchanged), calls invalidate(['claims','graphViews','searchResults','answers','audit']) (AC1 — the union of domains a server-side accept mutates: it creates a canonical claim via claimStore.createClaim, which projects to AGE + audit and can change search/answer freshness). SUBSCRIBERS already in place: Claims subscribes ['entities','claims'] (US-036) so the new inferred claim appears in the Claims panel without reload (AC2); GraphViews subscribes ['entities','claims','notes','sources','sourceExcerpts','graphViews'] (US-037) and its refresh() reloads all relational data + best-effort projection status so the claim appears in GraphViews while banners/AGE fallback are preserved (AC3) — no subscriber changes needed. Rules is rendered inside <InvalidationProvider key={selectedKb.id}> (App.tsx) alongside Claims/GraphViews so the bus is shared per selected KB. No API/schema/migration changes — pure web wiring of the existing bus. Regression test: new App.test.tsx test 'invalidates claims and graph views after accepting an inferred result (US-041)' renders <Rules> + a Probe subscriber (useInvalidationEffect(['claims']) and (['graphViews'])) inside a real <InvalidationProvider>, mocks the rules endpoints (packs [], rules [enabled+valid rule], run -> run+1 inferred result, accept -> {claimId,result}), clicks Run then Accept, and asserts both subscribers fired AND the Rules panel shows rules-run-accepted-<id> (AC1/AC2/AC3/AC4). Verified: pnpm verify FULLY GREEN (format/typecheck/lint/test web24/test:api 429pass+76skip/test:e2e smoke). Browser-verified via Playwright+Chromium against an ISOLATED throwaway DB (jotmind_us041 in the compose container) + my own tsx API on :3091 (WORKER_INLINE) + own vite on :5191 (temp VITE_PROXY_TARGET hook, reverted) so the concurrent :3001/:5173 dev stack was untouched: first-run setup -> create+select KB -> create Ada+Babbage entities -> create a 'knows' claim (subject=Ada, object=Babbage) -> claims-list count 1 -> author+validate+save the default knows-symmetry rule -> enable -> run -> inferred result -> Accept as claim -> claims-list count became 2 WITHOUT reload + rules-run-accepted shown + zero page errors. dev-browser skill unavailable -> Playwright per prior convention; temp spec/DB/processes cleaned up + vite.config reverted."
    },
    {
      "id": "US-042",
      "title": "Refresh schema and rule panels after module installation",
      "description": "As a KB editor, I want installed modules to update schema and rule sibling panels immediately so that newly installed module content is discoverable.",
      "acceptanceCriteria": [
        "Installing a module invalidates modules, schemas, rules, and audit data.",
        "New module-provided schema definitions appear in `SchemaDefinitions` without reload or KB reselect.",
        "New module-provided rule packs or installed rules appear in `Rules` without reload or KB reselect.",
        "The Modules panel still shows accurate installed/skipped status for idempotent installs.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 42,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-043",
      "title": "Mark search, command, and answer results stale after graph changes",
      "description": "As a KB user, I want previous search, command, and answer results to be marked stale or rerun after graph data changes so that I do not mistake old results for current state.",
      "acceptanceCriteria": [
        "Entity, claim, note, source, source excerpt, proposal-accept, and inferred-claim mutations invalidate search, command, and answer result freshness.",
        "`Search`, `CommandBox`, and `Answers` either visibly mark existing results as stale or automatically rerun the last query when relevant data changes.",
        "If auto-rerun is used, it avoids repeated network loops and keeps user-entered drafts intact.",
        "Stale-state messaging tells the user what action to take, for example `Graph data changed. Rerun this search for current results.`",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 43,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-044",
      "title": "Refresh Knowledge Base list after portable import",
      "description": "As a user importing a portable JSON backup, I want the imported Knowledge Base to become selectable immediately so that backup restore feels complete.",
      "acceptanceCriteria": [
        "Successful portable JSON import in `KbAdmin` invalidates the parent Knowledge Base list.",
        "The newly imported Knowledge Base appears in the Knowledge Base selector/list without page reload.",
        "Import success messaging identifies the imported Knowledge Base name or id when available.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 44,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-045",
      "title": "Refresh AI policy availability surfaces after policy changes",
      "description": "As a KB user, I want server, user, and KB AI policy changes to update every visible effective-policy and AI-availability indicator so that privacy controls are clear and current.",
      "acceptanceCriteria": [
        "Updating server AI policy invalidates global policy state, selected-KB effective policy, and AI availability surfaces.",
        "Updating user AI policy invalidates global policy state, selected-KB effective policy, and AI availability surfaces.",
        "Updating KB AI policy invalidates selected-KB effective policy and AI availability surfaces.",
        "`CommandBox`, `Answers`, quick capture/proposals, and embeddings UI reflect current effective policy without reload.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 45,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-046",
      "title": "Show AI provider configuration and readiness in the UI",
      "description": "As an admin/operator, I want to understand which AI provider is active and whether it is usable so that I can diagnose AI availability without reading environment variables.",
      "acceptanceCriteria": [
        "The UI shows the active provider kind when backend configuration exposes it: `ollama`, `openai-compatible`, `openai`, `anthropic`, or `mock`.",
        "The UI shows provider label/name, LLM model, embedding model, base URL, verification/readiness state, and local-vs-remote classification when available.",
        "If provider configuration remains environment-only, the UI clearly states that provider config is managed by server environment and cannot be edited in the browser.",
        "API keys and other secrets are never displayed, returned unnecessarily to the browser, logged in client metadata, or included in portable KB exports.",
        "Mock providers are visibly labeled as deterministic demo output when exposed.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 46,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-047",
      "title": "Handle provider configuration editing affordances safely",
      "description": "As an admin/operator, I want provider setup controls to be either safely editable or clearly marked environment-only so that I do not enter secrets into non-functional UI.",
      "acceptanceCriteria": [
        "If in-browser provider editing is implemented, only system admins can create/update server-level provider configuration.",
        "If provider editing is implemented, the form supports provider kind, display name/label, base URL where applicable, LLM model, embedding model, and API key where applicable.",
        "Existing API keys are treated as write-only secrets; the UI must not reveal stored secret values.",
        "Saving provider configuration validates required fields for the selected provider kind when editing is supported.",
        "If persisted provider config is deferred, the UI shows no non-functional edit controls and points to environment-only setup guidance.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 47,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-048",
      "title": "Show embedding status for the selected Knowledge Base",
      "description": "As a KB user, I want to see embedding index status for the selected Knowledge Base so that I understand whether vector search and embedding generation are available.",
      "acceptanceCriteria": [
        "Add API helpers in `apps/web/src/api.ts` for `GET /api/knowledge-bases/:kbId/embeddings`.",
        "Add an `EmbeddingsPanel` or equivalent selected-KB UI surface near KB AI policy/admin automation tooling.",
        "The panel shows vector search available/unavailable and generation available/unavailable as distinct states.",
        "The panel shows unavailable reasons when provided by the backend.",
        "The panel shows total embeddings, counts by target type, model, dimensions, last indexed time, and provider kind when present in the response.",
        "Viewer-role users can read embedding status if the backend permits the route.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 48,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-049",
      "title": "Enqueue embedding reindex jobs from the UI",
      "description": "As a KB editor, I want to enqueue embedding reindex jobs from the web UI so that I can rebuild vector search after graph changes or provider changes.",
      "acceptanceCriteria": [
        "Add API helper in `apps/web/src/api.ts` for `POST /api/knowledge-bases/:kbId/embeddings/reindex`.",
        "KB editors can trigger `Reindex embeddings`; viewers cannot trigger it.",
        "Editors can optionally choose target types: entity, claim, note, and source.",
        "If remote embeddings would be used, the UI preserves remote embedding consent as separate from remote LLM consent.",
        "After enqueueing, the UI shows the returned job id/status and points users to the selected-KB job status panel or displays a direct status link/summary.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 49,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-050",
      "title": "Confirm remote AI calls when policy requires per-request consent",
      "description": "As a privacy-conscious user, I want explicit confirmation before each remote AI call when effective policy is `remote_per_request` so that remote data sharing is intentional.",
      "acceptanceCriteria": [
        "Command interpretation asks for confirmation before a remote provider call under `remote_per_request`.",
        "AI answer generation asks for confirmation before a remote provider call under `remote_per_request`.",
        "Quick capture extraction asks for confirmation before a remote provider call under `remote_per_request`.",
        "Embedding reindex asks for confirmation before remote embedding generation under `remote_per_request` and treats remote embeddings as a distinct consent category.",
        "Confirmation discloses provider, model, feature name, and categories of content being sent.",
        "Confirmation does not display raw prompts, API keys, or hidden system metadata.",
        "Remote-call audit metadata uses allowlisted fields only and does not include raw prompts or secrets.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 50,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-051",
      "title": "Promote command create suggestions into proposal flow",
      "description": "As a KB editor, I want command `create` suggestions to become reviewable proposals so that useful AI-created structure can be accepted through the normal safe workflow.",
      "acceptanceCriteria": [
        "When `CommandBox` receives a `create` interpretation and the user is an editor, it offers a `Create proposal from suggestion` action if backend support exists.",
        "The action stores structured candidate changes as a pending proposal and never writes directly to canonical graph records.",
        "Provider, model, provenance, and confirmation metadata are preserved when available and safe to store.",
        "The new proposal appears in the Proposals panel without reload.",
        "If backend proposal creation from command interpretation is deferred, the create preview is clearly labeled as non-actionable preview-only and the UI does not imply it has been queued.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 51,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-052",
      "title": "Improve proposal editing beyond raw JSON",
      "description": "As a KB editor reviewing AI or import proposals, I want structured editing controls for common proposal item types so that I do not need to hand-edit JSON for routine corrections.",
      "acceptanceCriteria": [
        "Pending proposal items for create entity can be edited through structured fields for type, name, aliases/tags where supported, description, and properties.",
        "Pending proposal items for create claim can be edited through structured fields for predicate, arguments, confidence, timestamps/properties where supported, and provenance-safe metadata.",
        "Pending proposal items for create note and create source can be edited through structured title/content/source fields where supported by the proposal schema.",
        "Raw JSON editing remains available as an advanced escape hatch.",
        "Structured edits are validated against the same proposal change schema before saving.",
        "Invalid edits cannot be accepted or executed.",
        "Tests pass",
        "Verify in browser using dev-browser skill",
        "Typecheck passes"
      ],
      "priority": 52,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-053",
      "title": "Add frontend coverage for existing AI policy and AI workflow states",
      "description": "As a developer, I want focused frontend tests for AI-related UI states so regressions in privacy and review workflows are caught early.",
      "acceptanceCriteria": [
        "Add or extend tests for server/user AI policy display and editing role gates.",
        "Add or extend tests for KB AI policy display and editing role gates.",
        "Add or extend tests for command interpretation states: unavailable, interpreted search, low-confidence/fallback, and create preview.",
        "Add or extend tests for quick capture extraction states: created, empty, unavailable, and error.",
        "Add or extend tests for proposal accept/reject/edit visible behavior.",
        "Tests use deterministic mock providers and do not require remote provider credentials or network calls.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 53,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-054",
      "title": "Add frontend coverage for new embedding and provider UI",
      "description": "As a developer, I want tests for embedding and provider UI so operators can trust setup and indexing screens.",
      "acceptanceCriteria": [
        "Add tests for embedding status available/unavailable states.",
        "Add tests for embedding generation unavailable reasons.",
        "Add tests for reindex action visibility by role.",
        "Add tests for reindex enqueue success showing job id/status.",
        "Add tests for provider configuration/readiness display, including local/remote/mock labels.",
        "Tests assert that API keys or secret values are not rendered.",
        "Tests pass",
        "Typecheck passes"
      ],
      "priority": 54,
      "passes": false,
      "notes": ""
    }
  ]
}